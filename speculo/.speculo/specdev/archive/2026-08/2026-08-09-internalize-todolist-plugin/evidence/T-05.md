# Evidence T-05: 周期与历史

## 1. 结果摘要

T-05 已完成。daily/weekly/monthly recurrence 有界物化、稳定 occurrence identity、skip/complete history 和 rule version 更新均在插件私有 store 内完成。

## 2. 修改范围

`<Path>plugins/todolist/src/service.ts</Path>`、`src/types.ts`、`routes/api.ts`、`tools/recurrence.ts` 和 `recurrence.integration.test.ts`。

## 3. 验收与合同映射

- AC-015：daily materialize 同一窗口重复调用返回 0 新 occurrence；afterCompletion 只由显式 `completeOccurrence` 创建一个 next occurrence。
- AC-016/029：skip 状态和历史日期保持不可变；rule edit 只递增 rule version，不重写既有 occurrence。

## 4. 验证执行

`recurrence.integration.test.ts` 3/3，插件套件 24/24；stable id、idempotency、skip/history、version assertions 全部通过。

## 5. 双轴审查

行为轴：occurrence identity、bounded horizon 和事务更新闭环。质量轴：通用 Todo complete 不再隐藏物化副作用；服务端无第二 timer/scanner。

## 6. 路径所有权审计

产品与测试变更全部在 `<Path>plugins/todolist/**</Path>`。

## 7. 偏差与决策

无偏差；不做复杂 RRULE 或无限期生成。

## 8. 残余风险与后续

TaskRegistry 真实重启/晚就绪交接需 Hana 提供稳定宿主能力，由 T-06 记录边界。
