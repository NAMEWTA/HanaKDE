# Evidence T-04: 类型化时间与聚焦视图

## 1. 结果摘要

T-04 已完成。时间模型区分 floating date 与 exact instant，并保存 IANA zone/offset；Today/Upcoming/Calendar 由同一 query projection 提供。

## 2. 修改范围

实现位于 `<Path>plugins/todolist/src/types.ts</Path>`、`src/service.ts`、routes、插件测试和 React Page。

## 3. 验收与合同映射

- AC-005/030：attentionDate 取 planned/deadline 较早值；视图不重复写数据，Completed/Calendar 是同源投影。
- AC-008：typed-time 测试覆盖 floating date、exact instant、非法日期和 America/New_York DST metadata；页面表单显式暴露 planned/deadline。
- AC-009/031：时间字段不会隐式启用 reminder/agent；五语言 UI matrix 通过。

## 4. 验证执行

`typed-time.integration.test.ts`、后台能力回归和其余插件测试合计 24/24；Playwright 10/10；`npm run typecheck`、双 tsconfig typecheck exit 0。无 Python 时间逻辑。

## 5. 双轴审查

行为轴：严格校验 IANA zone、offset range、mode trigger；质量轴：导航可聚焦、状态使用 aria-live、窄宽度 flex 不溢出。

## 6. 路径所有权审计

只写 `<Path>plugins/todolist/**</Path>`，未改全局日期/时区工具。

## 7. 偏差与决策

无偏差；复杂自然语言时间解析明确 deferred。

## 8. 残余风险与后续

宿主未提供完整 native calendar surface，因此 Calendar 当前是同源列表投影，不伪造外部日历能力。
