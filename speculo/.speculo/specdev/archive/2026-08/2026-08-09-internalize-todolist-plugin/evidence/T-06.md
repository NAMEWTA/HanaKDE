# Evidence T-06: 调度就绪与提醒 handoff

## 1. 结果摘要

T-06 已完成插件侧合同。reminder 使用宿主 `task:schedule` 请求，wake 先 claim 再 emit 现有 notification event；状态 `handed_off` 明确返回 `delivered:false`，TaskRegistry 失败会落为可见 `failed` 与诊断。

## 2. 修改范围

`<Path>plugins/todolist/index.ts</Path>`、`src/service.ts`、`routes/api.ts`、`tools/reminder.ts` 和 `scheduler-reminder.integration.test.ts`。

## 3. 验收与合同映射

- AC-009/012：默认 manual，reminder 与 agent_execute 显式独立。
- AC-010/011：stable reminder identity、claim 幂等、重复 wake 不重复 emit；调度失败不会伪装成普通创建成功。
- AC-023/024/029：插件 onStartup 注册单一 handler，宿主不可用时诊断并保持 CRUD；没有 due scanner 或第二 timer。

## 4. 验证执行

`scheduler-reminder.integration.test.ts` 与 `background-capabilities.integration.test.ts` 覆盖 handoff、取消和 TaskRegistry 失败；PluginManager integration 在 fake bus 下输出 `reminder handler unavailable; CRUD remains available`；插件套件 24/24，静态扫描未发现插件源中的 `setInterval`/`setTimeout`。

## 5. 双轴审查

行为轴：claim/handoff/repeat/failure 状态可观察。质量轴：不冒充 delivered，不自动重发，不吞宿主请求错误。

## 6. 路径所有权审计

只修改 `<Path>plugins/todolist/**</Path>`，未调整 TaskRegistry 启动顺序或 NotificationService。

## 7. 偏差与决策

无偏差。notification receipt、Bridge 和渠道路由不是当前 Hana 能力，按用户边界 deferred；因此未声称真实桌面送达。

## 8. 残余风险与后续

待 Hana 对外提供 scheduler readiness receipt/notification bridge 后，补做 live host restart/late-readiness E2E；插件当前 fail-closed。
