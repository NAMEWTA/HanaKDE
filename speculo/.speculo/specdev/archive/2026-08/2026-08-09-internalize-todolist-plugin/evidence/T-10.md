# Evidence T-10: 完整 builtin 产品发布 Gate

## 1. 结果摘要

T-10 已完成并关闭 G5。`plugins/todolist/` 是可发现、可构建、可整块移除的 TypeScript/React builtin；五语言和桌面/窄布局用户流程已执行，后台能力失败也有明确可恢复诊断。

## 2. 修改范围

产品写入仅为 `<Path>plugins/todolist/**</Path>`，包含 `manifest.json`、`package.json`、TS 服务/routes/tools、TSX Page、构建脚本/资产、Vitest 和 Playwright fixture/config。SpecDev Evidence/status 是流程产物，不是产品越界。

## 3. 验收与合同映射

- AC-001/003/025/029/032/033：PluginManager 发现、namespaced tool catalog、route import、构建资产、path audit 和 removal smoke 通过。
- AC-023/031：插件启动时 capability 不可用会诊断降级；Playwright 运行 `zh-CN/zh-TW/ja/ko/en` 的 desktop 1280x800 与 narrow 390x844，共 10/10，并生成插件内截图。
- AC-009～012/017～024/026/027～031：T-06～T-09 与后台能力回归覆盖应用状态机、隐私和交换合同；缺失宿主 capability 按 non-goal/deferred 记录。

## 4. 验证执行

通过：creator preflight；SpecDev implement/complete validator 0 error/0 warning；插件 Vitest = 11 files/24 tests；Playwright = 10 passed（五语言、desktop/narrow，包含 CSS 资产）；`npm run typecheck`；双 tsconfig typecheck；插件 lint = 0 errors/69 warnings；`node --experimental-strip-types plugins/todolist/build.ts`；`git diff --check`；route/build/removal smoke。

分类失败：根 `npm test` 为 47 failed/1168 passed/1 skipped，259 failed tests，主要是 better-sqlite3 Node ABI mismatch、macOS knowledge probe timeout 和已有 desktop assertions；不引用插件路径，也未改根测试。`npm run lint` 为既有全仓基线 1382 errors/16298 warnings，插件专属 lint 无 error。`npm run build:server` 在既有根依赖检查处失败：root `esbuild` 缺失；`npm run verify:seed-kit` 因既有 `dist-server-artifact/mac-arm64/seed-train-darwin-arm64.json` 缺失失败。两者均未通过跨界补丁修复，插件 build 自身已通过。

## 5. 双轴审查

行为轴：CRUD、Trash/restore/purge、capture/project、typed time/views、recurrence/history、reminder handoff、Run/Attempt/cancel、Automation、exchange/review 均有对应测试和 route/tool/UI 接缝。质量轴：TypeScript only、组件/ARIA/locale/narrow、敏感字段 redaction、稳定错误、无第二 scheduler、无 batch/NLP/shortcut；插件 lint 仅 warnings。

## 6. 路径所有权审计

实施期间没有生成 worktree；产品目录外的现有 dirty changes（其它 SpecDev changes、宿主/其它模块）未触碰。隔离删除 smoke 仅复制并移动临时目录中的 todolist，确认 manifest/资产可恢复后清理临时文件；当前工作区插件目录不受影响。`git status --short -- plugins/todolist` 仅为本 change 新目录。

## 7. 偏差与决策

无未批准 deviation、blocker 或 unverified 产品声明。按用户决定未实现批量创建、快捷键、NLP；按 HanaKDE 当前能力未私造 notification receipt/Bridge、session.open、原生日历/移动 surface、旧 SQLite migration。

## 8. 残余风险与后续

待 Hana 提供 scheduler readiness receipt、notification Bridge/receipt、session.open 和流式交换能力后，可在插件目录内补 live-host E2E；在此之前 reminder 仍诚实为 `handed_off`/`delivered:false`，Agent 仅保存最小结果与稳定 sessionRef。根级 server/seed/full-suite 基线失败不阻断插件源码闭环，但发布前仍需由宿主工程修复并重新运行 G5。
