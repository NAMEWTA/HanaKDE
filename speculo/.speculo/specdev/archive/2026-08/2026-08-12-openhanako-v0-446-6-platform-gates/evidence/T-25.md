# Evidence: T-25 - umbrella final acceptance

- **Change:** `2026-08-12-openhanako-v0-446-6-platform-gates`
- **Ticket:** `<Path>{roots.state}/specdev/changes/{change}/ticket/25-final-umbrella-acceptance.md</Path>`
- **Current branch/result:** `hanakde` at `f29abef4a7a79ac9eefebe0ed4597f1252a2b29c`
- **Implementation ancestor:** `f444b0e5884ca595c6edd8fd3d57d6f9ff2f7b0f`
- **Owner:** Root Lead / final-acceptance-owner
- **Status:** done with approved platform-evidence waiver

## 1. 实现摘要

**Final verdict: ACCEPTED WITH EXPLICIT WAIVER.** T-27、T-28、T-29 和真实 Windows T-22 已完成；macOS arm64 的 watcher、reconcile、package、DMG 与 direct-flow Evidence 已存在并在当前分支复核。用户于 2026-08-22 明确取消当前无法获得的 macOS x64、物理 sleep/wake、literal kernel descriptor 及相关最终 package/startup/repair 重跑，并要求归档。

T-23 因此为 `cancelled`，不是 `passed`。本 Evidence 不声称 macOS x64、物理睡眠或 literal descriptor 已验证；这些缺口作为残余风险归档。

## 2. Lead Dispatch And Candidate Return

- **Execution mode:** current workspace；用户禁止 worktree。
- **Implementation owner:** Lead，未派遣 subagent。
- **Candidate return:** 不适用；当前策略使用 direct-parent。
- **Result SHA:** `f29abef4a7a79ac9eefebe0ed4597f1252a2b29c`。
- **Authorization:** 用户先授权在当前分支完成 Tickets，随后明确授权取消不可用 macOS 测试并进入归档。

## 3. 修改范围与路径所有权

T-25 仅改写当前 change 的 SpecDev Evidence、Ticket、Map、Goal Plan 和状态投影；没有修改产品代码、依赖、lockfile、发布配置、真实用户数据或远程状态。功能实现仍位于系统本体的 runtime/startup 与 platform validation 路径，不属于插件贡献面。

## 4. 验收与合同映射

| 合同范围 | Verdict | Evidence / disposition |
|---|---|---|
| AC-001—AC-008 | accepted | 既有 integration、Knowledge、History、Agent 与 Resource Evidence；当前实现祖先关系成立 |
| AC-009—AC-010 | accepted with waiver | 真实 Windows T-22 通过；macOS arm64 single-watcher/cutover 通过；macOS x64/literal descriptor 不再要求 |
| AC-011 | accepted | EventBus / plugin adapter focused matrix 通过 |
| AC-012—AC-013 | accepted with waiver | macOS arm64 deterministic gap/reconcile 与健康状态通过；物理 sleep/wake 不再要求 |
| AC-014—AC-023 | accepted with waiver | 真实 Windows native/package 与 macOS arm64 security/restore/extraction/direct-flow Evidence 可定位；macOS x64 重跑不再要求 |
| AC-024—AC-026 | accepted | Workbench/Agent、new-store 与 security schema Evidence 可定位 |
| AC-027 | accepted with waiver | Windows NSIS/install/start/repair 通过；macOS arm64 app/DMG/direct-flow 通过；macOS x64 package 不再要求 |
| AC-028 | accepted | 结构、owner 和架构审查无新增 finding |
| AC-029—AC-031 | accepted with waiver | runtime dependency、startup recovery、optional JSON 合同与真实 Windows package 通过；macOS x64 最终重跑不再要求 |

原 15 项 umbrella DoD 以相同原则接受：1—11、14—15 使用已有 Evidence；12—13 使用真实 Windows 与 macOS arm64 native/package Evidence，并对缺失的 macOS x64、物理睡眠和 literal descriptor 行应用用户批准的 waiver。

## 5. Workspace Verification

- **current-workspace:** `hanakde`，HEAD `f29abef4a7a79ac9eefebe0ed4597f1252a2b29c`，`f444b0e5` 为其祖先。
- `volta run --node 24.16.0 -- npm run verify:runtime-deps`：pass，49 个 production dependencies 与 Pi AI 已验证。
- 目标 Vitest：14 files，143 passed，1 个明确的 real-Windows-only skip。
- `volta run --node 24.16.0 -- npm run typecheck`：pass。
- `volta run --node 24.16.0 -- npm run build:client`：pass。
- changed-path ESLint：0 errors；repository-wide quiet lint 的 7 个错误仅位于用户已有、与本 change 无关的未跟踪 `<Path>speculo/skills/source-code-zip/scripts/zip_source_code.js</Path>`。
- `volta run --node 24.16.0 -- node scripts/platform/macos/run-gate.mjs`：darwin/arm64 pass，观察到 single watcher lease、zero cutover overlap、loss/reconcile 和完整健康状态序列。
- `git diff --check`：pass。

## 6. 双轴审查

- **Standards axis:** pass。实现位于既有系统本体与 platform harness 边界；当前 diff 未发现新增代码缺陷、路径越权、依赖漂移或错误吞没。
- **Specification axis:** accepted with approved waiver。已有行为和真实 Windows/macOS arm64 Evidence 满足用户当前验收范围；缺失 macOS 环境行被取消而非伪装通过。

## 7. Integration Verification

- `61dcfcba5b7fe92cbf0c3219d1ac3d4114b41875` 是当前结果祖先。
- `f444b0e5884ca595c6edd8fd3d57d6f9ff2f7b0f` 包含 T-27/T-28/T-29/T-22 实现与 Evidence，且是 `f29abef4` 祖先。
- 当前分支使用 direct-parent，未创建 Ticket worktree、candidate branch、PR、push 或 release。
- T-25 为只读验收汇合；现有产品 result SHA 保持 `f29abef4`。

## 8. 偏差与决策

- **D-T25-02 (approved):** 用户于 2026-08-22 取消真实 macOS x64、物理 sleep/wake、literal descriptor 及依赖这些环境的最终 package/startup/repair 重跑。
- **Disposition:** T-23 = `cancelled`；T-25 = `done`；缺失行不计为 pass。
- **Scope:** 只收缩本 change 的验收 Evidence，不改变产品行为，不形成 macOS x64 或生产发布能力声明。

## 9. 残余风险与交付定位

- macOS x64 runtime/DMG、物理 sleep/wake 与 literal kernel descriptor accounting 仍未验证。
- 当前验收不能作为生产签名、公证、发布或全架构 macOS 认证。
- Repository-wide lint 仍受用户无关的未跟踪脚本错误影响；本 change 的 changed-path lint 与目标回归通过。
- **Delivery:** 当前 `hanakde` 分支 `f29abef4a7a79ac9eefebe0ed4597f1252a2b29c`。
- **Evidence:** `<Path>{roots.state}/specdev/changes/{change}/evidence/T-25.md</Path>`。
