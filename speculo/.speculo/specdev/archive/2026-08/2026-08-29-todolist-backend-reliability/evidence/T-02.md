# Evidence: T-02 — Todo 系统 UI 与 AI 工具合同

- **Workspace/branch：** `current` / `hanakde`
- **环境：** macOS Electron Desktop；本地真实 Server `127.0.0.1:37453`；authenticated plugin surface。
- **状态：** 实现、专项验证与本地集成完成；实现提交 `b0c74282`。

## Implemented

- Todo 页面改为 Hana 工作台式侧栏、平面列表与详情检查器。
- CSS 直接继承宿主 `--bg`、`--bg-card`、`--accent`、`--text`、`--border`、`--radius-input` 与 `--radius-card`。
- 移除私有紫色回退、13/18px UI 圆角、浮动工作区卡片、装饰阴影和负字距。
- 添加 CSS 设计合同与 16 个 AI Todo tools 精确目录合同。

## Verification

| Gate | Result |
|---|---|
| Todo plugin verify | typecheck pass；35 tests passed；build/package smoke pass；16 tools / 19 entrypoints |
| CSS contract | host tokens present；4/8px fallback contract；no private purple/legacy Hana token/negative tracking/oversized UI radius |
| real theme metrics | new-warm-paper resolved button radius `2px`, panel radius `3px`, button min-height `32px`, title `16px`, horizontal overflow `0` |
| real Desktop visual | 1440x900 three-pane and 600x800 responsive surfaces rendered; console errors `0` |
| real Desktop CRUD | create/edit/complete/reopen/trash/purge passed; auth failures `0`; test item purged |
| repository static | root typecheck pass; scoped ESLint `0 errors / 0 warnings` |

## Integration

实现已提交为 `b0c74282`，并获用户授权进入 `v0.0.9` release。

## 2. Lead Dispatch And Candidate Return

- **Implementation owner：** Lead direct。
- **Dispatch Packet/checkpoint：** current workspace / base `dc8ef720`。
- **允许动作：** 获授权的 implementation commit 与 direct-parent integration。
- **返回：** `b0c74282`；CSS、AI tools、响应式与 CRUD Evidence 已核对。
- **Lead 独立核对：** pass。

## 3. 修改范围与路径所有权

- **writable：** `<Path>plugins/todolist/**</Path>`。
- **read-only 修改：** core、server、desktop、SpecDev archive 与用户 pnpm 文件均未修改。
- **未声明路径：** 无。

## 4. 验收与合同映射

| Contract | 验证接缝 | 结果 |
|---|---|---|
| AC-007 | CSS token/static contract 与 Desktop visual/响应式 | pass |
| AC-008 | 16 个 AI tool 精确目录合同 | pass |

## 5. Workspace Verification

| 命令或步骤 | 运行环境 | 结果 | 摘要 |
|---|---|---|---|
| Todo plugin verify、CSS/tool contract、typecheck/lint | current-workspace | pass | 35 tests；16 tools / 19 entrypoints |
| authenticated Desktop visual/CRUD | current-workspace final composition | pass | 1440x900、600x800、0 overflow/error/auth failure；测试项已清理 |

- **E2E：** required；current-workspace passed；source-worktree not-run，因为本 change 使用 current workspace。

## 6. 双轴审查

- **标准轴：** 主题继承、布局稳定性、工具兼容与 CRUD 回归通过。
- **规范轴：** AC-007—AC-008 与 UI/AI 合同、路径边界一致。

## 7. Integration Verification

| 项目 | 结果 |
|---|---|
| Parent before SHA | `dc8ef720d4fe0bf1e0427127ff495cf2b7c5daa7` |
| Implementation/source SHA | `b0c74282ad147b5f9427dd5c1f4a9c5dbbfe5f5e` |
| Method/conflicts | direct-parent；无冲突 |
| E2E disposition/result | required / passed |
| Parent result/re-read | `b0c74282ad147b5f9427dd5c1f4a9c5dbbfe5f5e`；当前父分支包含该提交 |

## 8. 偏差与决策

- **偏差：** 无；未改变 Store v2、HTTP envelope 或 TaskRegistry 合同。

## 9. 残余风险与交付定位

- **残余风险/已知限制：** 无已知 blocker。
- **后续 Ticket：** 无。
- **Source commit / Parent result：** `b0c74282ad147b5f9427dd5c1f4a9c5dbbfe5f5e`。
- **Source workspace：** `current`。
