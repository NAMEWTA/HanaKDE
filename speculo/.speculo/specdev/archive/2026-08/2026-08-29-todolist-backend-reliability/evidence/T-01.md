# Evidence: T-01 — Todo 宿主后台与 CRUD

- **Base SHA：** `dc8ef720d4fe0bf1e0427127ff495cf2b7c5daa7`
- **Workspace/branch：** `current` / `hanakde`
- **状态：** 实现、专项验证、真实 E2E 与本地集成完成；实现提交 `b0c74282`。

## Red Evidence

- 真实 `/status`：`runtime.taskBackend=backend_unavailable`。
- 宿主错误：`No handler registered for \"task:register-handler\"`。
- 顺序测试修复前：1 failed / 7 passed，handler 索引晚于 plugin init。
- UI timeout 测试修复前：永不响应的 SDK fetch 留下常驻 spinner。

## Implemented

- TaskRegistry bus handlers 移到 `engine.initPlugins` 之前。
- Todo 页面 SDK 请求增加 10 秒默认超时、AbortSignal 和卸载取消。
- 添加 Todo 与 Project 的真实 Hono route + disk Store CRUD/重启持久化测试。
- 重新生成 Todo page asset。

## Verification

| Gate | Result |
|---|---|
| server ordering + task bus | 2 files / 11 passed |
| Todo plugin verify | typecheck pass; 33 tests passed; build/package smoke pass; 16 tools / 19 entrypoints |
| Todo real HTTP CRUD | Todo create/read/update/complete/reopen/trash/restore/restart/purge and Project create/rename/trash/restore passed |
| host focused regression | 3 files / 90 tests passed |
| root typecheck / scoped ESLint | pass / pass |
| real Desktop status | `store.writable=true`; `runtime.taskBackend=ready`; schema v2 |
| real Todo page CRUD | create/edit/complete/reopen/trash/restore/trash/purge passed; no 401/403; test item purged |
| SpecDev validator / diff check | 0 errors, 0 warnings / pass |
| repository full suite with required Node 24 | 持久化指纹已通过项目生成器重钉为 `bd250300...`；专项 32 tests passed。并发全量中的 3 个 timeout-only cases 隔离复跑为 17/17 与 21/21 passed；无功能断言失败。 |

## Integration

实现已提交为 `b0c74282`，并获用户授权进入 `v0.0.9` release。用户未跟踪的 pnpm 文件未触碰。

## 2. Lead Dispatch And Candidate Return

- **Implementation owner：** Lead direct。
- **Dispatch Packet/checkpoint：** current workspace / base `dc8ef720`。
- **允许动作：** 获授权的 implementation commit 与 direct-parent integration。
- **返回：** `b0c74282`；路径、测试、真实 Desktop E2E 与数据清理已核对。
- **Lead 独立核对：** pass。

## 3. 修改范围与路径所有权

- **writable：** `<Path>server/index.ts</Path>`、`<Path>tests/server-port-ownership.test.ts</Path>`、`<Path>plugins/todolist/**</Path>`。
- **read-only 修改：** SpecDev archive 与用户 pnpm 文件均未修改。
- **未声明路径：** 无。

## 4. 验收与合同映射

| Contract | 验证接缝 | 结果 |
|---|---|---|
| AC-001—AC-006 | ordering、真实 Hono/disk CRUD、页面 timeout、authenticated Desktop、plugin verify | pass |

## 5. Workspace Verification

| 命令或步骤 | 运行环境 | 结果 | 摘要 |
|---|---|---|---|
| focused server/Todo、plugin verify、typecheck/lint | current-workspace | pass | backend、CRUD 与 16 tools / 19 entrypoints 通过 |
| repository suite | current-workspace | pass | fingerprint 重钉后专项与隔离复跑无功能断言失败 |
| authenticated Desktop Todo CRUD | current-workspace final composition | pass | create/edit/complete/reopen/trash/restore/purge；测试项已清理 |

- **E2E：** required；current-workspace passed；source-worktree not-run，因为本 change 使用 current workspace。

## 6. 双轴审查

- **标准轴：** 启动顺序、真实 Store/route、超时取消、权限与数据清理通过。
- **规范轴：** AC-001—AC-006 与 Ticket IN/OUT、API/schema 不变量一致。

## 7. Integration Verification

| 项目 | 结果 |
|---|---|
| Parent before SHA | `dc8ef720d4fe0bf1e0427127ff495cf2b7c5daa7` |
| Implementation/source SHA | `b0c74282ad147b5f9427dd5c1f4a9c5dbbfe5f5e` |
| Method/conflicts | direct-parent；无冲突 |
| E2E disposition/result | required / passed |
| Parent result/re-read | `b0c74282ad147b5f9427dd5c1f4a9c5dbbfe5f5e`；当前父分支包含该提交 |

## 8. 偏差与决策

- **偏差：** 无；fingerprint 由项目生成器重钉，没有放宽断言。

## 9. 残余风险与交付定位

- **残余风险/已知限制：** 无已知 blocker。
- **后续 Ticket：** T-02。
- **Source commit / Parent result：** `b0c74282ad147b5f9427dd5c1f4a9c5dbbfe5f5e`。
- **Source workspace：** `current`。
