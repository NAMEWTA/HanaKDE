# Evidence: T-03 — upstream v0.450.0 分类与收敛

- **Fixed points：** fork base `b348cf1b`；upstream `1d3ef308`；implementation start `dc8ef720`
- **Workspace/branch：** `current` / `hanakde`
- **状态：** runtime 收敛、验证与本地集成完成；实现提交 `b0c74282`。

## UPSTREAM_REUSE

以下文件从 `upstream/main` byte-for-byte 采用：

- `lib/known-model-fallbacks.json`
- `lib/known-models.json`
- `lib/providers/deepseek-responses.ts`
- `tests/known-models.test.ts`
- `tests/provider-compat/deepseek-responses.test.ts`
- `tests/session-thinking-level.test.ts`

Knowledge 顶层页面复用 `DeskSection`、`PreviewPanel` 与 `WorkspaceFileChangeBridge`。

## HANA_DELTA

- HanaKDE 品牌与版本线。
- Todo builtin plugin。
- Knowledge indexing、ResourceIO、mobile 和 route contracts。
- 内部 seed/OTA 签名与 pinned keyset 校验。
- macOS/Windows 无系统发行身份策略，以及 loader 必需的 ad-hoc sealing。
- Speculo runtime 和当前 Evidence。

## EXPERIMENT_RETIRED

- Finance Workbench、Markdown WeChat 及 `plugin.page.open`。
- 两个插件的 build/persistence/test 专属接缝。

## DUPLICATE

删除不可达的 `KnowledgeIndexWorkspace` React 链、15 个组件测试、4 个旧 E2E 文件、专用 CI 分流和孤立 Knowledge tree event fan-out。保留被 shared editor 消费的 `KnowledgeSlashMenu`、被 performance adapter 消费的 `resource-tree-selection.ts`，以及 Knowledge backend。

## HISTORY_ONLY

Archived SpecDev 与 release digests 可继续记载历史插件/文件。当前契约显式登记退役测试路径，不篡改归档证据。

## Verification

| Gate | Result |
|---|---|
| full Vitest | 1204 files / 12259 passed / 8 skipped |
| desktop E2E | 4 passed / 2 capability-skipped |
| web-open E2E | 2 passed / 4 capability-skipped |
| web-full E2E | 1 passed / 5 capability-skipped |
| typecheck / ESLint | pass / 0 errors |
| renderer/client build | pass |
| server build | pass with generated temporary internal keyset; key removed on exit |

## Integration

分类收敛已提交为 `b0c74282`；版本线与 release digest 在获授权的 `v0.0.9` release 提交中更新。用户未跟踪的 pnpm 文件未触碰。

## 2. Lead Dispatch And Candidate Return

- **Implementation owner：** Lead direct。
- **Dispatch Packet/checkpoint：** fork/upstream/implementation 固定点 `b348cf1b` / `1d3ef308` / `dc8ef720`。
- **允许动作：** 获授权的 implementation commit 与 direct-parent integration。
- **返回：** `b0c74282`；分类 ledger、路径审计和验证事实已核对。
- **Lead 独立核对：** pass。

## 3. 修改范围与路径所有权

- **writable：** AC-013 ledger 判定的 upstream runtime、Knowledge 重复 UI/E2E、退役插件及关联 build/test paths。
- **read-only 修改：** SpecDev archive 与用户 pnpm 文件均未修改。
- **未声明路径：** 无。

## 4. 验收与合同映射

| Contract | 验证接缝 | 结果 |
|---|---|---|
| AC-013 | fixed-point 分类 ledger、full suite、三组合 E2E、typecheck/lint/build | pass |

## 5. Workspace Verification

| 命令或步骤 | 运行环境 | 结果 | 摘要 |
|---|---|---|---|
| full Vitest、typecheck、ESLint、renderer/server build | current-workspace | pass | 1204 files / 12259 tests；0 lint errors |
| Desktop、Web Open、Web Full E2E | current-workspace final composition | pass | 4 / 2 / 1 passed，其他为 capability skips |

- **E2E：** final current-workspace composition passed；source-worktree not-run，因为本 change 使用 current workspace。

## 6. 双轴审查

- **标准轴：** fixed-point diff、共享 owner、删除安全与构建边界通过。
- **规范轴：** AC-013 分类、保留与退役集合与 Ticket 一致。

## 7. Integration Verification

| 项目 | 结果 |
|---|---|
| Parent before SHA | `dc8ef720d4fe0bf1e0427127ff495cf2b7c5daa7` |
| Implementation/source SHA | `b0c74282ad147b5f9427dd5c1f4a9c5dbbfe5f5e` |
| Method/conflicts | direct-parent；无未决冲突 |
| E2E disposition/result | required / passed |
| Parent result/re-read | `b0c74282ad147b5f9427dd5c1f4a9c5dbbfe5f5e`；当前父分支包含该提交 |

## 8. 偏差与决策

- **偏差：** 无；Hana delta、历史 Evidence 与内部签名边界均保留。

## 9. 残余风险与交付定位

- **残余风险/已知限制：** 无已知 blocker。
- **后续 Ticket：** 无。
- **Source commit / Parent result：** `b0c74282ad147b5f9427dd5c1f4a9c5dbbfe5f5e`。
- **Source workspace：** `current`。
