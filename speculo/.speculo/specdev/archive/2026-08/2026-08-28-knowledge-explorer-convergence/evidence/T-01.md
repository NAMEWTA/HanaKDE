# Evidence: T-01 — 将 Knowledge 绑定到当前授权工作区

- **Change：** `2026-08-28-knowledge-explorer-convergence`
- **Ticket：** `<Path>{roots.state}/specdev/changes/2026-08-28-knowledge-explorer-convergence/ticket/01-bind-knowledge-to-active-workspace.md</Path>`
- **Spec：** `<Path>{roots.state}/specdev/changes/2026-08-28-knowledge-explorer-convergence/spec.md</Path>`
- **Goal Plan：** 不适用
- **Lead：** `root`
- **Workspace/branch：** `current` / `hanakde`
- **Base/implementation/source/candidate/result SHA：** `24705bb2fa3bdcfaeca444513d87e60443e5727b` / `b0c74282ad147b5f9427dd5c1f4a9c5dbbfe5f5e` / `not-applicable` / `b0c74282ad147b5f9427dd5c1f4a9c5dbbfe5f5e`
- **状态：** done；代码、required E2E 与 direct-parent integration 已验证。

## 1. 实现摘要

Renderer 从活动 Desk local/mount 状态生成 selector，并统一传播到 Knowledge、ResourceIO 与 source watch。Server 对本地目录执行 local-owner 和 approved-root 校验，对 mount 继续由 SourceRegistry/ResourceIO 校验；registry 与 operation coordinator 以真实 main root 隔离。source 失败不再制造 available main DTO。

补充修复了同一路径切换展示名称时的 registry cache identity：cache signature 同时包含 root 与 display name。`recovering` 只阻断写操作，不再阻断目录 list/read；历史恢复日志不会把 Explorer 退化为空白页。

## 2. Lead Dispatch And Candidate Return

- **Implementation owner：** Lead direct。
- **允许动作：** current workspace edits/tests；commit、push、release 未授权。
- **Lead 独立核对：** pass；逐项读取 client、watch、route、cache 与组件接缝。

## 3. 修改范围与路径所有权

| 路径 | 所有权 | 改动目的 |
|---|---|---|
| `<Path>core/knowledge-workspace/workbench-compatibility.ts</Path>` | writable | 显式 local/mount main selector |
| `<Path>server/routes/knowledge-workspace.ts</Path>` | writable | 授权解析与 root-aware cache |
| `<Path>desktop/src/react/services/knowledge-workspace-client.ts</Path>` | writable | scoped fetch |
| `<Path>desktop/src/react/services/resource-events.ts</Path>` | writable | scoped/deduped watch |
| `<Path>desktop/src/react/components/knowledge-workspace/KnowledgeWorkspace.tsx</Path>` | writable | Desk store 接入 |
| `<Path>desktop/src/react/services/resource-events.ts</Path>` | writable | scoped watch；删除退役树的孤立通知 |

- **未声明路径：** 无。
- **生成文件/锁文件：** 用户已有未跟踪 `pnpm-lock.yaml`、`pnpm-workspace.yaml` 未修改。

## 4. 验收与合同映射

| Contract | 验证接缝 | 结果 |
|---|---|---|
| AC-001—AC-002 | client + Workspace component tests | pass |
| AC-003 | source error DOM test | pass |
| AC-004 | local/mount/remote route tests | pass |
| AC-005 | registry/coordinator root identity review + switch test | pass |

## 5. Workspace Verification

| 命令或步骤 | 环境 | 结果 | 摘要 |
|---|---|---|---|
| focused client/watch/component/route Vitest | current-workspace | pass | 97 tests；最终聚合 109 tests |
| selector/recovering/i18n regression | current-workspace | pass | 4 files，53 tests |
| `npm run typecheck` | current-workspace | pass | 三个 TS 项目通过 |
| targeted ESLint | current-workspace | pass | 0 errors；TDD 临时 any 已移除 |
| real dev-web workspace | `/Users/wta/Desktop/OH-WorkSpace` | pass | sources 200、list 200、Markdown read 200；根 label 为 `OH-WorkSpace` |
| Desktop/Web Open/Web Full E2E | isolated fixtures | pass | 适用场景全部通过；shared workbench 编辑保存通过 |

- **E2E：** final current-workspace composition passed；source-worktree not-run，因为本 change 使用 current workspace。

## 6. 双轴审查

- **标准轴：** 安全边界、cache identity、watch lifecycle 与兼容调用检查通过。
- **规范轴：** AC-001—AC-005 与最终组合态 required E2E 全部通过。

## 7. Integration Verification

| 项目 | 结果 |
|---|---|
| Parent before SHA | `24705bb2fa3bdcfaeca444513d87e60443e5727b` |
| Implementation/source SHA | `b0c74282ad147b5f9427dd5c1f4a9c5dbbfe5f5e` |
| Method/conflicts | direct-parent；无冲突 |
| E2E disposition/result | required / passed |

## 8. 偏差与决策

- **偏差：** 无。临时 layout-only E2E 开关仅用于本地诊断，验证后已从工作树删除。

## 9. 残余风险与交付定位

- **残余风险：** 无已知 blocker；实现固定点已由授权提交并进入 `v0.0.9` 发布线。
- **后续 Ticket：** T-02。
- **Source workspace：** `current`。
