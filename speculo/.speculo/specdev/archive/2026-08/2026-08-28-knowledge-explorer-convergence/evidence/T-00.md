# Evidence: T-00 — 移除已拒绝的内置工作台插件

- **Change：** `2026-08-28-knowledge-explorer-convergence`
- **Ticket：** `<Path>{roots.state}/specdev/changes/2026-08-28-knowledge-explorer-convergence/ticket/00-retire-rejected-workbenches.md</Path>`
- **Lead：** `root`
- **Workspace/branch：** `current` / `hanakde`
- **状态：** 实现、验证与本地集成完成；实现提交 `b0c74282`。

## 实现摘要

删除 `plugins/finance-workbench/**` 与 `plugins/markdown-wechat/**`，清理它们的构建排除、持久化登记和唯一消费者 `plugin.page.open`。用户 `HANA_HOME` 数据、Todo、其余 builtin 和内部 artifact 签名均保留。

## 验证

| 验证 | 结果 |
|---|---|
| bundled PluginManager inventory | pass；两个 id 不存在 |
| plugin/protocol/SDK/persistence focused suites | pass；239 tests |
| `rg` live runtime audit | pass；仅保留否定断言与历史记录 |
| full Vitest | pass；1204 files，12259 tests |
| typecheck / ESLint errors | pass / 0 errors |
| client + temporary-key signed server build | pass |

## Integration

实现已提交为 `b0c74282`，并获用户授权进入 `v0.0.9` release。用户已有未跟踪 pnpm 文件未修改。

## 2. Lead Dispatch And Candidate Return

- **Implementation owner：** Lead direct。
- **Dispatch Packet/checkpoint：** current workspace / base `dc8ef720`。
- **允许动作：** 获授权的 implementation commit 与 direct-parent integration。
- **返回：** `b0c74282`；工作区路径、验证和未跟踪文件审计均已核对。
- **Lead 独立核对：** pass。

## 3. 修改范围与路径所有权

- **writable：** `<Path>plugins/finance-workbench/**</Path>`、`<Path>plugins/markdown-wechat/**</Path>` 及其唯一构建、协议和持久化消费者。
- **read-only 修改：** 无。
- **未声明路径：** 无。
- **生成文件/锁文件：** 用户 pnpm 文件未修改。

## 4. 验收与合同映射

| Contract | 验证接缝 | 结果 |
|---|---|---|
| AC-012 | bundled PluginManager inventory、runtime 引用扫描、full suite 与 build | pass |

## 5. Workspace Verification

| 命令或步骤 | 运行环境 | 结果 | 摘要 |
|---|---|---|---|
| focused inventory/protocol/persistence suites | current-workspace | pass | 239 tests |
| full Vitest、typecheck、ESLint、client/server build | current-workspace | pass | 1204 files / 12259 tests；0 lint errors |

- **E2E：** not-required；真实 bundled inventory 与 build receipt 更直接证明插件缺席。

## 6. 双轴审查

- **标准轴：** 删除范围、残余引用、用户数据与内部签名边界检查通过。
- **规范轴：** AC-012 与 Ticket IN/OUT、路径合同一致。

## 7. Integration Verification

| 项目 | 结果 |
|---|---|
| Parent before SHA | `dc8ef720d4fe0bf1e0427127ff495cf2b7c5daa7` |
| Implementation/source SHA | `b0c74282ad147b5f9427dd5c1f4a9c5dbbfe5f5e` |
| Method/conflicts | direct-parent；无冲突 |
| Parent result/re-read | `b0c74282ad147b5f9427dd5c1f4a9c5dbbfe5f5e`；当前父分支包含该提交 |

## 8. 偏差与决策

- **偏差：** 无；历史归档与用户数据未删除。

## 9. 残余风险与交付定位

- **残余风险/已知限制：** 无已知 blocker。
- **后续 Ticket：** T-01。
- **Source commit / Parent result：** `b0c74282ad147b5f9427dd5c1f4a9c5dbbfe5f5e`。
- **Source workspace：** `current`。
