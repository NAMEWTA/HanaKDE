# Evidence: T-22 - Windows blocking gate (blocked)

- **Change:** `2026-08-09-openhanako-v0-446-6-integration`
- **Ticket:** `<Path>{roots.state}/specdev/changes/{change}/ticket/22-windows-blocking-gate.md</Path>`
- **Worktree:** `specdev-worktree/T-22`
- **Baseline / candidate:** `4231ec3f4ed93167ab3c7a69b8c2160d886113ff` / `98d0600861bb8162df6edc66c3bceca3813c8db2`; candidate is integrated at `e06a5230`, current product fixed point is `2421bfed`
- **Implementer:** Worker-T-22 / Lead platform owner
- **Status:** blocked; harness candidate only, no Windows pass claimed

## 1. 实现摘要

新增 Windows-only blocking runner 和 contract tests。入口在非 `win32` 主机硬失败，不创建 fixture；真实 Windows 路径覆盖大小写别名、junction 越界、recursive watcher、rename burst、temporary-save、root identity replacement 和 PowerShell `FileShare.None` locked-file rename。可选 package inventory 检查真实 `win-unpacked` 的 app.asar、MinGit runtime、Windows sandbox helper、server/renderer seed、manifest/signature、AnyDoc/HTML child 与目标架构 secure helper，并检查 NSIS `.exe` 的 PE 头。

**Gate verdict: BLOCKED.** 当前主机是 macOS，没有真实 Windows runner、MSVC/cross compiler、`dist:win`/NSIS install-start 或 Windows Electron/Office/Knowledge E2E，不能把静态或合成 package 测试当成 blocking pass。

## 2. 修改范围

| Path | Ownership | Delivered behavior |
|---|---|---|
| `<Path>scripts/platform/windows/run-gate.mjs</Path>` | T-22 | fail-closed Windows filesystem/package/installer gate runner |
| `<Path>tests/platform/windows/windows-gate.test.ts</Path>` | T-22 | Windows-only real matrix guard plus synthetic package positive/negative contracts |
| `<Path>{roots.state}/specdev/changes/{change}/evidence/T-22.md</Path>` | SpecDev | blocked evidence and exact residuals |

No package/shared path or product source was changed.

## 3. 验收与合同映射

| Contract | Result | Evidence |
|---|---|---|
| AC-009/AC-010 | blocked | watcher/cutover rows require real Windows runner |
| AC-014/AC-016 | blocked | junction/root replacement/locked-file runtime unverified |
| AC-015/AC-017 | blocked | restore/convergence Windows direct flow unverified |
| AC-018--AC-023 | blocked | native extraction, Office Knowledge and package runtime unverified |
| AC-027 | blocked | `dist:win`, NSIS install/start and secure helper runtime unverified |

The harness supplies the intended verification seam, but no blocking contract is closed by this macOS run.

## 4. 验证执行

| Command | Result |
|---|---|
| `volta run --node 24.16.0 -- npm exec vitest run tests/platform/windows/windows-gate.test.ts --reporter=dot --maxWorkers=1` | pass: 1 file, 7 tests; 1 Windows-only real-matrix test explicitly skipped on macOS |
| `volta run --node 24.16.0 -- npm exec eslint scripts/platform/windows/run-gate.mjs tests/platform/windows/windows-gate.test.ts` | pass: 0 errors |
| `node --check scripts/platform/windows/run-gate.mjs` | pass |
| `node scripts/platform/windows/run-gate.mjs` | expected fail on macOS: `blocking gate requires win32, got darwin` |
| `git diff --check` | pass |
| `node speculo/workflows/specdev/common/tools/validate-specdev.mjs --stage implement ...` | pass: 0 errors, 0 warnings |
| Real Windows `npm ci`, MSVC helper build, `npm run dist:win`, NSIS install/start, standalone smoke, native matrix and Office/Knowledge direct E2E | **not run / blocking** |

## 5. 双轴审查

### Standards axis

- Changes stay inside T-22 writable paths and are isolated from package/shared owners.
- Runner rejects non-Windows execution and cleans temporary fixture/PowerShell state.
- Synthetic archive tests are explicitly contract tests; they do not report native runtime success.

### Specification axis

- The ticket requires a real Windows runner and disallows macOS simulation as a substitute.
- No installer, signature, native helper, restore, or E2E claim is inferred from source inspection.
- The candidate is therefore useful infrastructure but cannot transition T-22 to `done`.

## 6. 路径所有权审计

- `writable_paths` are limited to `<Path>scripts/platform/windows/**</Path>` and `<Path>tests/platform/windows/**</Path>`; all changes comply.
- No `package.json`, lockfile, desktop/native, server, lib, route, or release files were changed.
- No generated package, key, credential, or machine-absolute path is included.

## 7. 偏差与决策

- **D-T22-01 (Lead classification):** retain the fail-closed harness while marking the Ticket `blocked` until real Windows execution is available. This does not waive any AC/DoD or convert the skip to pass.
- No product deviation or compatibility fallback was introduced.

## 8. 残余风险与后续

1. On a real Windows runner, execute clean install, secure helper/MSVC build, standalone server smoke, client/server build, `dist:win`, NSIS install/start, and `run-gate.mjs --package dist/win-unpacked --installer <installer>`.
2. Record OS/build architecture, exact package SHA/inventory, fixture outcomes, cleanup, and direct Workspace/Agent/@/Office Knowledge traces.
3. Re-run T-25 at the new integration fixed point after T-22 and T-23 are both passing.

## 9. Delivery Location

- **Candidate:** `98d06008` (`fix(T-22): verify packaged Windows runtime closure`), based on the hardened harness `7ca3098b`.
- **Evidence:** `<Path>{roots.state}/specdev/changes/{change}/evidence/T-22.md</Path>`.
- **Ticket/worktree state:** `blocked` / `specdev-worktree/T-22`; candidate implementation is integrated at `e06a5230` and remains an ancestor of current product fixed point `2421bfed`, while the Gate remains blocked.

## 10. Lead Integration Acceptance

- **Standards axis:** pass for the bounded harness diff and path ownership.
- **Specification axis:** blocked because all real Windows/native/package/E2E rows remain unverified on this host.
- **Decision:** preserve candidate `98d06008` for a Windows-capable rerun; do not mark T-22 or the umbrella change complete.
