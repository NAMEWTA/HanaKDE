# Evidence: T-26 - Agent mutation producer correlation

- **Change:** `2026-08-09-openhanako-v0-446-6-integration`
- **Ticket:** `<Path>{roots.state}/specdev/changes/{change}/ticket/26-close-agent-mutation-producer-correlation.md</Path>`
- **Baseline:** `1ad0f271b47808666547dae87f15fa43237a07f4`
- **Branch / worktree:** `speculo/2026-08-09-openhanako-v0-446-6-integration/T-26`; `specdev-worktree/T-26`
- **Governance checkpoints:** `35130f34`, `e983cca2`, `76e068e3`
- **Candidate:** `4cfd37f35e0b1ab853bc0480a89f713f620a79a9`
- **Status:** done; Lead merge `feb906d6b5a2c28dee7ca52f71cbbc6e446c5d30`

## 1. Delivered behavior

Each built-in Pi `write` or `edit` invocation now freezes one UUIDv4 operation
identity and its current session identity. An invocation-local
`AsyncLocalStorage` capture records only successful `ResourceIO.write`
mutation receipts. A fact is attempted only when exactly one receipt exists.

The production workspace runtime revalidates the current healthy main root and
derives the source-relative address from `ResourceMutationResult.resource`.
Tool arguments, SessionFile paths, cwd, Agent id and raw roots never become
History authority. The Engine then writes a non-persistent `agent_tool`
ActivityHub correlation and returns the closed T-17 fact. The existing
SessionFile card carries that fact through the normal write/edit block
extractor and `/sessions/messages` reconstruction.

The desktop session factory now supplies its already-owned session path to the
existing `buildTools` callback. On renderer reload, the first authorized
websocket subscription to that session triggers one replay of existing
in-memory activity; repeated messages do not replay. A server restart clears
the activity, so the persisted fact remains fail-closed and link-free.

## 2. 修改范围

| Area | Paths | Result |
|---|---|---|
| mutation capture | `<Path>lib/resource-io/pi-tool-operations.ts</Path>`, `<Path>lib/sandbox/index.ts</Path>` | UUID/session freeze, exact-one successful receipt, sanitized optional-correlation failure |
| main authority | `<Path>core/workspace-runtime/production-workspace-runtime.ts</Path>`, `<Path>core/engine.ts</Path>` | healthy current-root revalidation, main-only address, one in-memory activity/fact; legacy path-derived event removed |
| conversation identity/replay | `<Path>core/session-coordinator.ts</Path>`, `<Path>server/routes/chat.ts</Path>` | D-T26-01 session callback and D-T26-02 first authorized subscription replay |
| serialization/consumer | `<Path>lib/activity-hub.ts</Path>`, `<Path>server/block-extractors.ts</Path>`, `<Path>desktop/src/react/stores/agent-activity-slice.ts</Path>` | non-persistent operation identity, strict write/edit file block, existing T-17 local-epoch projection |
| tests | 12 focused unit/integration files plus isolated Knowledge E2E fixture/spec | authoritative receipt, concurrency, root/mount/failure, live/replay, reload and UI chain |

Candidate `4cfd37f3` changes exactly 21 authorized paths. It does not modify
`lib/file-history/**`, History or ResourceIO routes, the T-17 shared fact
schema, History UI, package metadata, lockfiles or CI. No second watcher,
History store, restore writer, public route or durable workspace identity was
added.

## 3. 验收与合同映射

| Contract | Evidence | Result |
|---|---|---|
| AC-008 | authoritative receipt differs from requested path; real Engine write persists and replays the main fact | pass |
| AC-015/AC-016 | T-16 modal is reused; T-15 remains the only expected-version restore writer; no raw fallback | pass for T-26 producer boundary |
| AC-017 | one canonical ResourceIO event per write/edit; root replacement, unhealthy and mount projection return no fact | pass |
| AC-024 | existing activity bucket and renderer epoch are reused; first-subscription replay is bounded and memory-only | pass |
| AC-026 | fact contains only session id, UUID operation id and source-relative address; correlation errors are sanitized | pass |

## 4. 验证执行

| Command / step | Result |
|---|---|
| red producer contracts before implementation | expected red: missing capture/fact exports and producer calls |
| D-T26-01 targeted session factory test before correction | expected red: `getSessionPath` was absent; 1 failed / 87 skipped |
| D-T26-02 targeted chat test before correction | expected red: authorized first subscription made 0 replay calls |
| `volta run --node 24.16.0 -- npx vitest run` over the 12 T-26/T-17 focused files with `--maxWorkers=1` | pass: 12 files / 411 tests |
| `volta run --node 24.16.0 -- npm run typecheck` | pass: root, Node and test TypeScript projects |
| scoped ESLint with `--quiet` over all 21 candidate paths | pass: 0 errors |
| `volta run --node 24.16.0 -- npm run build:client` | pass; only existing Vite script/chunk warnings |
| `volta run --node 24.16.0 -- npx playwright test -c tests/knowledge-workspace-e2e/playwright.config.ts --project=desktop-full tests/knowledge-workspace-e2e/specs/agent-file-changes.spec.ts --workers=1 --reporter=list` | pass: 1/1 in 5.1 seconds |
| `git diff --check` and candidate `git show --check` | pass |
| SpecDev implement validator | pass: 0 errors / 0 warnings |

The E2E uses an isolated full-access test plugin backed by Pi's deterministic
faux provider. It selects the model through the production models route, sends
a prompt through the real desktop UI, executes `SessionCoordinator ->
Engine.buildTools -> sandbox write -> ResourceIO`, verifies the persisted
tool-result fact and normal `/sessions/messages` replay, reloads the renderer,
selects the same session through the normal session list, and opens the shared
main History modal. It uses no external model, network response, production
test route, websocket injection or raw file-change sideband.

### Baseline-classified composition check

The existing T-16 `file-history-workspace.spec.ts` was also run to check the
separate diff/restore/read-back owner flow. It returned 503 on its first
`write-expected-version` request before any History/restore action. The exact
same failure reproduced on clean authoritative integration `e28c0c42`, where
T-26 product code is absent. A normal authenticated session creation proved
the expected main cwd but did not change the failure. The owning T-16 review
then identified the missing T-15 native helper build prerequisite: the
standalone Playwright command had skipped `build:secure-fs-helper`. With the
real arm64 helper built, the unchanged 60 second timeline/diff/restore/read-back
flow passed 1/1 at test checkpoint `6687c150`. This closes the composition gap
without a session/model bypass and confirms it was not a T-26 regression.

## 5. 双轴审查

### Standards axis

- **Result:** pass at immutable candidate `4cfd37f3`.
- Receipt concurrency is invocation-local; optional correlation failure cannot
  convert a successful file write into a failed tool result or leak the raw
  projection error.
- The main projector revalidates before deriving a relative address. The old
  SessionFile path-derived `resource.changed` emission was removed, leaving
  ResourceIO as the only mutation event owner.
- Typecheck, focused regressions, scoped lint, build and direct E2E are green.

### Specification axis

- **Result:** pass for the T-26 producer and same-process reload contract.
- Exact session, operation and renderer-local generation correlation precedes
  main classification. Mount, invalid, missing, multiple, unhealthy and
  root-replaced cases remain link-free.
- `agent_tool` is excluded from ActivityHub persistence and existing right-side
  cards. A server restart therefore cannot revive the correlation.
- D-T17-01, D-T26-01 and D-T26-02 remain within their exact approved paths and
  do not alter T-15/T-16 ownership.

## 6. 路径所有权审计

- Candidate `4cfd37f3` changes exactly the 21 product/test paths enumerated in
  Ticket T-26 and its E2E fixture glob.
- D-T26-01 owns only `<Path>core/session-coordinator.ts</Path>` and its focused
  test; D-T26-02 owns only `<Path>server/routes/chat.ts</Path>` and its focused
  test.
- Read-only History/restore domain, public ResourceIO kernel/routes, T-17
  shared fact shape, package/lockfile and CI paths are absent from the
  candidate diff.
- The merge resolution changed only three SpecDev status projections from
  `active/in_progress` to `review`; no product conflict or foreign worktree
  edit was present.

## 7. 偏差与决策

- **D-T17-01:** authorizes the bounded producer paths and memory-only
  ActivityHub correlation required by the T-17 residual.
- **D-T26-01:** authorizes only the existing desktop session-path callback
  injection and its regression test. The real E2E proved this callback was
  required for tool-result details.
- **D-T26-02:** authorizes only one existing ActivityHub rebroadcast after the
  first authorized websocket session subscription and its regression test.
  Repeated messages do not replay.

No further deviation was used.

## 8. 残余风险与后续

- Real Windows, signed package and platform claims remain T-22/T-23/T-25.
- Server restart intentionally removes Agent History correlation; no durable
  workspace binding or backfill is promised.
- The current T-16 composition E2E passes after its required native-helper
  build prerequisite; T-16 records the exact command and `6687c150` checkpoint.
- **Candidate commit:** `4cfd37f35e0b1ab853bc0480a89f713f620a79a9`.
- **Lead action:** verify the immutable candidate, merge locally into the
  authoritative integration line, rerun final-SHA gates, then update T-17 and
  T-25 Evidence. No push, PR, release or archive is authorized.

## 9. Lead integration acceptance

- **Integration checkpoint:** `feb906d6b5a2c28dee7ca52f71cbbc6e446c5d30`,
  with parents `e28c0c42698df5b8ca07890aae11a405d2d6d962` and
  `92a4f4eb52d81b4b7b5509f0cc8c8431f78b63b5`.
- **Path/ancestry review:** candidate `4cfd37f3` and Evidence `92a4f4eb` are
  reachable from the merge; all 21 product/test paths match the Ticket and
  approved deviations. The three governance conflicts were resolved field by
  field by retaining the integration approvals and selecting the candidate's
  `review` projection.
- **Final-SHA verification:** 12 files / 411 tests passed; all three TypeScript
  projects passed; scoped ESLint reported zero errors; client build passed;
  deterministic desktop Agent write/reload/History E2E passed 1/1 in 5.2
  seconds; SpecDev validator reported 0 errors / 0 warnings; diff check passed.
- **Standards axis:** pass. ResourceIO remains the only mutation event owner,
  optional correlation remains fail-closed, and no durable or raw-path
  authority was introduced.
- **Specification axis:** pass for AC-008, AC-015--AC-017, AC-024 and AC-026.
  The direct flow uses the production Engine tool chain and the existing T-17
  consumer/T-16 modal; server restart intentionally removes correlation.
- **Decision:** accept T-26 as `done`. T-16's independent native-helper
  prerequisite has since been classified and its owner flow passes at
  `6687c150`. T-22/T-23/T-25 platform and umbrella blocks remain unchanged.
