# Evidence: T-01 - Freeze implementation baseline and authorization gates

- **Change:** `2026-08-09-openhanako-v0-446-6-integration`
- **Ticket:** `<Path>{roots.state}/specdev/changes/{change}/ticket/01-freeze-baseline-and-authorization-gates.md</Path>`
- **Spec:** `<Path>{roots.state}/specdev/changes/{change}/spec.md</Path>`
- **Tickets Map:** `<Path>{roots.state}/specdev/changes/{change}/tickets-map.md</Path>`
- **Goal Plan:** `<Path>{roots.state}/specdev/changes/{change}/goal-plan.md</Path>`
- **Baseline / branch:** `hanakde@5c281aae27eed87a0954c93b553ea7b2b255eef4` at audit; authorized integration branch created only after the audit.
- **Worktree reference:** `project-root-readonly`; post-G0 integration locator: `specdev-worktree/integration`.
- **Implementation owner:** Worker-T-01 (read-only candidate) / persistent Lead (independent audit and G0 integration).
- **Started / candidate received:** 2026-08-09T16:12:17+08:00 / native Worker-T-01 report received and independently checked.
- **Status:** done

## 1. 实现摘要

T-01 has frozen the real execution point as `5c281aae27eed87a0954c93b553ea7b2b255eef4`, not the historical planning anchor. It proves the exact upstream target, common ancestor, divergence, path overlap, and the clean-before-audit repository state. Only after that read-only audit did the Lead create the authorized isolated integration branch/worktree; no product code was changed.

### Preflight Receipt

1. Goal: freeze the actual local execution point without changing project code, index, refs, remotes, or user content.
2. Order: inspect current HEAD/status/origin/target; calculate merge base/divergence/overlap; run controlled invalid-object checks; compare before/after snapshots.
3. Maximum risk: treating the old planning anchor as the implementation baseline and thereby losing the committed out-of-band planning artifacts.
4. Actual base/origin: `5c281aae27eed87a0954c93b553ea7b2b255eef4` / `origin/hanakde@5c281aae27eed87a0954c93b553ea7b2b255eef4`.
5. Frozen target: `v0.446.6^{commit}=5f08a4f30203abb61dafac7dbb7ab92d11c23efa`; merge base: `ef8a6f700191c2486effd3761a4bd2b7f3ad774c`.
6. Out-of-band Goal Plan: `<Path>{roots.state}/specdev/changes/{change}/goal-plan.md</Path>`; `schema_version: 3`, `artifact: goal-plan`, matching change, `status: ready`, `ready_for_execution: true`.
7. External content digest: SHA-256 `09d732d7ae847f42024d8e8f29fa4a07a7a018fe5469e4771751efff790d3dc5`; local Git source package hash remains `n/a-local-git`.
8. The real baseline differs from planning anchor `5f819b1233d6acdc0893363d4647bf1d53af8355` by one committed planning/state-only change; product-code diff outside `speculo/.speculo/specdev/**` is empty.

## 2. 修改范围

| 路径 | 所有权 | 改动目的 |
|---|---|---|
| `<Path>**</Path>` | read-only | G0 audit only; no project path changed. |
| `<Path>{roots.state}/specdev/changes/{change}/evidence/T-01.md</Path>` | workflow-owned | Persist the fixed-point and authorization evidence on the isolated integration line. |
| Ticket/Map/Goal Plan/change/global status projections | workflow-owned | Synchronize the accepted G0 result without touching product code. |

### Fixed-Point Audit

| Fact | Result | Evidence |
|---|---|---|
| Actual HEAD and branch | `hanakde@5c281aae27eed87a0954c93b553ea7b2b255eef4`, tree `f3b18a3af89350b00c4e43532ec225c597f936d2` | `git rev-parse HEAD`, `git branch --show-current`, `git rev-parse HEAD^{tree}` |
| Origin protection | `origin/hanakde` resolves to the same full SHA; initial status and index were clean | `git rev-parse origin/hanakde`, `git status --porcelain=v1`, worktree/index diff checks |
| Frozen target identity | `v0.446.6^{commit}` and `upstream/main` resolve to `5f08a4f30203abb61dafac7dbb7ab92d11c23efa` | `git rev-parse refs/tags/v0.446.6^{commit}` and ref inventory |
| Common ancestor | `ef8a6f700191c2486effd3761a4bd2b7f3ad774c` | `git merge-base HEAD v0.446.6^{commit}` |
| Divergence and patch comparison | `203` HanaKDE-only / `286` upstream-only commits; `git cherry -v target HEAD` reports `201 +`, no equivalent `-` entries | `git rev-list --left-right --count`, `git cherry -v` |
| Path surface | HanaKDE side: `888` paths; upstream side: `671` paths; semantic overlap: `50` paths | merge-base range path lists and `comm -12` |
| Release checkpoints | `v0.421.24`, `v0.433.1`, `v0.441.3`, `v0.441.32`, `v0.442.0`, `v0.443.46`, `v0.443.54`, `v0.444.1`, `v0.446.6` all resolve locally to their frozen commits | exact ref inventory recorded during audit |
| Planning-anchor reconciliation | `5f819b..5c281` changes only Speculo planning/state artifacts; no product code is newly introduced by that delta | `git diff --name-only` and excluded-path `git diff --exit-code` |

The 50 overlapping paths include the expected high-risk Resource/Knowledge/Workbench, root security, runtime, package, and generated-receipt surfaces. They remain inputs to staged semantic resolution, not a reason to bypass the release sequence.

## 3. 验收与合同映射

| Contract / Acceptance ID | 验证接缝 | 证据 | 结果 |
|---|---|---|---|
| AC-001 | actual fixed point, target ancestry input, merge base, patch comparison, path overlap, checkpoint list | exact Git commands and values in the Fixed-Point Audit and Verification Execution sections | pass |
| T-01 audit preservation | before/after HEAD, status, refs and controlled invalid-object checks | snapshot comparisons and exit-128 negative checks | pass |
| T-01 authorization boundary | explicit action matrix and isolated integration branch | Authorization and Isolation table; no unauthorized remote/production action | pass |
| T-01 Ticket/Map/Evidence synchronization | frontmatter/status/map reconciliation | this Evidence plus status projections on the integration line | pass |

### Authorization and Isolation

| Action | Verdict | Boundary |
|---|---|---|
| G0 audit | executed read-only | no project/index/ref/remote/user-content mutation during audit |
| Local branch/worktree | authorized after G0 audit | `speculo/2026-08-09-openhanako-v0-446-6-integration/integration` at `specdev-worktree/integration` from actual fixed point |
| Candidate/integration commits and Lead merges | authorized | only in the isolated integration/Ticket worktrees |
| Non-force cleanup | authorized when an integrated Ticket worktree is clean and registered | preserve any mismatch or uncommitted content |
| Push, PR, deploy, release, archive, remote write, sign/notarize, migration, production config/feature, real user data | not authorized | not executed |

## 4. 验证执行

| Command or step | Environment | Result | Summary |
|---|---|---|---|
| fixed-point identity, target, merge-base, divergence, path lists | local Git object database | pass | exact SHAs and counts recorded above |
| `git diff --exit-code 5f819b..5c281 -- . ':(exclude)speculo/.speculo/specdev/**'` | local Git object database | pass | current execution point adds no non-Speculo change after planning anchor |
| before/after `HEAD`, status, refs snapshots | local Git object database | pass | all three comparisons identical before the authorized post-audit integration branch creation |
| invalid target lookup `v0.446.6-not-a-real-tag^{commit}` | controlled negative check | pass | exits `128`; no ref creation or fallback target selection |
| invalid all-zero object lookup | controlled negative check | pass | exits `128`; no object fallback or repository mutation |
| post-G0 integration worktree registry | local Git worktree | pass | exact branch and `5c281aae` registry entry exists; main `hanakde` remains at its original SHA |

- **Failure repair and rerun:** none.
- **Not-run checks:** product test/build commands are not applicable to this read-only G0 Ticket.
- **E2E:** not applicable.
- **Negative verification:** both missing-target and missing-object checks failed closed, then the valid frozen target was re-resolved successfully.
- **External claims:** Worker-T-01 independently confirmed the actual `5c281aae` fixed point, matching origin and exact target object. Its sampling report deliberately did not complete merge-base/divergence/overlap or snapshot checks; the Lead's recorded command results are the sole evidence for those fields.

## 5. 双轴审查

### 标准轴

- **Result:** pass.
- **Sources:** repository `AGENTS.md`, Speculo execution preflight, Git worktree contract, and Fowler baseline as applicable.
- **Findings:** none. The audit was performed before the authorized branch creation; the branch is intentionally excluded from the before/after ref-preservation assertion.
- **Correction and rerun:** not applicable.

### 规范轴

- **Result:** pass.
- **Sources:** AC-001, ADR-001, T-01, Goal Plan G0/Dispatch, and user-provided current-HEAD authority.
- **Findings:** the ticket's historical planning snapshot is stale. The governing Goal Plan and explicit authorization require actual `5c281aae` to be recorded instead of silently using `5f819b`; product scope remains unchanged.
- **Correction and rerun:** no contract change required; this is an executed fixed-point reconciliation recorded before downstream work.

## 6. 路径所有权审计

- **Writable project paths:** none.
- **Shared modifications:** none.
- **Read-only project paths:** all project paths; no project code was changed during the audit.
- **Workflow-owned projections:** this Evidence, Ticket/Map/Goal Plan status, and change/global state are written only on the isolated integration line.
- **Generated files / lockfiles:** none.

## 7. 偏差与决策

- **Deviation:** none requiring Spec, ADR, or Ticket change. After the initial clean snapshot, Worker-T-01 observed uncommitted planning artifacts owned by the separate `2026-08-09-internalize-todolist-plugin` change in the authority worktree. They are neither touched nor used as an implementation input; the isolated integration worktree remains based on the audited clean `5c281aae` tree.
- **Decision source:** explicit execution authority supplied with this implementation task plus Goal Plan section 4. The stale planning anchor is treated as an expected out-of-band planning delta, not an implementation input.
- **Impact:** T-02 must use the post-G0 integration SHA rather than the old `G0_INTEGRATION_SHA` placeholder; all later Gate SHAs will be written as they are published.

## 8. 残余风险与后续

- **Residual risk:** 50 semantic-overlap paths require five-class review during W1 merges; no product behavior has been inferred from a raw conflict count.
- **Known limitation:** platform/product verification begins with the relevant implementation Tickets, not G0.
- **Next Ticket:** T-02, strictly from the committed post-G0 integration checkpoint.
- **Recovery trigger:** target/object/branch registry mismatch, unclassified conflict, or any unapproved Git side effect blocks the affected Ticket and retains its worktree.

## 9. Delegated Evidence Addendum

- **Execution model / Provider:** native-subagent / native Worker-T-01.
- **Session / Package locator:** `project-root-readonly`; source package `n/a-local-git`.
- **Dispatch / Final checkpoint:** `5c281aae27eed87a0954c93b553ea7b2b255eef4` / pending Lead G0 close commit.
- **Correction rounds:** 0 of 3.
- **Candidate delivery verification:** Worker-T-01 report received. It independently verifies base/origin/target identity and correctly leaves the full fixed-point calculation to the Lead. Lead verified scope, target/object identity, audit snapshots, no project-code diff after the planning anchor, and that concurrent other-change artifacts are excluded.
- **Lead E2E:** not applicable.
- **Unverified claims:** none.

## 10. Delivery Location

- **Commit / PR:** Lead G0 state/Evidence commit follows this accepted review; no PR.
- **Final workspace reference:** `specdev-worktree/integration`.
- **Evidence file:** `<Path>{roots.state}/specdev/changes/{change}/evidence/T-01.md</Path>`.
