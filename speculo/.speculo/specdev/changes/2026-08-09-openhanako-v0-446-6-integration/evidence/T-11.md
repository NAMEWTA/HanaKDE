# Evidence: T-11 - Establish main Workspace infrastructure

- **Change:** `2026-08-09-openhanako-v0-446-6-integration`
- **Ticket:** `<Path>{roots.state}/specdev/changes/{change}/ticket/11-establish-main-workspace-infrastructure.md</Path>`
- **Spec:** `<Path>{roots.state}/specdev/changes/{change}/spec.md</Path>`
- **Tickets Map:** `<Path>{roots.state}/specdev/changes/{change}/tickets-map.md</Path>`
- **Goal Plan:** `<Path>{roots.state}/specdev/changes/{change}/goal-plan.md</Path>`
- **Baseline / branch:** `2018ce1dc671f0d9bb3c6f61f4078625c7863001` / `speculo/2026-08-09-openhanako-v0-446-6-integration/T-11`
- **Worktree reference:** `specdev-worktree/T-11`
- **Implementer:** Worker-T-11
- **Start / end:** `2026-08-10T05:05:37+0800` / `2026-08-10T05:22:26+0800`
- **Status:** review

## Preflight Receipt

- Exact base/HEAD: `2018ce1dc671f0d9bb3c6f61f4078625c7863001`; branch: `speculo/2026-08-09-openhanako-v0-446-6-integration/T-11`.
- G2 is closed: T-10 is `done` in the Tickets Map and its integrated Resource Kernel is present in this fixed base.
- T-11 depends only on T-10 and owns no shared W3 path with T-18 or T-19.
- Worktree registration uses `specdev-worktree/T-11` and the fixed product base above.
- Writable scope is limited to workspace runtime/shared contracts and `tests/workspace-*.test.ts`.
- Placement ruling: HanaKDE system core; it defines a privileged shared observation contract and startup-lifetime infrastructure.
- Root authority must fail closed; the highest risks are root replacement and duplicate watcher/baseline ownership.
- This Ticket proves the isolated coordinator only; T-12 alone may cut over production ownership.
- Node 24.16.0 is available; focused Vitest and full typecheck are the planned verification.
- Verdict: preflight passed; correction limit is three returned Lead rounds.

## 1. 实现摘要

The isolated `WorkspaceWatchCoordinator` now owns one logical `main` lifecycle. It proves a local main `ResourceRef` through the T-10 ResourceIO root/target seam before closing an old lifecycle, shares one physical watcher and one baseline per repair cycle across logical consumers, and exposes only normalized relative observations plus the four allowed health states.

Mounted ResourceRefs are rejected before a lifecycle change. Root replacement or unavailable proof clears cached facts, closes the watcher, and reaches `FAILED`; scoped retry starts a new repair cycle. A safe shared cutover descriptor describes T-12's required stop/prove/start order without assigning production ownership or modifying `desktop/main.cjs`.

## 2. 修改范围

| Path | Ownership | Purpose |
|---|---|---|
| `<Path>core/workspace-runtime/main-workspace-runtime.ts</Path>` | writable | main lifecycle, logical subscription, watcher, baseline, health, and fail-closed coordination module. |
| `<Path>core/workspace-runtime/resource-main-root-authority.ts</Path>` | writable | T-10 ResourceIO identity/watch-target proof adapter. |
| `<Path>core/workspace-runtime/index.ts</Path>` | writable | internal workspace-runtime export surface. |
| `<Path>shared/workspace-observation.ts</Path>` | writable | safe observation DTOs, health vocabulary, and T-12 cutover descriptor. |
| `<Path>tests/workspace-runtime.test.ts</Path>` | writable | lifecycle, single-owner, health, root authority, safe projection, and 50k baseline regressions. |
| `<Path>{roots.state}/specdev/changes/{change}/ticket/11-establish-main-workspace-infrastructure.md</Path>` | Ticket state | candidate moved to review after local verification. |
| `<Path>{roots.state}/specdev/changes/{change}/tickets-map.md</Path>`, `<Path>{roots.state}/specdev/changes/{change}/.status.json</Path>` | state projection | T-11 review/worktree projection only. |
| `<Path>{roots.state}/specdev/changes/{change}/evidence/T-11.md</Path>` | Evidence | this worker handoff. |

`desktop/main.cjs` and `desktop/workspace-watch-registry.cjs` are intentionally unchanged: this Ticket does not perform T-12 production ownership cutover.

## 3. 验收与合同映射

| Contract / Acceptance ID | Verification seam | Evidence | Result |
|---|---|---|---|
| AC-004 | `switchMain` lifecycle | focused test closes the existing watcher before creating a fresh `main` session; no subscriptions migrate. | pass: isolated proof |
| AC-005 | main/mount scope | mount ResourceRef is rejected before `proveMain` or old-main closure. | pass: isolated proof |
| AC-009 / AC-012 | coordinator factory, scan counters | two consumers share one watcher and one baseline; 50k synthetic baseline scans once; repair performs exactly one new scan. | pass: isolated proof |
| AC-013 | health state machine | tests cover `RECONCILING`, `HEALTHY`, `DEGRADED`, `FAILED`, baseline failure closure, and scoped retry recovery. | pass: isolated proof |
| AC-014 / AC-026 | ResourceIO proof and safe DTO seam | mount/unproved/replaced/unavailable root cases fail closed; snapshots and observations contain no absolute root; no public `workspaceId` is introduced. | pass: isolated proof |
| AC-025 | initial/repair failure handling | baseline failure reaches `FAILED`, releases its watcher, and retry returns to `HEALTHY` without changing ordinary production workspace wiring. | pass: isolated proof |

## 4. 验证执行

| Command or step | Environment | Result | Summary |
|---|---|---|---|
| red: `volta run --node 24.16.0 npm test -- --maxWorkers=1 tests/workspace-runtime.test.ts` | Node 24.16.0 | expected fail | initial contract import was absent before the runtime module existed. |
| TDD safety/replay reds and greens | Node 24.16.0 | pass | root replacement closes its watcher; late consumers replay normalized baseline facts without a second walk; failed baselines close their watcher; unavailable root clears cached facts. |
| `volta run --node 24.16.0 npm test -- --maxWorkers=1 tests/workspace-runtime.test.ts tests/workspace-watch-registry.test.ts tests/resource-root-identity.test.ts tests/provider-root-identity.test.ts` | Node 24.16.0 | pass | 4 files, 20 tests; includes the 50k synthetic-tree case. |
| `volta run --node 24.16.0 npm run typecheck` | Node 24.16.0 | pass | root, node, and test TypeScript projects pass. |
| static production-owner scan | Node 24.16.0 | pass | no `createMainWorkspaceRuntime` or `WorkspaceWatchCoordinator` reference exists in `<Path>desktop/main.cjs</Path>`. |
| `git diff --check` | Git | pass | no whitespace errors. |
| `node <Path>{roots.workflows}/specdev/common/tools/validate-specdev.mjs</Path> --stage implement <Path>{roots.state}/specdev/changes/{change}</Path>` | Node 24.16.0 | not-run: pre-existing blocker | validator stops on the base T-10 Evidence missing current-template headings; T-11 headings are checked directly below without modifying T-10. |

- **Failure fixes and reruns:** all local red/green slices above were repaired and rerun before the implementation checkpoint. No returned Lead correction round has occurred; the delegated limit remains three.
- **Unrun checks:** Windows/macOS native watcher/package validation is outside T-11 and remains blocking evidence for T-22/T-23. Full repository E2E is not applicable to this isolated proof.
- **E2E:** pending Lead validation after integration. Scenario: switch a selected main root while a legacy owner is stopped, prove zero old physical watchers, start this coordinator, then verify one watcher/baseline and health recovery. Expected: overlap remains zero; raw root never reaches renderer output.
- **Reverse validation:** injected root replacement, root-unavailable proof, baseline failure, and gap/repair cases reached `FAILED`/repair rather than continuing to publish cached or unproved facts.
- **External declarations:** none; all provider/worker results are local and independently runnable from this worktree. Lead integration/E2E remains unverified.

## 5. 双轴审查

### 标准轴

- **Result:** pass
- **Sources:** `<Path>AGENTS.md</Path>`, `<Path>CONTRIBUTING.md</Path>`, repository TypeScript/Vitest configuration, and Fowler smell baseline.
- **Findings:** no unresolved finding. The coordinator is the deep module; ResourceIO proof and watcher implementations remain injected adapters at a narrow seam. Callback failures are isolated and public observation data is normalized.
- **Fixes and reruns:** review added the unavailable-root cache-clear regression and case-mode identity comparison; focused tests and typecheck were rerun.

### 规范轴

- **Result:** pass
- **Sources:** `<Path>{roots.state}/specdev/changes/{change}/spec.md</Path>`, `<Path>{roots.state}/specdev/changes/{change}/ticket/11-establish-main-workspace-infrastructure.md</Path>`, `<Path>{roots.state}/specdev/changes/{change}/ADR.md</Path>`, and `<Path>{roots.state}/specdev/changes/{change}/goal-plan.md</Path>`.
- **Findings:** no unresolved finding. The implementation keeps `main` singular, rejects mount promotion, establishes one observer/baseline contract, uses only four health states, fails closed on root proof failure, and leaves production cutover to T-12.
- **Fixes and reruns:** no contract deviation; the cutover descriptor deliberately has `productionOwner: null`.

## 6. 路径所有权审计

- **Writable changes:** only T-11 workspace-runtime/shared contract/test paths and the authorized T-11 state projections listed above.
- **Shared changes and owner approval:** none.
- **Read-only changes:** none.
- **Undeclared paths:** none.
- **Generated files or lockfiles:** none. `npm ci --ignore-scripts` restored the locked local dependency tree only; it did not modify tracked manifests or lockfiles.

## 7. 偏差与决策

- **Deviation:** none.
- **Decision source:** Ticket T-11, ADR-002/007/010/011, T-10 Evidence, and the supplied system-core placement decision.
- **Impact on follow-up Tickets:** T-12 consumes `MAIN_WORKSPACE_CUTOVER_DESCRIPTOR` and owns actual stop-then-start production wiring; T-13/T-14 consume the stable observation/health contract after W3 integration.

## 8. 残余风险与后续

- **Residual risk:** no Electron production owner has been wired, so true native watcher behavior, descriptor counts, and stop-before-start overlap still require Lead/T-12 and platform evidence.
- **Known limitation:** the proof adapter only authorizes local main roots; mounts stay intentionally outside the Workspace lifecycle.
- **Follow-up Tickets:** T-12, T-13, and T-14.
- **Monitoring / rollback trigger:** T-12 must stop the old owner and prove zero legacy watchers/baselines before startup; any nonzero overlap, unproved root, or failed baseline remains `FAILED` and must not proceed as healthy.

## 9. 交付定位

- **Implementation commit:** `294b6384093a22399e7e8177d385e7d1dbaf3b89`
- **Final workspace reference:** `specdev-worktree/T-11`
- **Evidence file:** `<Path>{roots.state}/specdev/changes/{change}/evidence/T-11.md</Path>`

## Delegated Evidence Addendum

- **Execution model / Provider:** `native-subagent` / native worker
- **Session / package locator:** `specdev-worktree/T-11` / `n/a-local-git`
- **Dispatch / final implementation checkpoint:** `2018ce1dc671f0d9bb3c6f61f4078625c7863001` / `294b6384093a22399e7e8177d385e7d1dbaf3b89`
- **Correction rounds:** `0 / 3` returned by Lead; local TDD slices are recorded above.
- **Candidate delivery verification:** implementation commit, allowed-path audit, 20 focused tests, typecheck, static no-cutover scan, and diff check have local evidence; Lead must independently validate the candidate and integration base.
- **Lead E2E:** pending: stop old owner -> prove release -> start coordinator -> main switch/health recovery; expected overlap is zero.
- **Unverified claims:** no production cutover, real Electron E2E, Windows native evidence, or macOS native evidence is claimed by this worker.
