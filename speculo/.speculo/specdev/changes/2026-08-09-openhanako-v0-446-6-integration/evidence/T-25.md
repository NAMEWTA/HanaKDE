# Evidence: T-25 - umbrella final acceptance (blocked)

- **Change:** `2026-08-09-openhanako-v0-446-6-integration`
- **Ticket:** `<Path>{roots.state}/specdev/changes/{change}/ticket/25-final-umbrella-acceptance.md</Path>`
- **Product fixed point:** `2421bfed` (last product commit)
- **Audit checkpoint:** `aa3d80e2`; this Evidence refresh is a docs-only descendant
- **Owner:** Worker-T-25 / Root Lead
- **Status:** blocked; change remains active

## 1. 实现摘要

**Final verdict: BLOCKED.** The current local product fixed point closes the
T-15 Node strip-only regression, T-20 incremental Office projection, and T-16
Workbench History host/E2E. It does not close T-22 or T-23: no real Windows
runner has executed the blocking matrix, and macOS still lacks x64 runtime/DMG,
descriptor accounting, physical sleep/event-loss reconciliation, and a package
rebuilt from the final product SHA with packaged Office/History direct flows.

T-17 also remains fail-closed when production activity/chat payloads omit the
required session, UUID operation and source-relative resource envelope. Missing
platform or producer Evidence is not converted into a pass; the 28 ACs and 15
DoD items therefore cannot be accepted as an umbrella release.

## 2. 修改范围

T-25 itself changes only SpecDev Evidence/status projections. Product commits
since the previous audit are owned corrections in T-15, T-20 and T-16. The
audit does not authorize product fixes, a push/PR, signing, notarization,
publishing, migration, archive, or any write to external infrastructure.

`git diff dce4676b..2421bfed` is intentionally not docs-only: it contains the
T-15 strip-only correction, T-20 store correction, and T-16 host/UI/E2E. All
commands below were rerun after those product commits; retained package outputs
built before `2421bfed` are historical diagnostics only.

## 3. 验收与合同映射

### 3.1 Fixed point and dependencies

The frozen target `5f08a4f3`, T-21 `e1b232b3`, T-22 harness merge `e06a5230`,
T-23 adapter/harness `dce4676b`, T-24 docs `10a79be8`, T-15 correction merge
`9738a105`, T-20 correction merge `d482768f`, and T-16 final product
`2421bfed` are ancestors of the audit checkpoint. T-22 remains `blocked`, T-23
remains `review`, and T-25 remains `blocked`.

### 3.2 AC-001..AC-028 verdict

`covered` means current focused/local Evidence exists. It does not override a
blocking platform or final-package row.

| AC | Verdict | Basis |
|---|---|---|
| AC-001 | covered | frozen target and integration ancestry verified |
| AC-002 | covered | upstream integration and T-21 build-input Evidence |
| AC-003 | covered | current T-20 122-test and Office E2E results |
| AC-004 | covered | main lifecycle/watcher focused Evidence |
| AC-005 | covered | main-only History service and boundary tests |
| AC-006 | covered | History timeline/deleted/diff plus owner E2E |
| AC-007 | covered | deterministic policy/store tests |
| AC-008 | blocked | T-17 projector is strict, but production producer envelope is absent |
| AC-009 | blocked | no real Windows watcher Gate |
| AC-010 | blocked | no real Windows cutover Gate |
| AC-011 | covered | plugin/EventBus adapter and 140-test matrix |
| AC-012 | blocked | physical sleep/event-loss and Windows matrix absent |
| AC-013 | blocked | final platform health/reconcile matrix incomplete |
| AC-014 | blocked | macOS security fixture passes; Windows junction/root runtime absent |
| AC-015 | blocked | local restore passes; final cross-platform/package Gate incomplete |
| AC-016 | blocked | secure TOCTOU tests pass; Windows native runtime absent |
| AC-017 | covered | Office and History modify/restore convergence passes locally |
| AC-018 | blocked | extraction tests pass; current Windows/macOS package matrix incomplete |
| AC-019 | blocked | extraction failure tests pass; production platform package incomplete |
| AC-020 | blocked | materialize/root tests pass; Windows native Gate absent |
| AC-021 | covered | Office extraction/index/search/restore E2E passes in dev runtimes |
| AC-022 | blocked | no-write/no-OCR tests pass; final package/platform proof incomplete |
| AC-023 | blocked | transfer/materialize tests exist; final platform proof incomplete |
| AC-024 | blocked | Workbench E2E passes; Agent producer-envelope flow remains skipped |
| AC-025 | covered | new-store initialization Evidence |
| AC-026 | covered | route/schema/security scans and focused tests |
| AC-027 | blocked | historical arm64 package only; current macOS x64 and Windows NSIS absent |
| AC-028 | blocked | architecture docs pass; platform/producer closure is incomplete |

### 3.3 Umbrella DoD

| DoD | Verdict | Reason |
|---|---|---|
| 1 | pass | frozen target is an ancestor of `2421bfed` |
| 2 | covered | current focused regression Evidence exists |
| 3 | blocked | Workspace History passes; Agent production envelope does not |
| 4 | covered | extraction fixtures and stable-failure tests pass |
| 5 | blocked | dev Office/History flows pass; current packaged direct flow absent |
| 6 | covered | Materialize/Transfer contracts pass focused tests |
| 7 | blocked | Windows native root/junction verification absent |
| 8 | blocked | macOS arm64 fixture only; Windows and physical sleep proof absent |
| 9 | covered | EventBus single-owner/adapter contract passes |
| 10 | covered | baseline reconciliation focused tests exist |
| 11 | blocked | final cross-platform convergence traces incomplete |
| 12 | blocked | T-22/T-23 blocking Gates are not both passed |
| 13 | blocked | final macOS package and Windows NSIS package unverified |
| 14 | blocked | structural checks pass; platform-dependent closure does not |
| 15 | pass | architecture and upstream ledger are integrated |

## 4. 验证执行

Executed on required Node `24.16.0` at product fixed point `2421bfed` or its
docs-only descendant:

| Command | Result |
|---|---|
| SpecDev implement validator | pass: 0 errors, 0 warnings |
| `git merge-base --is-ancestor <target/checkpoints> HEAD` | pass for frozen target and listed T-15/T-16/T-20/T-21/T-22/T-23/T-24 checkpoints |
| T-15 secure writer/helper/provider/runtime focused matrix | pass: 4 files, 44 tests |
| T-20 Office/Knowledge/extraction matrix | pass: 9 files, 122 tests |
| T-16 History UI/service/store/boundary matrix | pass: 8 files, 91 tests |
| Office E2E, `desktop-full` + `web-open` | pass: 2/2; modify, automatic reindex, restore/search convergence, no derived file |
| History E2E, `desktop-full` | pass: 1/1 in 1.1m; visible Workbench entry, real 60s merge window, diff, expected-version restore, ResourceIO read-back |
| macOS/Windows/package contract Vitest matrix | pass: 27; skip: 1 real Windows-only row |
| plugin context/route/EventBus/manager/SDK matrix | pass: 5 files, 140 tests |
| `volta run --node 24.16.0 -- npm run typecheck` | pass: all three TypeScript projects |
| scoped T-16/T-20 ESLint and `git diff --check` | pass |
| `volta run --node 24.16.0 -- npm run build:client` | pass; current renderer bundle built for E2E |

Historical platform evidence retained without promotion:

- arm64 `npm run dist`, app inventory, DMG verification and isolated todo
  launch passed on an earlier product checkpoint with local-only ad-hoc signing;
  the package was not rebuilt after final T-20/T-16 commits.
- x86_64 helper cross-compiles, but this arm64 host has no Rosetta and cannot
  execute it or an x64 DMG.
- Windows harness tests pass statically on macOS while the real matrix is an
  explicit skip. No MSVC/native runtime, `dist:win`, NSIS install/start, or
  Windows Office/History flow has run.
- Read-only external audit on 2026-08-11 found no remotely reachable current
  integration ref, no self-hosted Actions runner and no repository signing
  secrets; hosted workflow definitions alone are not execution Evidence.

## 5. 双轴审查

- **Standards axis:** pass for the local audit. Current product corrections are
  owner-scoped, typecheck/lint/tests are green, and missing platform evidence is
  classified fail-closed.
- **Specification axis:** blocked. T-22 and T-23 do not satisfy their blocking
  platform rows, and T-17 lacks the production producer envelope.

## 6. 路径所有权审计

T-25 writes only its Evidence/status management files. T-15/T-20/T-16 product
changes are recorded under their approved tickets/deviations. No package,
lockfile, release config, credential, remote ref or user namespace is changed
by this audit. Untracked/generated build helpers are not counted as Evidence.

## 7. 偏差与决策

- **D-T25-01:** retain `blocked` while T-22 is blocked and T-23 is review. This
  status decision waives no AC, DoD, platform row or producer contract.
- No compatibility fallback, raw-path projection, platform substitute,
  unsigned-release exception, push or release action is authorized.

## 8. 残余风险与后续

1. Run T-22 on real Windows: filesystem matrix, MSVC helper, watcher/restore,
   extraction, `dist:win`, NSIS install/start, and Office/History E2E.
2. Run T-23 on a fresh final product SHA: arm64/x64 package/runtime, descriptor
   accounting, physical sleep/wake with actual loss/reconcile, and packaged
   Office/History direct flows without local signing substitutions.
3. Supply the strict T-17 producer envelope and unskip its Agent E2E without a
   raw-path fallback.
4. Re-run T-25 at the resulting final SHA. Do not reuse this blocked verdict as
   completion evidence.

## 9. Delivery Location

- **Product fixed point:** `2421bfed`.
- **Evidence:** `<Path>{roots.state}/specdev/changes/{change}/evidence/T-25.md</Path>`.
- **Ticket status:** `blocked`; umbrella change remains `active`.

## 10. Lead Integration Acceptance

- **Standards axis:** pass for audit scope.
- **Specification axis:** blocked by T-22/T-23 and the T-17 producer residual.
- **Decision:** preserve the integrated product through `2421bfed`; do not mark
  the umbrella complete, archive it, or claim release readiness.
