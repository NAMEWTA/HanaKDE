# Evidence: T-10 - Resource Kernel convergence
## Preflight Receipt
- Exact base/HEAD: `589a3c0810a472ed4ab12e93e6c1d7195da5017c`; branch: `speculo/2026-08-09-openhanako-v0-446-6-integration/T-10`.
- G1 dependency is closed: T-09 Lead checkpoint `c45d1e544f8f2611f92a459947b6a49e9b91239d`; T-09 is `done`.
- T-10 is `ready`, blocked only by T-09, with AC-011/014/020/023/026 and no shared path conflict.
- Worktree registration is recorded below as `active` with the dispatch base and `specdev-worktree/T-10` locator.
- Writable scope is limited to Resource Kernel, route/context, and `tests/resource-*.test.ts`; consumers remain read-only.
- Feature placement: HanaKDE system core (`lib/` + `server/`), because it defines privileged shared authority/event primitives.
- Authority invariants: fail closed before effects; no raw root/public workspaceId/path/token/content exposure.
- Highest risks: root TOCTOU/replacement and materialize staging cleanup; copy/transfer budgets stay distinct.
- Environment starts without `node_modules`; clean local dependency installation is required before verification.
- Verdict: preflight passed after this local worktree-state registration; D-T10-01 was subsequently approved by Lead for the one generated persistence inventory receipt.

## 1. Implementation Summary

Resource Kernel convergence is implemented at `cfc0c7877e672a26f48a5e7f5d6a0f04c99e8776`: retained event history now reports stale cursors, committed events isolate fan-out failures, local root relations require private physical proof and matching scope tokens, URL materialization owns a bounded cleanup lease, aborted transfers stop before target staging, and route authority rejects forged `workspaceId` input.

Correction round: a same-physical-root scope-token change now returns `unknown` rather than the source-registry-allowed `disjoint`; subscriber and emitter returned thenables now have rejection handlers without changing synchronous fan-out order.

## 2. Modification Scope

| Path | Ownership | Purpose |
|---|---|---|
| `<Path>lib/resource-io/resource-event-bus.ts</Path>` | writable | stale catch-up and sync/async fan-out isolation. |
| `<Path>lib/resource-io/root-identity.ts</Path>` | writable | physical proof and fail-closed scope-token relation. |
| `<Path>lib/resource-io/resource-io.ts</Path>`, `<Path>lib/resource-io/providers/url-provider.ts</Path>`, `<Path>lib/resource-io/types.ts</Path>` | writable | materialize leases and authority contract. |
| `<Path>server/routes/resource-io.ts</Path>` | writable | reject forged client authority. |
| `<Path>tests/resource-*.test.ts</Path>` | writable | contract and correction regression coverage. |
| `<Path>build/persistence-store-inventory.json</Path>` | D-T10-01 generated receipt | scanner records the two new URL staging cleanup sites. |

## 3. Acceptance Mapping

| Contract | Evidence | Result |
|---|---|---|
| AC-011 | `tests/resource-event-bus.test.ts`; `tests/resource-events-ws.test.ts` prove ordering, stale catch-up, sync/async subscriber and emitter isolation. | pass |
| AC-014 | `tests/resource-root-identity.test.ts`, `tests/provider-root-identity.test.ts`, `tests/knowledge-source-registry.test.ts` prove private proof, replacement disjointness, and permission-mode scope drift fail closed. | pass |
| AC-020 | `tests/resource-io-materialize.test.ts`, `tests/resource-io-materialize-tool.test.ts`, `tests/resource-io-url-provider.test.ts` cover lease cleanup and materialize authority. | pass |
| AC-023 | `tests/resource-io-transfer.test.ts` proves cancellation before staging/import and preserves fixed transfer behavior. | pass |
| AC-026 | `tests/resource-io-route.test.ts`, `tests/resource-io-authority-boundary.test.ts`, `tests/resource-io-authority-context.test.ts` reject raw/public forged authority. | pass |

## 4. Verification

| Command | Result | Summary |
|---|---|---|
| red: `volta run --node 24.16.0 npm test -- --maxWorkers=1 tests/resource-root-identity.test.ts` | expected fail | New chmod scope test received `disjoint` before correction. |
| red: `volta run --node 24.16.0 npm test -- --maxWorkers=1 tests/resource-event-bus.test.ts` | expected fail | Async subscriber produced one unhandled rejection before correction. |
| root/source green | pass | `tests/resource-root-identity.test.ts tests/provider-root-identity.test.ts tests/knowledge-source-registry.test.ts`: 3 files, 16 tests. |
| event green | pass | `tests/resource-event-bus.test.ts`: 1 file, 7 tests. |
| persistence receipt | pass | `tests/persistence-store-registry.test.ts tests/persistence-startup-receipt.test.ts`: 2 files, 17 tests. |
| `volta run --node 24.16.0 npm run typecheck` | pass | all three TypeScript projects passed. |
| `node scripts/scan-persistent-stores.mjs` | pass | 62 stores, 758 sites; only inventory changed. |
| `volta run --node 24.16.0 npm test -- --maxWorkers=1` | unverified | Process completed, but this worker's terminal summary was unavailable after session detachment; do not treat it as a pass. |

- The initial full-suite failure in `tests/persistence-store-registry.test.ts` was T-10-introduced stale inventory, not a baseline failure. D-T10-01 was approved by Lead on 2026-08-10; regeneration adds only the two `UrlProvider` `fs.rmSync(stageDir, { recursive: true, force: true })` `remove-path` receipts (ordinals 0 and 1). `build/persistence-startup-receipt.json`, scanner logic, and all other `build/**` paths remain unchanged.
- Full repository lint remains a baseline-wide failure (11 errors and 7,979 warnings in unrelated files); T-10 targeted lint had no errors and 17 pre-existing warnings in unchanged `any` sites.
- E2E is not applicable: this is a ResourceIO/server contract change verified at its stable module and route seams.

## 5. Review

### Standards Axis

- Result: pass.
- Findings: correction round resolved scope-token fail-open classification and async callback rejection isolation; no remaining T-10 standards finding.

### Specification Axis

- Result: pass.
- Findings: AC-011, AC-014, AC-020, AC-023, and AC-026 map to the focused regression suite above. No public raw-root or workspace authority was added.

## 6. Ownership and Deviation

- Writable changes remain within T-10, except D-T10-01: Lead explicitly approved only `<Path>build/persistence-store-inventory.json</Path>` because this Ticket added persistent-store-scanned staging cleanup sites.
- No read-only consumer changed; `<Path>tests/provider-root-identity.test.ts</Path>` remains untouched.
- Worktree: `specdev-worktree/T-10`; status: `review`; implementation checkpoint: `cfc0c7877e672a26f48a5e7f5d6a0f04c99e8776` plus this follow-up candidate.
