# Evidence: T-12 - Single-owner production cutover
## 1. 实现摘要
- Creation base_sha: `e758c7a12d31e8385b4993c406ae5acc04b18635`; resume/current checkpoint: `e6a687ba17752b4b5da3d46d6000f68b29abeaa9`; branch: `speculo/2026-08-09-openhanako-v0-446-6-integration/T-12`.
- Accepted input: T-13 governance `eba0480fc46fb929ab4473ff514f96ea0ecab09a`; reviewed File History candidate `e4600aff1fcd71285f8032fb610425ced5ead7cb`.
- Dependencies: T-10, T-11, T-19, and the accepted T-13 domain candidate; W4 and AC-009 through AC-013 govern this assembly proof.
- Writable: Engine, `production-workspace-runtime.ts`, one open-route mount, narrow route-security classification, and ten explicit production seam/composition/security test paths. T-19 File Tool identity injection remains unchanged.
- Read-only: ResourceIO, WorkspaceObservation contract, File History domain, and Knowledge domain. T-13/T-14 retain their domain ownership and remain `review`.
- Cutover descriptor: isolated proof -> stop old -> prove release -> start new; File History adds only a logical consumer, never a second physical owner.
- Required proof: activation, bounded read, one EventBus projection, rebind/dispose cleanup, one watcher/baseline for History plus Knowledge consumers, one production route mount, and `files.read`/`LOCAL_ONLY` authorization proof.
- Placement: HanaKDE system core, not a plugin, because privileged Engine/Workspace lifecycle and shared startup contract cannot fit plugin boundaries.

## D-T12-03: Resource Event Privacy Projection

- The `/resource-io/events` HTTP route now applies one safe projection to every caller, including authenticated loopback callers. A stale page or a page with one or more retained events returns only `{ stale: true, latestSequence, events: [], resync: "resource-stat-required" }`; an empty current page remains `{ stale: false, latestSequence, events: [] }`.
- `latestSequence` remains a JSON number. The route never serializes a raw event, `resourceKey`, resource path, root, or scope through this catch-up endpoint.
- Renderer catch-up already consumes this result as an authoritative gap recovery. Its regression now verifies resync/requery rather than raw catch-up event replay.
- Scope authorization: the integration lead explicitly assigned this D-T12-03 route/renderer regression work to T-12. No T-14 Knowledge implementation or type surface was changed.

## D-T12-04: Production Workspace Runtime Extraction (Correction Round 3)

- Authorization: `048eef9019d50fae4a7c72c3d75e2673d2ca14da` explicitly assigned `<Path>core/workspace-runtime/**</Path>` and `<Path>tests/production-workspace-runtime.test.ts</Path>` to T-12. The module remains in `core`; no core-to-server dependency was introduced.
- `<Path>core/workspace-runtime/production-workspace-runtime.ts</Path>` now owns the shared-baseline adapter, single-owner cutover state machine, legacy watch-owner release/proof, production watcher and baseline adapter, EventBus observation bridge, and physical-root helpers.
- `<Path>core/engine.ts</Path>` now injects authority, EventBus, legacy registry, cutover proof, and watch repartition into that deep module. It retains root selection, Engine lifecycle state, the descriptor proof callback, and active-watch repartition. The obsolete local bridge, legacy owner inspection/release helpers, coordinator inspection helper, and subscription field were removed.
- The bridge assertions now exercise `bridgeProductionWorkspaceObservation` directly. Existing Engine tests continue to cover production startup/dispose, agent/root switching, watch repartition, aggregate health, and logical subscriber behavior.
- The existing `KnowledgeIndexRuntime` constructor cast is unchanged until the integration lead applies T-14's exported shared-baseline port types from `ff9f38dd`. No additional cast or type bridge was added here.

## D-T12-06: File History Production Persistence Registration

- **Level / trigger:** ticket deviation. The healthy main production assembly now constructs `FileHistoryService`, while the persistence scanner still excludes `<Path>lib/file-history/**</Path>` as `dormant-file-history-input` and the production-boundary test still prohibits `file-history-sqlite` registration.
- **Affected artifacts / paths:** T-12 Ticket, Goal Plan, Tickets Map and status projection; `<Path>shared/persistence/store-registry.ts</Path>`, `<Path>scripts/scan-persistent-stores.mjs</Path>`, `<Path>scripts/generate-persistence-schema-fingerprint.mjs</Path>`, generated persistence inventory/fingerprint/startup receipt, and the focused production/persistence/runtime tests.
- **Options:** retain the exclusion (incorrect production census); defer it to T-21 (leaves W4 with an incorrect owner inventory); or let T-12 make the narrow registry correction. **Recommended:** T-12 registers the activated SQLite owner and regenerates the existing receipts without changing the History domain.
- **Approval:** Root Lead approved the ticket-level deviation at `2026-08-10T14:12:37+0800`, limited to the paths recorded in D-T12-06. T-13 retains `lib/file-history/**`; T-21 retains all unrelated manifest/build/package ownership.
- **Required result:** scanner counts the real production constructor and no longer has a dormant File History exclusion; registry/inventory/fingerprint agree on `file-history-sqlite` through a real-store temporary SQLite introspector; the physical private-store destination test rejects both a candidate alias and a Hana home ancestor symlink into main Workspace.
- **Focused red (before implementation):** `volta run --node 24.16.0 npx vitest run --maxWorkers=1 tests/file-history-production-boundary.test.ts tests/production-workspace-runtime.test.ts tests/persistence-store-registry.test.ts tests/persistence-schema-tripwire.test.ts` exited `1`: the new production-boundary assertion could not find `file-history-sqlite`; the registry contract lacked that store; tripwire checks reported the stale pre-registration fingerprint. The new Hana home ancestor symlink regression passed, confirming the existing physical-ancestor check already rejects that path after `historyStorePathForKey()`.
- **Implementation:** `file-history-sqlite` now owns the opaque `file-history/{historyStoreKey}/history.sqlite` SQLite family, is first opened at `runtime_ready`, and explicitly declares the main-only path/root invariant. The scanner no longer excludes `lib/file-history/**` and now censes both Engine `FileHistoryService` construction and the service's `FileHistoryStore` construction. The fingerprint generator opens a temporary real `FileHistoryStore` and inspects its runtime schema rather than duplicating DDL.
- **Generated receipts:** `volta run --node 24.16.0 node scripts/scan-persistent-stores.mjs` passed and rewrote inventory/startup receipt with `63 stores, 763 sites`. The existing fingerprint writer regenerated `<Path>build/persistence-schema-fingerprint.json</Path>` with compatible review reasoning: the activated schema-v1 baseline has no released compatibility or migration contract and leaves DATA_EPOCH unchanged.
- **Focused green:** `volta run --node 24.16.0 npx vitest run --maxWorkers=1 tests/file-history-production-boundary.test.ts tests/production-workspace-runtime.test.ts tests/persistence-store-registry.test.ts tests/persistence-schema-tripwire.test.ts` passed: 4 files, 42 tests. This covers the production registry/scanner, exact receipt equality, real temporary File History SQLite schema, cache reset behavior, and both direct and Hana home ancestor symlink private-store rejection.
- **Final result:** resolved, subject to the complete W4 matrix, project checks, and independent two-axis review.

## Verification Receipt

- `npx vitest run tests/resource-io-route.test.ts desktop/src/react/__tests__/services/resource-events.test.ts` -- pass: 2 files, 84 tests. Existing watch retry tests emit their expected failure-path console warnings.
- `npx vitest run tests/production-workspace-runtime.test.ts tests/engine-resource-events.test.ts tests/engine-lifecycle.test.ts tests/resource-io-route.test.ts desktop/src/react/__tests__/services/resource-events.test.ts` -- pass: 5 files, 126 tests. The lifecycle suite logs the known local `better-sqlite3` Node ABI mismatch while preserving its assertions.
- `npx vitest run desktop/src/react/__tests__/architecture/no-ui-native-watch-production.test.ts tests/engine-resource-events.test.ts tests/production-workspace-runtime.test.ts` -- pass: 3 files, 31 tests.
- `npx eslint core/engine.ts core/workspace-runtime/production-workspace-runtime.ts tests/production-workspace-runtime.test.ts tests/engine-resource-events.test.ts tests/engine-lifecycle.test.ts server/routes/resource-io.ts tests/resource-io-route.test.ts desktop/src/react/__tests__/services/resource-events.test.ts desktop/src/react/services/resource-events.ts` -- pass: 0 errors; repository warnings remain.
- `git diff --check` -- pass.
- `npm run lint` -- blocked only by 11 pre-existing `no-undef` errors in Speculo scripts outside this candidate: `<Path>speculo/skills/github-npm-ops/scripts/issue-transport.mjs</Path>` and `<Path>speculo/workflows/specdev/common/tools/validate-specdev.mjs</Path>`.

## Current Candidate Status

- D-T12-03 and the D-T12-04 extraction are complete and focused checks are green.
- W4 File History production assembly remains `in_progress` from `e6a687ba`. Its real Engine/Workspace/EventBus seam, root-switch lifecycle, and stop-failure recovery are implemented and verified below; it still awaits combined W4 review.
- No W4 Ticket or change status is advanced to `done`; T-13 and T-14 remain `review`, and this worktree must not merge to integration.

## W4 File History Assembly Receipt

- `createMainFileHistoryBinding` is a main-only logical consumer: it reuses `MainWorkspaceRuntime.subscribe` for the canonical baseline and subscribes only to committed `ResourceEventBus` facts. It creates no watcher, source walk, second event bus, or raw-root projection.
- The binding derives its opaque history key from the coordinator-approved root identity. It uses the revalidated physical watch target for membership and ResourceIO reads, so a `/var` to `/private/var` canonicalization cannot discard valid main baseline files. Root/store comparison also resolves the nearest physical ancestor, rejecting an external-looking private-store path that enters the workspace through a symlink.
- Reads are bounded through `ResourceIO.stat` and `openRead`; the inclusive `openRead.end` is capped at `maxBytes - 1` even when the stat size is unavailable. Oversized content is reported as truncated before its body is consumed.
- Engine activates History only after a healthy production cutover, skips reactivation for an unchanged healthy opaque store key, and closes History before coordinator stop. A failed coordinator stop retains the binding and assembly state so a same-root retry can reactivate History; successful stop clears the binding.
- The real Engine seam uses `FileHistoryService` with an in-memory store adapter only to isolate the local SQLite native-module ABI. It still runs production Engine, coordinator, ResourceIO, EventBus, watcher adapter, root authority, and Knowledge runtime paths.

## W4 Verification Receipt

- `volta run --node 24.16.0 npx vitest run --maxWorkers=1 tests/production-workspace-runtime.test.ts tests/engine-resource-events.test.ts tests/engine-lifecycle.test.ts` -- pass: 3 files, 51 tests. Includes baseline capture, later watcher/EventBus capture, unchanged-root no-reactivation, History-before-stop recovery, one real shared History+Knowledge watcher/baseline/EventBus seam, and root-switch ordering: old History store close precedes old cutover stop and new binding/store activation.
- The combined real Engine seam asserts exactly one physical `fs.watch`, healthy coordinator counts `{ watchers: 1, baselines: 1 }`, initial baseline delivery to both History and Knowledge, exactly one EventBus fact for a physical change, delivery of that change to both consumers, and cleanup after Knowledge dispose plus production stop.
- The root-switch seam asserts a distinct opaque History store key for the new root, no raw root text in either key, and that the current History service cannot query the old root path after switch.
- `volta run --node 24.16.0 npx vitest run --maxWorkers=1 tests/engine-lifecycle.test.ts tests/file-history-production-boundary.test.ts tests/http-route-security.test.ts` -- pass: 3 files, 44 tests.
- `volta run --node 24.16.0 npx eslint --quiet core/engine.ts core/workspace-runtime/production-workspace-runtime.ts tests/engine-resource-events.test.ts tests/production-workspace-runtime.test.ts tests/engine-lifecycle.test.ts` -- pass: 0 errors.
- `git diff --check` -- pass.
- The T-12 worktree's ignored `node_modules` had only Vitest cache entries, so direct relative SDK and TypeScript imports could not resolve packages already installed in sibling worktrees. Restoring local ignored links for `@earendil-works`, `diff`, and `@firecrawl` (including Anydoc's macOS ARM64 optional binary) does not change source, manifests, or the lockfile; it restores the declared dependency closure for this isolated worktree.

## Lead Verification Reopened (Resolved, 2026-08-10)

- **Execution model / provider:** `native-subagent` / `Worker-T-12`; Lead's final-checkpoint typecheck found real `tsconfig.test.json` errors after the earlier local verification batch.
- **Dispatch / final checkpoint:** `e758c7a12d31e8385b4993c406ae5acc04b18635` / `e6a687ba17752b4b5da3d46d6000f68b29abeaa9`; current authorized-path candidate diff SHA-256: `401fad51b1abd23f4a4d9ab7e6c0b63b9b065e76e3a0fd23394d6ebede991224`.
- **Candidate delivery verification:** code/test changes remain limited to the authorized T-12 assembly paths; the correction must retain this boundary and must not change File History, Knowledge, ResourceIO, route implementation, manifests, or lockfiles.
- **Prior results invalidated:** the earlier 187-test W4 matrix and three-stage typecheck were not run against Lead's final checkpoint. They are provisional only and must not support acceptance.
- **Required correction:** repair the concrete test type contracts in `engine-lifecycle`, `engine-resource-events`, and `production-workspace-runtime` without broad casts; then rerun complete Node 24 typecheck and the final W4 matrix from the corrected worktree.
- **Status:** T-12 remains `in_progress`; T-13 and T-14 remain `review`. No acceptance, commit, merge, status promotion, or W4 closure is authorized.

## Lead Correction Receipt (2026-08-10)

- **Corrected test contracts:** lifecycle callback fixtures now return `void`; Workspace observations include `cursor` and `repairCycle`; EventBus fixtures implement `subscribe`; Knowledge baseline runtime fixtures return complete `WorkspaceSnapshot` values; the watcher fixture uses a typed `FSWatcher`; and File History fixtures use narrowed roots, typed `MainWorkspaceRuntime`, `ResourceStat`, and `ResourceOpenReadResult` adapters rather than broad casts.
- **`volta run --node 24.16.0 npm run typecheck`** -- pass: all three configured TypeScript projects (`tsc --noEmit`, `tsconfig.node.json`, and `tsconfig.test.json`) completed with zero diagnostics. This replaces the previously reported dependency-resolution condition.
- **`volta run --node 24.16.0 npx vitest run --maxWorkers=1 tests/engine-resource-events.test.ts tests/engine-lifecycle.test.ts tests/production-workspace-runtime.test.ts tests/file-history-service.test.ts tests/file-history-route.test.ts tests/file-history-production-boundary.test.ts tests/knowledge-index-event-coordinator.test.ts tests/knowledge-index-runtime.test.ts tests/knowledge-index-shared-repair.test.ts tests/knowledge-workspace-route.test.ts tests/http-route-security.test.ts tests/server-composition-boundary.test.ts`** -- pass: 12 files, 187 tests.
- **`volta run --node 24.16.0 npx eslint --quiet core/engine.ts core/workspace-runtime/production-workspace-runtime.ts tests/engine-resource-events.test.ts tests/production-workspace-runtime.test.ts tests/engine-lifecycle.test.ts`** -- pass: zero errors.
- **`node <Path>speculo/workflows/specdev/common/tools/validate-specdev.mjs</Path> --stage implement <Path>speculo/.speculo/specdev/changes/2026-08-09-openhanako-v0-446-6-integration</Path>`** -- pass: 0 errors, 0 warnings.
- **`git diff --check`** -- pass.
- **Status remains unchanged:** this receipt records only corrected verification; T-12 remains `in_progress`, T-13/T-14 remain `review`, and no acceptance, commit, merge, or status transition occurred.

## Lead Independent Re-verification (2026-08-10)

- Lead reran the corrected candidate rather than reusing any pre-correction result: `volta run --node 24.16.0 npm run typecheck` completed all three configured projects with exit 0, and the authoritative 12-file W4 Vitest matrix completed with 187 passing tests, including both spawned-server composition smokes.
- **Standard axis:** pass. The correction uses complete `WorkspaceSnapshot`, `ResourceStat`, `ResourceOpenReadResult`, EventBus, and lifecycle callback contracts. The remaining partial watcher doubles are isolated from File History/Knowledge behavior, while the invalid assembly test deliberately supplies incomplete input to exercise the factory's runtime fail-fast guard. No broad cast hides the corrected ResourceIO, observation, baseline, or read seam.
- **Specification axis:** pass. The fresh matrix still proves the W4 obligations: one physical watcher and baseline for History plus Knowledge (AC-009/AC-012); release-before-start and retry ordering (AC-010); committed EventBus fan-out (AC-011); and health-gated, main-only, opaque-keyed File History assembly plus narrow route authorization (AC-013/AC-026). ADR-002, ADR-005, ADR-006, and ADR-007 remain preserved.
- Lead reran the full authorized-path ESLint command, the Implement validator, and `git diff --check`; all pass. The earlier acceptance was properly invalidated before this fresh result and must not be cited separately.
- **Final W4 candidate status:** Lead verification passes, but T-12 remains `in_progress` until the W4 integration owner accepts all three T-12/T-13/T-14 candidates. No commit, merge, Ticket status promotion, or W4 closure is authorized.

## D-T12-06 Final Correction and W4 Acceptance (2026-08-10)

- **Late red/green correction:** a final registry census review found the obsolete Engine `_ensureFileHistoryService()` fallback still created a second `FileHistoryService` site. The added constructor-census assertion was red with **2** Engine `file-history-sqlite` sites. Removing that fallback leaves the single constructor in `HanaEngine` initialization; the final registry assertion is green with exactly **1** Engine site.
- **Generated receipt refresh:** `volta run --node 24.16.0 node scripts/scan-persistent-stores.mjs` passed and regenerated the committed production census as **63 stores, 762 sites**. The prior `763` count was before the duplicate constructor correction and is superseded. The schema fingerprint continues to use a temporary real `FileHistoryStore` runtime DDL inspection and its compatible no-migration review declaration.
- **Authoritative W4 matrix:** `volta run --node 24.16.0 npx vitest run --maxWorkers=1 tests/engine-resource-events.test.ts tests/engine-lifecycle.test.ts tests/production-workspace-runtime.test.ts tests/file-history-service.test.ts tests/file-history-route.test.ts tests/file-history-production-boundary.test.ts tests/knowledge-index-event-coordinator.test.ts tests/knowledge-index-runtime.test.ts tests/knowledge-index-shared-repair.test.ts tests/knowledge-workspace-route.test.ts tests/http-route-security.test.ts tests/server-composition-boundary.test.ts tests/persistence-store-registry.test.ts tests/persistence-schema-tripwire.test.ts` -- pass: **14 files, 220 tests**. This includes the post-registration registry and schema tripwires, both spawned-server composition checks, the T-14 route regression, one physical watcher/shared baseline/EventBus proof, root-switch teardown/rebind, and private-store symlink isolation.
- **Project checks:** `volta run --node 24.16.0 npm run typecheck` -- pass (all three TypeScript projects, zero diagnostics). `volta run --node 24.16.0 npx eslint --quiet` over every changed T-12 source/test path -- pass (zero errors). `node speculo/workflows/specdev/common/tools/validate-specdev.mjs --stage implement speculo/.speculo/specdev/changes/2026-08-09-openhanako-v0-446-6-integration` -- pass (0 errors, 0 warnings). `git diff --check` -- pass.
- **Acceptance mapping:** AC-009/AC-012 are covered by the single physical watcher and shared baseline seam; AC-010 by release-before-start and stop-failure retry ordering; AC-011 by exactly one committed EventBus fact fanning out to History and Knowledge; AC-013/AC-025 by health-gated File History activation plus its existing isolated `FAILED`/scoped-retry contract; AC-026 by main-only opaque key, physical private-store rejection, and `files.read`/`LOCAL_ONLY` route classification.

## Final Independent Review (2026-08-10)

**Fixed point and scope:** baseline `e6a687ba17752b4b5da3d46d6000f68b29abeaa9`; controlled dirty T-12 candidate diff. Source discovery found the change-local Spec, T-12/T-13/T-14 Tickets, ADR-002/003/005/006/007/011, Goal Plan W4 dispatch, and local frozen source. Applicable engineering sources are `<Path>AGENTS.md</Path>`, `<Path>CONTRIBUTING.md</Path>`, `<Path>eslint.config.js</Path>`, TypeScript/Vitest configuration, and the SpecDev Fowler baseline.

### Standards Axis

- **Result:** pass.
- **Findings:** no hard-rule violation or unresolved correctness, lifecycle, security, resource-release, or maintainability finding. The `FileHistoryService` construction is now singular; its deep lifecycle remains behind the existing service interface. The Engine only composes a logical consumer, while the runtime adapter owns path projection and bounded reads. ESLint, typecheck, deterministic receipt tests, and diff integrity cover the mechanical standards.

### Specification Axis

- **Result:** pass.
- **Findings:** no missing, out-of-scope, or conflicting behavior. The candidate preserves ADR-002 main-only scope, ADR-005's separate History/Knowledge stores with shared facts, ADR-006 stop-then-start cutover, and ADR-007 opaque private identity. It adds no watcher, full walk, duplicate EventBus, raw root/store projection, migration, compatibility switch, mount History, or History domain edit. The post-registration persistence census exactly matches the activated production owner required by D-T12-06.

The standards axis was completed before the specification axis; the two results were kept independent. No unapproved deviation or blocker remains. Windows/macOS package gates, restore convergence, and UI E2E remain explicitly owned by later Tickets rather than being claimed by W4.

## 2. 修改范围

| 路径 | 所有权 | 改动目的 |
|---|---|---|
| `<Path>core/engine.ts</Path>` and `<Path>core/workspace-runtime/production-workspace-runtime.ts</Path>` | T-12 writable/shared owner | Compose the healthy main-only logical History consumer, lifecycle teardown/rebind, bounded ResourceIO read bridge, and one-owner cutover proof. |
| `<Path>server/composition/open-root.ts</Path>` and `<Path>server/http/route-security.ts</Path>` | T-12 shared owner | Mount the existing read-only route once and classify its three reads as `files.read`, every other method/path as `LOCAL_ONLY`. |
| `<Path>shared/persistence/store-registry.ts</Path>`, persistence scanner/generator, and committed receipts | D-T12-06 approved extension | Register the activated private SQLite store and fingerprint its real runtime DDL. |
| T-12 runtime, boundary, security, composition, and persistence tests | T-12 writable/shared owner | Prove cutover, physical-store isolation, security, deterministic receipts, and one Engine constructor. |

No `<Path>lib/file-history/**</Path>`, `<Path>core/knowledge-workspace/**</Path>`, ResourceIO, route implementation, manifest, or lockfile was changed by this candidate.

## 3. 验收与合同映射

| Contract / Acceptance ID | 验证接缝 | 证据 | 结果 |
|---|---|---|---|
| AC-009 | Real Engine History + Knowledge assembly | one `fs.watch`, one baseline, logical subscriber churn | pass |
| AC-010 | production cutover/recovery state machine | release-before-start and stop-failure retry assertions | pass |
| AC-011 | ResourceEventBus bridge | one committed fact reaches both logical consumers | pass |
| AC-012 | shared main baseline | History and Knowledge consume one canonical observation walk | pass |
| AC-013 | health-gated activation and scoped retry | coordinator and File History health/failure behavior | pass |
| D-T12-06 / AC-025 / AC-026 | registry/schema/security seams | real SQLite introspection, opaque external store, symlink rejection, route policy | pass |

## 4. 验证执行

| 命令或步骤 | 运行环境 | 结果 | 摘要 |
|---|---|---|---|
| 14-file W4 Vitest matrix recorded above | Node 24.16.0 | pass | 220 tests, including registry/schema tripwires and all W4 consumers. |
| `volta run --node 24.16.0 npm run typecheck` | Node 24.16.0 | pass | All configured TypeScript projects have zero diagnostics. |
| Changed-path `eslint --quiet` | Node 24.16.0 | pass | Zero errors. |
| persistence scanner | Node 24.16.0 | pass | Deterministic receipt: 63 stores, 762 sites. |
| SpecDev implement validator and `git diff --check` | local | pass | 0 errors/0 warnings; no whitespace errors. |

- **失败后修复与重跑：** Removed the second Engine constructor after the registry census was red with two sites; reran the complete matrix and project checks.
- **未运行检查：** Windows/macOS package gates, restore E2E, and UI E2E belong to T-15/T-16/T-22/T-23 and are not W4 claims.
- **E2E：** Not applicable to this backend/production-assembly Ticket.
- **反向验证：** Symlinked private-store destination, unhealthy startup, failed release proof, stale schema/receipt, and unauthorized route variants all fail closed in focused tests.
- **外部声明：** None.

## 5. 双轴审查

### 标准轴

- **结果：** pass.
- **来源：** `<Path>AGENTS.md</Path>`, `<Path>CONTRIBUTING.md</Path>`, ESLint/TypeScript/Vitest configuration, and Fowler baseline.
- **Findings：** None unresolved; the duplicate Engine construction was corrected before this final review.
- **修正与重跑：** Final 14-file matrix, typecheck, lint, registry scan, and diff check passed.

### 规范轴

- **结果：** pass.
- **来源：** Spec, T-12/T-13/T-14, ADR-002/003/005/006/007/011, and W4 Goal Plan dispatch.
- **Findings：** None; no migration, compatibility switch, non-main History, private observer, duplicate baseline/EventBus, or raw-root projection was introduced.
- **修正与重跑：** D-T12-06 was approved before its minimal registry correction and final verification.

## 6. 路径所有权审计

- **writable 内修改：** All production paths match the expanded T-12 frontmatter grant.
- **shared 修改与 owner 批准：** T-12 owns the named assembly/security paths; D-T12-06 explicitly grants only the persistence registration correction.
- **read-only 修改：** None in File History, Knowledge, ResourceIO, or route domain source.
- **未声明路径：** None.
- **生成文件或锁文件：** Only the three approved deterministic persistence receipts; no manifest or lockfile edit.

## 7. 偏差与决策

- **偏差：** `D-T12-06` ticket-level, resolved.
- **偏差记录：** This Evidence, T-12 Ticket, Goal Plan, Tickets Map, and change status.
- **批准人或决策来源：** Root Lead, `2026-08-10T14:12:37+0800`.
- **对范围、契约、依赖或后续 Ticket 的影响：** Activates only the real File History SQLite registration; no History domain ownership transfer or T-21 package scope transfer.

## 8. 残余风险与后续

- **残余风险：** Native Windows/macOS behavior and product restore/UI flows are intentionally deferred to their blocking Ticket owners.
- **已知限制：** File History remains main-only and uses no released-schema migration contract.
- **后续 Ticket：** T-15 secure restore convergence, then T-16/T-17 and platform/package gates.
- **监控或回滚触发条件：** Any nonzero owner overlap, unavailable root proof, schema receipt drift, or private-store relation failure reopens the owning Ticket; recovery remains stop-then-start.

## 9. 交付定位

- **Commit / PR：** Local T-12 candidate branch; no PR, push, deployment, release, archive, signing, or notarization.
- **最终 Workspace 引用：** `specdev-worktree/T-12`.
- **Evidence 文件：** `<Path>{roots.state}/specdev/changes/2026-08-09-openhanako-v0-446-6-integration/evidence/T-12.md</Path>`.
