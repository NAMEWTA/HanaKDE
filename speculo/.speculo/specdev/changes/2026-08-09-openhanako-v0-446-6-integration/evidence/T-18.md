# Evidence: T-18 - @ search lifecycle

- **Change:** `2026-08-09-openhanako-v0-446-6-integration`
- **Ticket:** `<Path>{roots.state}/specdev/changes/{change}/ticket/18-fuse-at-search-lifecycle.md</Path>`
- **Spec:** `<Path>{roots.state}/specdev/changes/{change}/spec.md</Path>`
- **Tickets Map:** `<Path>{roots.state}/specdev/changes/{change}/tickets-map.md</Path>`
- **Goal Plan:** `<Path>{roots.state}/specdev/changes/{change}/goal-plan.md</Path>`
- **Baseline / branch:** `2018ce1dc671f0d9bb3c6f61f4078625c7863001`; `speculo/2026-08-09-openhanako-v0-446-6-integration/T-18`
- **Worktree:** `specdev-worktree/T-18`
- **Status:** review

## 1. 实现摘要

The file mention search owns one cancelable identity at a time. A new query, scope change, tab change, menu close, or unmount retires the prior identity; only the current identity may update results or loading. The existing desk search adapter remains unchanged, so cancellation prevents stale UI writes even though the adapter does not yet accept an `AbortSignal`.

## 2. 修改范围

| Path | Ownership | Purpose |
|---|---|---|
| `<Path>desktop/src/react/components/InputArea.tsx</Path>` | writable | Retire stale file-search work on query, scope, tab, close, and unmount transitions. |
| `<Path>desktop/src/react/utils/file-mention-items.ts</Path>` | writable | Provide the cancelable current-search identity. |
| `<Path>desktop/src/react/__tests__/components/InputArea.file-mention.test.tsx</Path>` | writable | Cover rapid input, scope changes, clear/close, unmount, and provider failure. |
| `<Path>desktop/src/react/__tests__/utils/file-mention-items.test.ts</Path>` | writable | Cover lifecycle identity retirement. |
| `<Path>tests/knowledge-workspace-e2e/specs/at-search-lifecycle.spec.ts</Path>` | writable | Exercise direct keyboard selection and expose provider-response failures. |

## 3. 验收与合同映射

| Contract / acceptance ID | Verification seam | Evidence | Result |
|---|---|---|---|
| AC-024 | InputArea lifecycle, MentionMenu contract, and direct web-full flow | Component/utility regression proves latest-query, scope, close, unmount, and error behavior; the MentionMenu suite asserts tab ARIA and the layout suite asserts compact constraints. The direct web-full flow searches the full-only Desk route, selects with Enter, and closes with Escape. | pass |

## 4. 验证执行

| Command or step | Environment | Result | Summary |
|---|---|---|---|
| red: `volta run --node 24.16.0 npm exec -- vitest run desktop/src/react/__tests__/components/InputArea.file-mention.test.tsx` | Node 24.16.0 | expected fail | Before the lifecycle change, an active-scope switch issued only one search request. |
| `volta run --node 24.16.0 npm exec -- vitest run desktop/src/react/__tests__/components/InputArea.file-mention.test.tsx desktop/src/react/__tests__/utils/file-mention-items.test.ts` | Node 24.16.0 | pass | 2 files, 11 tests. |
| `volta run --node 24.16.0 npm exec -- vitest run desktop/src/react/__tests__/components/FileMentionMenu.test.tsx desktop/src/react/__tests__/components/MentionMenu.test.tsx desktop/src/react/__tests__/components/InputArea.layout.test.ts desktop/src/react/__tests__/utils/mention-items.test.ts desktop/src/react/__tests__/components/InputArea.file-mention.test.tsx desktop/src/react/__tests__/utils/file-mention-items.test.ts` | Node 24.16.0 | pass | 6 files, 24 tests. The MentionMenu suite asserts tab ARIA and the layout suite asserts compact/narrow CSS constraints. |
| `volta run --node 24.16.0 npm run typecheck` | Node 24.16.0 | pass | All three TypeScript projects passed. |
| correction trigger: `volta run --node 24.16.0 npm run test:knowledge:e2e -- specs/at-search-lifecycle.spec.ts --project=web-open` | Node 24.16.0 / Playwright | expected fail, D-T18-01 | 1 test failed, exit 126. The E2E applicability guard selected web-open even though its direct flow calls the full-only `/api/desk/search-files`, which returned `404 Not Found`. |
| `volta run --node 24.16.0 npm run test:knowledge:e2e -- specs/at-search-lifecycle.spec.ts --project=web-full` | Node 24.16.0 / Playwright | pass | 1 executed, 1 passed in 3.7s. Real `@` search returned the fixture file; Enter selected it and Escape closed the reopened menu. |
| `volta run --node 24.16.0 npm exec -- vitest run desktop/src/react/__tests__/components/InputArea.paste-and-slash.test.tsx -t "selects the highlighted slash command on Enter without falling through to message send"` | Node 24.16.0 | pass | 1 selected keyboard test passed; 17 unrelated tests skipped by the name filter. |

- **Failure repair and rerun:** D-T18-01 corrects only the E2E runtime applicability from web-open to web-full. The original 404 is retained above as the triggering observation; the corrected web-full direct flow executed and passed.
- **Unrun checks:** No separate locale-render or visual theme screenshot run. `window.t` labels and theme CSS are unchanged; this remains low residual review risk.
- **E2E:** web-full direct keyboard flow passed; no waiver is required.
- **Reverse verification:** The red scope-change test demonstrated the pre-fix stale-lifecycle gap; the focused suite is green after the change.
- **External declarations:** none.

## 5. 双轴审查

### Standards Axis

- **Result:** pass
- **Sources:** repository TypeScript/React patterns, `<Path>{roots.workflows}/specdev/common/rules/codebase-design.md</Path>`, and Fowler baseline.
- **Findings:** none. The lifecycle module has a small interface, owns its cancellation invariant locally, and prevents retired promises from mutating React state.
- **Correction and rerun:** not applicable.

### Specification Axis

- **Result:** pass
- **Sources:** T-18, AC-024, and the change spec.
- **Findings:** none after D-T18-01. The existing Desk adapter is intentionally full-only, and the corrected E2E runs in that applicable runtime without changing any provider or server contract.
- **Correction and rerun:** web-full direct flow passed.

## 6. 路径所有权审计

- **Writable changes:** D-T18-02 adds the exact `<Path>desktop/src/react/components/InputArea.tsx</Path>` host-lifecycle path to the Ticket. After that approved ticket correction, every actual project change is covered by T-18 `writable_paths`.
- **Shared changes and owner approval:** none.
- **Read-only changes:** none.
- **Undeclared paths:** none.
- **Generated files or lockfiles:** none. `npm ci` was used only to restore the worktree test environment and did not modify tracked dependency metadata.

## 7. 偏差与决策

- **D-T18-01 (local verification correction, round 1 / 3):** the direct E2E was guarded for `web-open`, but its exercised adapter calls the full-only Desk route. The resulting `404 Not Found` was a test applicability error, not a product composition defect.
- **Affected path and scope:** only `<Path>tests/knowledge-workspace-e2e/specs/at-search-lifecycle.spec.ts</Path>` changes its applicability guard and skip message from web-open to web-full.
- **Rejected alternative:** registering Desk routes in Open composition was rejected because Desk is deliberately closed-product and `<Path>server/composition/full-root.ts</Path>` remains its sole owner. Changing server composition, desk routes, or the provider adapter would violate T-18 ownership and the open/closed boundary.
- **Approval:** parent integration-owner correction instruction in this execution turn; approval scope is the T-18 E2E applicability correction and Evidence/state projection only.
- **Outcome:** web-full direct flow executed and passed. Candidate remains `review`; no product behavior, backend, ResourceRef, or provider contract changed.
- **D-T18-02 (ticket-level path-contract correction, round 2 / 3):** Lead audit found that `<Path>desktop/src/react/components/InputArea.tsx</Path>` is required by T-18's host lifecycle contract but was not included by the narrower `<Path>desktop/src/react/components/input/**</Path>` glob.
- **Approval and overlap audit:** Lead approves only the exact InputArea path in `expected_changes` and `writable_paths`. It has no W3 writable overlap: T-11 owns `core/workspace-runtime/**`, `shared/workspace-*.ts`, and desktop main/watch infrastructure; T-19 owns `lib/document-extract/**` and `<Path>lib/tools/file-tool.ts</Path>`.
- **Outcome:** Ticket authority now covers every actual T-18 project edit without widening to `components/**`; status remains `review` and no production code changed in this correction round.

## 8. 残余风险与后续

- **Residual risk:** the UI abort identity cannot physically abort the existing HTTP request until the read-only desk adapter accepts an `AbortSignal`; it does prevent stale result/loading writes.
- **Known limitation:** no known E2E applicability blocker remains for the Desk flow.
- **Follow-up:** rerun the web-full direct flow after any future Desk provider or input-menu change.
- **Monitoring or rollback:** no migration or persistent state; revert this renderer slice if an integration regression is found.

## 9. 交付定位

- **Commit / PR:** implementation checkpoint `94a39379a1cb966d949c752be1d56ff73907d75a`; this Evidence/state-only amendment follows it.
- **Final worktree:** `specdev-worktree/T-18`
- **Evidence file:** `<Path>{roots.state}/specdev/changes/{change}/evidence/T-18.md</Path>`
