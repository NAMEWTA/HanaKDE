# Evidence: T-20 - Upgrade Office to Knowledge Ingestion

- **Change:** `2026-08-09-openhanako-v0-446-6-integration`
- **Ticket:** `<Path>{roots.state}/specdev/changes/2026-08-09-openhanako-v0-446-6-integration/ticket/20-upgrade-office-to-knowledge-ingestion.md</Path>`
- **Worktree:** `specdev-worktree/T-20`
- **Execution model / provider:** Lead implementation; Worker-T-20 candidate line
- **Status:** candidate; awaiting independent Lead review and integration

## 1. 实现摘要

Knowledge source rereads now route Office/PDF resources through the T-19
`DocumentExtractionService`, project the returned derived Markdown through the
existing Markdown Semantic IR extractor, and retain the original Office path
and resource kind in the source-partitioned index. The resource version and
T-19 extractor version are combined into the document version token, and a
post-extraction ResourceIO stat rejects a version race before publication.

The Office plugin accepts the same canonical extraction port for full reads of
authorized resource refs. HTML DOCX, structured XLSX JSON, and PDF page-range
responses remain product adapters; legacy absolute `filePath` callers retain
their existing local-only behavior. No derived Markdown resource is written.

## 2. Feature placement decision

This capability belongs to the system core: it changes the privileged
Knowledge ingestion/index contract, owns source generation and extractor
version invalidation, and must be driven by the main/mount ResourceIO event
pipeline. The Office plugin remains a format-specific adapter contribution for
HTML/JSON/page-range output and consumes the shared service; it does not own
Knowledge state, watchers, or indexing.

## 3. 修改范围与路径审计

| Path | Ownership | Delivered behavior |
|---|---|---|
| `<Path>core/knowledge-workspace/knowledge-index-event-coordinator.ts</Path>` | writable | Canonical Office reread seam with address stability check |
| `<Path>core/knowledge-workspace/knowledge-index-runtime.ts</Path>` | writable | Shared DocumentExtractionService and extractor contract version |
| `<Path>lib/knowledge-workspace/document-index-extractor.ts</Path>` | writable | Resource/version metadata, extraction failure projection, derived IR adapter |
| `<Path>lib/knowledge-workspace/markdown-index-extractor.ts</Path>` | writable | Reusable derived-Markdown Semantic IR projection |
| `<Path>plugins/office/**</Path>` | writable | Canonical resource read path plus retained product adapters |
| `<Path>tests/knowledge-office-ingestion.test.ts</Path>` | writable | IR, version, failure, race, and no-write tests |
| `<Path>tests/office-plugin-tools.test.ts</Path>` | writable | Canonical Office tool consumer regression |
| `<Path>tests/knowledge-workspace-e2e/specs/office-knowledge.spec.ts</Path>` | writable | Owner E2E create/rebuild/search/no-derived-file flow |

No T-19 extraction, ResourceIO kernel, History, package, or lockfile path was
changed. No undeclared production path or generated artifact was added.

## 4. 验收与合同映射

| Contract | Verification seam | Result |
|---|---|---|
| AC-003 | Existing Office and Knowledge suites | Passed in the focused regression matrix |
| AC-017 | Resource version post-stat race and indexed version token | Passed; stale extraction rejects with `resource_version_conflict` |
| AC-021 | Office resource -> shared extraction -> Markdown IR/index/search seam | Passed in focused unit/reader tests; runtime contract includes T-19 extractor version |
| AC-022 | Canonical extraction boundary and filesystem assertions | Passed; no OCR or derived Workspace write path |

## 5. 验证执行

| Command | Result |
|---|---|
| `volta run --node 24.16.0 -- npm exec vitest run tests/knowledge-office-ingestion.test.ts tests/office-plugin-tools.test.ts --reporter=dot` | pass: 2 files, 20 tests |
| `volta run --node 24.16.0 -- npm exec vitest run tests/knowledge-office-ingestion.test.ts tests/office-plugin-tools.test.ts tests/knowledge-index-runtime.test.ts tests/knowledge-index-event-coordinator.test.ts tests/knowledge-index-rebuild.test.ts tests/markdown-index-extractor.test.ts tests/document-extract-unit.test.ts tests/document-extract-resource.test.ts --reporter=dot` | pass: 8 files, 108 tests |
| Scoped ESLint over changed T-20 source/tests | pass: 0 errors; existing `any` warnings only in Office adapter |
| `git diff --check` | pass |
| `tsc --noEmit -p tsconfig.node.json` | classified baseline: existing missing `@firecrawl/anydoc` and Pi SDK declarations; no T-20 diagnostics |
| `tests/knowledge-workspace-e2e/specs/office-knowledge.spec.ts` | added; not run locally because the full Electron/Playwright fixture requires the packaged extraction/runtime environment |

The same Knowledge runtime/rebuild suite fails under the host Node 22 only
when loading the prebuilt `better-sqlite3` ABI; the required Node 24.16 run is
green and is the recorded result.

## 6. 双轴审查

### Standards axis

- ResourceIO remains the only stat/read/materialization authority.
- Canonical extraction is bounded and inherits T-19's no-OCR, no-write, and
  materialize cleanup guarantees.
- Derived Markdown is parsed directly into the existing IR; no second parser,
  watcher, or workspace artifact was introduced.
- Scoped lint and whitespace checks are clean.

### Specification axis

- Office/PDF content is searchable under its original `.docx`/`.xlsx`/`.pdf`
  resource path and binary/pdf kind.
- Resource and extractor versions invalidate the derived projection; a race
  raises typed `resource_version_conflict` before a stale document is returned.
- Canonical full-resource reads use T-19 while HTML/JSON/PDF-range and legacy
  local-only product behaviors remain available.

## 7. 偏差与决策

- **Deviation:** none.
- **Residual gate:** the owner E2E is committed but awaits a packaged/native
  Anydoc environment; T-21 owns production packaging and T-22/T-23 own native
  platform gates.
- **Correction rounds:** 0 / 3.

## 8. 回滚与后续

Rollback is a code-wave revert before integration; no user data migration is
needed because the derived projection is rebuilt from the source resource.
Before release, run the owner Playwright flow with the packaged Anydoc assets,
then close T-21 packaging and platform gates. Candidate commit SHA is recorded
after the final pre-merge check.
