# Evidence: T-02 - 整合上游 v0.421.24 checkpoint

- **Change：** `2026-08-09-openhanako-v0-446-6-integration`
- **Ticket：** `<Path>{roots.state}/specdev/changes/{change}/ticket/02-integrate-v0-421-24.md</Path>`
- **Spec / ADR / CONTEXT：** `<Path>{roots.state}/specdev/changes/{change}/spec.md</Path>`、`<Path>{roots.state}/specdev/changes/{change}/ADR.md</Path>`、`<Path>{roots.state}/specdev/changes/{change}/CONTEXT.md</Path>`。
- **依赖：** T-01=`done`，已读取 `<Path>{roots.state}/specdev/changes/{change}/evidence/T-01.md</Path>`。
- **基线 / target / 分支：** `fabe31dd8f36313f05ec635a4ce30d890bb91bd3` / `v0.421.24^{commit}=e87769a070d12803247e5cc619dacf5814fe1f52` / `speculo/2026-08-09-openhanako-v0-446-6-integration/T-02`。
- **Worktree：** `specdev-worktree/T-02`；source package=`n/a-local-git`。
- **实施者 / 状态：** native Worker-T-02 / `review`（尚未自行集成或关闭 Gate）。

## 开工回执（8 行）

1. 目标：仅将冻结 `v0.421.24` 作为首个 staged merge checkpoint，不提前进入 T-10+ 收敛。
2. 基线：`fabe31dd8f36313f05ec635a4ce30d890bb91bd3`；分支与工作树均匹配 Dispatch。
3. 目标：`v0.421.24^{commit}=e87769a070d12803247e5cc619dacf5814fe1f52`；merge-base=`ef8a6f700191c2486effd3761a4bd2b7f3ad774c`。
4. 依赖：T-01 状态 `done`，Evidence 已读取；当前 index/worktree 干净。
5. 路径：Ticket 的 writable=`<Path>**</Path>`，但只接受本 checkpoint 差异与保持合同绿色的最小适配。
6. 预期冲突：五个路径；运行时冲突按 HanaKDE 安全/生命周期合同语义裁决，生成 receipt 待源配置最终确定后重建。
7. 最大风险：以 `ours/theirs` 覆盖丢失 Resource/Knowledge/Transfer/security/Workbench 外部合同，或提前引入 compatibility、migration、dual-run/dual-write。
8. 计划：`git merge --no-ff --no-commit v0.421.24` 后逐项分类、定向验证、受控反向检查和双轴审查；候选仅交 Lead 集成。

## 1. 实现摘要

已在授权分支执行 `git merge --no-ff --no-commit v0.421.24`，并将该 release 作为唯一 staged checkpoint。正常上游增量被保留；五项文本冲突均已按来源、HanaKDE 合同和最终生成源裁决。特别地，`session-actions.ts` 同时保留上游 Primary Agent 默认工作区行为和 HanaKDE Knowledge Workspace close guard；mobile bootstrap 新增的 `effectiveHomeFolder` 仅对本地 owner 披露，远端不泄漏 native path。未引入 T-10+ Resource/History/Extraction 收敛、dual-run、dual-write、compatibility shell 或 migration。

## 2. 修改范围

| 路径 | 所有权 | 改动目的 |
|---|---|---|
| `<Path>**</Path>` 中的 146 个上游路径 | T-02 writable | 吸收 `v0.421.24` release 增量。 |
| `<Path>desktop/src/react/stores/session-actions.ts</Path>` | T-02 writable | 合并 Primary Agent 默认工作区与 Knowledge close guard。 |
| `<Path>server/routes/mobile-workbench.ts</Path>` | T-02 writable | 保留 local-path redaction，并安全吸收 `effectiveHomeFolder`。 |
| `<Path>tests/mobile-workbench-route.test.ts</Path>` | T-02 writable | 增加远端 bootstrap 不泄漏 `effectiveHomeFolder` 的行为断言。 |
| `<Path>build/cli-runtime-closure.json</Path>`、`<Path>build/persistence-schema-fingerprint.json</Path>`、`<Path>build/persistence-store-inventory.json</Path>` | generated | 最终 merged source/lockfile 下由 canonical generators 重建。 |
| 本 Evidence 与 T-02 Ticket | workflow-owned | 记录候选、验证和 `review` 状态。 |

## 3. 验收与合同映射

| Contract / Acceptance ID | 验证接缝 | 证据 | 结果 |
|---|---|---|---|
| AC-001 | Git ancestry / staged merge | `MERGE_HEAD` 与 `v0.421.24^{commit}` 均为 `e87769a070d12803247e5cc619dacf5814fe1f52`；五项冲突已清零 | pass，待 Lead 用候选 SHA 复核 ancestor |
| AC-002 | release 回归、typecheck、client build | Node 24 `npm test`、`npm run typecheck`、`npm run build:client` 均 exit 0 | pass |
| AC-003 | Workbench、Knowledge lifecycle、persistence/security stable seams | 定向 Vitest：mobile Workbench、session/desk actions、Knowledge close lifecycle、persistence/closure suites均 exit 0 | pass |

## 4. 验证执行

| 命令或步骤 | 运行环境 | 结果 | 摘要 |
|---|---|---|---|
| `git merge --no-ff --no-commit v0.421.24` | local Git | pass | 只产生预期 5 个文本冲突，无额外冲突。 |
| Node 24 `npm ci` | isolated T-02 `node_modules` | pass | 按 lockfile 安装 1368 packages；Node `v24.16.0` / ABI 137。 |
| `node scripts/scan-persistent-stores.mjs` | Node 24 | pass | 59 stores、831 sites。 |
| `node scripts/generate-persistence-schema-fingerprint.mjs --classification compatible ...` | Node 24 | pass | DATA_EPOCH=1；fingerprint=`sha256:959ba20e66df2c9076c0800ef6717efb02be05011b368301d12df08f32956005`。 |
| `node scripts/compute-cli-closure.mjs` | Node 24 | pass | 9,640 files（source-graph=660、runtime-asset=11、nft-runtime-trace=8969）。 |
| 定向 Vitest（Workbench/session/desk/lifecycle/persistence/closure） | Node 24 | pass | 所选回归套件全部 exit 0。 |
| `npm run typecheck` | Node 24 | pass | 三个 `tsc --noEmit` 项均通过。 |
| `npm test` | Node 24 | pass | 完整 Vitest 回归 exit 0。 |
| `npm run build:client` | Node 24 | pass | 五个 Vite build 完成；仅现有 HTML script-tag / unused import 警告。 |

- **失败后修复与重跑：** 首次 fingerprint 在 Node 22 遇到 `better-sqlite3` ABI 127/137 不匹配，且依赖树缺 package；分类为环境/调用错误。以本项目声明的 Node 24 重装后重跑通过。首次 typecheck 调用误用了不存在的本地 npm 路径；分类为验证调用错误，正确命令重跑通过。无本 Ticket 产品失败。
- **受控反向验证：** 先以未 redacted 的 upstream `effectiveHomeFolder` 候选运行 `tests/mobile-workbench-route.test.ts`，远端 bootstrap 用例精确因真实 workspace path 泄漏而 red（26 pass、1 fail）；随后纳入 `mayDiscloseLocalPaths` 后同一套件 green（27/27）。`persistence-schema-tripwire` 还执行其 source-drift fail-closed cases；`git diff --name-only --diff-filter=U` 与 marker scan 均为空。
- **未运行检查：** Windows/macOS native/package Gate、真实用户 E2E、T-10+ product convergence 不属于 T-02，保持未验证而非宣称通过。
- **E2E：** 待 Lead 在 integration baseline 执行最小 mobile bootstrap / session workspace-switch 场景；预期本地 owner 有效目录可见、远端无 native path、close guard 仍能取消切换。

## 5. 双轴审查

### 标准轴

- **结果：** pass。
- **来源：** Git three-way stages、运行时调用者、定向回归、完整 Vitest、typecheck、client build、receipt tripwire。
- **Findings：** 未发现正确性、类型、生成物、错误处理或安全回归。两处运行时冲突采用合并而非覆盖；生成 receipt 由最终源重建。
- **修正与重跑：** mobile path disclosure 发现后完成一轮 red→green 修正；受影响套件、完整回归、typecheck/build 均重跑通过。

### 规范轴

- **结果：** pass。
- **来源：** AC-001—AC-003、ADR-001、DEC-001、DEC-013、T-02、Dispatch W1.1/G1 与 T-01 Evidence。
- **Findings：** 已保留 Knowledge close guard、Resource/Transfer/security/Workbench 既有合同；上游增量保持在 `v0.421.24`，未跳 tag，未改 Map/Goal Plan/change/global status，也没有提前实施 T-10+、migration、compat、dual-run 或 dual-write。
- **修正与重跑：** 无规范偏差；候选仅推进 `review`，不自报 `done`。

## 6. 五类裁决与路径所有权审计

| 类别 | 路径 / 裁决 |
|---|---|
| upstream normal | 141 个无文本冲突的上游路径按 ADR-001 默认接受；不以 fork 内部偶然实现拒绝 release 修复。 |
| HanaKDE contract | `<Path>desktop/src/react/stores/session-actions.ts</Path>` 保留 Knowledge Workspace close guard；`<Path>server/routes/mobile-workbench.ts</Path>` 保留 `mayDiscloseLocalPaths` 与 safe-message/redaction 边界。 |
| semantic merge | 前者同时导入并使用上游 `findPrimaryAgent` / `resolveAgentWorkspace`；后者把上游 `effectiveHomeFolder` 纳入既有本地披露 gate。 |
| generated | 三份 `build/` receipt 不接受任一侧旧 hash/line receipt，均由 canonical generator 对最终源重建。 |
| deletion | 拒绝上游对 close guard、remote path redaction 与相关 Workbench 安全语义的删除；保留它们并吸收不冲突的上游行为。 |

- **writable 内修改：** 实际项目路径均为 Ticket `writable_paths=<Path>**</Path>` 内，且限于该 checkpoint；无 shared path。
- **read-only / sibling / integration 修改：** 无。
- **未声明路径：** 无；未改 Tickets Map、Goal Plan、change/global status 或其他 worktree。
- **生成文件：** 仅上述三份 tracked receipt；生成器执行记录见第 4 节。

## 7. 偏差与决策

- **偏差：** 无需 Spec、ADR 或 Ticket 变更的偏差。
- **决策来源：** T-02、Dispatch W1.1/G1、ADR-001、T-01 Evidence 与 Lead 的冲突约束。
- **影响：** 后续 T-03 必须以 Lead 集成后的 T-02 checkpoint 为基线；本 Worker 不移动 integration line。

## 8. 残余风险与后续

- **残余风险：** 跨平台原生/package 与 UI E2E 尚未在 T-02 关闭；它们属于后续 Gate/Lead 验收，不能由 Linux/macOS 本地单元验证替代。
- **已知限制：** candidate 仍在 Ticket worktree，尚未被 Lead 独立应用、审查或 merge。
- **后续 Ticket：** T-03，仅在 Lead E2E 与集成验收后。
- **恢复触发：** candidate parent、target ancestor、path audit、TDD 红绿记录或 Lead 回归不符时，保留本 worktree 并从此 checkpoint修正，最多 3 轮。

## 9. 交付定位

- **Commit / PR：** 本地候选 merge commit（待本 Worker 创建；无 PR、push 或 remote write）。
- **最终 Workspace：** `specdev-worktree/T-02`。
- **Evidence：** `<Path>{roots.state}/specdev/changes/{change}/evidence/T-02.md</Path>`。

## 10. Delegated Evidence Addendum

- **Execution model / Provider：** `native-subagent` / native Worker-T-02。
- **Session / Package locator：** `specdev-worktree/T-02` / `n/a-local-git`。
- **Dispatch / Final checkpoint：** `fabe31dd8f36313f05ec635a4ce30d890bb91bd3` / pending local candidate merge commit。
- **Correction rounds：** 1 of 3。
- **Candidate delivery verification：** 工作树、base、target、T-01、五项 conflict paths、source/lockfile、receipt generators、范围和命令结果均由本 Worker 独立核对；待 Lead 再次独立核对。
- **Lead E2E：** 待执行：mobile bootstrap 与 Knowledge close-guard workspace-switch。
- **Unverified claims：** Windows/macOS native/package、真实 E2E 与 Lead integration 仍未验证；没有把它们标记为 pass。
