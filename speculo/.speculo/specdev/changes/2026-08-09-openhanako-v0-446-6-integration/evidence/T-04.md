# Evidence: T-04 - 整合上游 v0.441.3 checkpoint

- **Change：** `2026-08-09-openhanako-v0-446-6-integration`
- **Ticket：** `<Path>{roots.state}/specdev/changes/{change}/ticket/04-integrate-v0-441-3.md</Path>`
- **Spec：** `<Path>{roots.state}/specdev/changes/{change}/spec.md</Path>`
- **Tickets Map：** `<Path>{roots.state}/specdev/changes/{change}/tickets-map.md</Path>`
- **Goal Plan：** `<Path>{roots.state}/specdev/changes/{change}/goal-plan.md</Path>`
- **基线/目标：** `12ccbc02a919b08cac86a03b2b0ba5cfb4eeba26` / `v0.441.3^{commit}=f4f1b00282ea4e32ee8f64c71c1a57012c7b052a`
- **分支/工作树：** `speculo/2026-08-09-openhanako-v0-446-6-integration/T-04` / `specdev-worktree/T-04`
- **执行者：** `Worker-T-04`（native-subagent）；本 Evidence 由 Lead 独立核对后写入。
- **状态：** `done`

## 1. 实现摘要

### 开工回执

1. T-03 Ticket 为 `done`，T-03 Evidence 已读；基线是 Lead 清理 T-03 后发布的本地 checkpoint。
2. target 为可解析 commit；双方共同基点为 `427821a3c27a03e84370b285065d5fd9d56ddf98`（`v0.433.1`）。
3. 真实冲突面是三份 generated receipt 和 MCP `mcp-api.ts`；没有把 `servers` 回退或 legacy migration 带入解析结果。
4. 最大回退风险覆盖 MCP persistence/routes、session submit、Resource/Knowledge/Transfer/Workbench 和 credential security；无 push、PR、deploy、release 或远程写入。

吸收 `v0.433.1..v0.441.3` 的冻结上游增量：MCP 两级 defer、session-file ResourceIO/materialize、loop lifecycle、DeepSeek Responses、alpha update worker、i18n error presentation、credential custody 和 upload realpath 修复。冲突解析保留 HanaKDE 的 connector-only MCP contract，并将 generated closure/persistence receipts 按最终源码重建。

独立标准轴发现并修复了 defer threshold 的异步显示缺陷：`McpTab` 先以 `EMPTY_MCP_STATE` 的 `10` 渲染，加载已有阈值后 `DeferSettings` 的 local state 不会更新。先增加 `10 -> 25` 且不触发保存的 red test，再以 `[deferThreshold]` effect 同步显示 state；没有改变 API、保存或输入校验语义。

按本 change 的锁定决策，运行时移除了 retired provider/config migration、`added-models.yaml` 回退和 migration-backup compatibility 行为；`provider-catalog.json` 是唯一 catalog，provider/media CRUD、credential permission healing、DeepSeek Responses 与 `builtinDeferEnabled` 均保留。

## 2. 修改范围

| 路径 | 所有权 | 改动目的 |
|---|---|---|
| `<Path>core/</Path>`、`<Path>lib/</Path>`、`<Path>server/</Path>`、`<Path>shared/</Path>` | writable | 吸收 loop、provider、credential、ResourceIO、permission、MCP 与 server runtime 增量；删除 retired migration modules。 |
| `<Path>desktop/</Path>` | writable | 吸收 deferred-tool settings、error presentation、session/input UI、locales 与 updater 增量；修复异步加载的 defer threshold 显示。 |
| `<Path>tests/</Path>` | writable | 上游行为覆盖，并删除仅断言 retired migration 的测试。 |
| `<Path>build/</Path>`、`<Path>release-digest.v*.json</Path>`、`<Path>export-manifest.json</Path>` | writable/generated | 从最终源码重建 closure、persistence receipts、release/export inventory。 |
| `<Path>tools/alpha-gate-worker/</Path>`、`<Path>SECURITY.md</Path>` | writable | 吸收 invite-gated alpha update worker 与安全说明。 |

实际范围为相对 `12ccbc02` 的 175 个已跟踪路径；`git diff --check 12ccbc02` 通过，未出现未解决 index entry。

## 3. 验收与合同映射

| Contract / Acceptance ID | 验证接缝 | 证据 | 结果 |
|---|---|---|---|
| AC-001 | target ancestry、merge parent、冲突/生成物审计 | candidate `2ae89bd839473849fd28aae48130a12451e851ca` has `12ccbc02` and `f4f1b002` as parents; integration `5dee75c3a1c88eddc02c2e890fd583e036c083a2` contains the candidate and `v0.441.3`; the three receipts were rebuilt | pass |
| AC-002 | 上游 feature regressions | loop、MCP defer、provider/DeepSeek、credential custody、ResourceIO/materialize、upload 相关定向测试通过 | pass |
| AC-003 | HanaKDE contract union | Resource/Knowledge/session/server/port guard 定向回归、typecheck、client build、结构扫描通过 | pass |

## 4. 验证执行

| 命令或步骤 | 运行环境 | 结果 | 摘要 |
|---|---|---|---|
| 9 个隔离 Vitest suites | Node `24.16.0`，每项 fresh `HANA_HOME` | pass | 175 tests：CLI closure 19、knowledge copy 12、knowledge performance 13、persistence receipt 3、registry 11、server composition 8、home guards 15、port ownership 7、sessions route 87。 |
| `npm test` | Node `24.16.0`，fresh isolated home | pass (prior run) | `1155 passed | 1 skipped` files；`11638 passed | 7 skipped` tests。 |
| `volta run --node 24.16.0 npm run typecheck` | Node `24.16.0` | pass | TypeScript checks completed. |
| `volta run --node 24.16.0 npm run build:client` | Node `24.16.0` | pass | main/preload/renderer/splash/theme completed. |
| deferred-tool focused regression | Node `24.16.0` | pass | `DeferSettings`、`McpTab`、MCP API/route、engine defer 和 preference config：6 files、69 tests。 |
| Lead integration affected-regression replay | Node `24.16.0` | pass | 27 files：402 passed、1 skipped；covers the affected Resource/Knowledge/session/server contract union. |
| Lead generated/server/session replay | Node `24.16.0` | pass | 9 files：175 passed in fresh isolated homes. |
| `HANA_HOME="$(mktemp -d)" volta run --node 24.16.0 npx vitest run --maxWorkers=1 ...McpTab... ...DeferSettings... ...mcp-api... tests/engine-tool-defer.test.ts tests/mcp-routes.test.ts` | Node `24.16.0` | pass | 5 files、57 tests：loaded defer threshold, MCP route/API and engine-defer seams passed. |
| `HANA_HOME="$(mktemp -d)" volta run --node 24.16.0 npm run typecheck` | Node `24.16.0` | pass | all three TypeScript projects passed on the integration line. |
| `HANA_HOME="$(mktemp -d)" volta run --node 24.16.0 npx playwright test -c tests/knowledge-workspace-e2e/playwright.config.ts --project=web-open --grep 'E2E-KW-001 opens a blank main Knowledge shell'` | Node `24.16.0` | pass | 1 browser E2E passed with one worker. |
| structural scans | Node `24.16.0` | pass | retired migration files absent；no retired provider/config runtime refs；no MCP `servers` fallback；no runtime `added-models.yaml` fallback；`git diff --check` clean。 |
| `node scripts/session-path-identity-audit.mjs` | Node `24.16.0` | pass | exit 0；输出的既有 identity risks 均为已分类、已测试项。 |
| `npm run lint` | repository baseline | fail (pre-existing) | 11 errors、7,847 warnings；errors only in `<Path>speculo/skills/github-npm-ops/scripts/issue-transport.mjs</Path>` and `<Path>speculo/workflows/specdev/common/tools/validate-specdev.mjs</Path>`; `git diff 12ccbc02 --` 这两路径为空。 |

- **失败后修复与重跑：** 先前 T-04 orphan Vitest/server process 已识别并清理；上述 9 个进程型/生成型 suite 之后均在 fresh `HANA_HOME` 中通过。`DeferSettings` red test 先确认 `25` 被错误保留为 `10`，修复后 related 69 tests 与 typecheck 均通过。
- **未运行检查：** candidate commit 后的单 worker full suite 尚待 Lead 重跑。一次并行饱和的 JSON full-suite run 报 `11623 passed, 15 failed, 7 skipped`，其中 11 个为 `STACK_TRACE_ERROR` timeout、4 个为 server-spawn timeout；它不覆盖此前的隔离绿证据，不能作为 pass。
- **E2E：** Lead integration ran the minimum existing browser smoke: `E2E-KW-001 opens a blank main Knowledge shell` passed on `web-open` with one worker. MCP deferred settings and session/server paths are covered by the focused integration regression replay above; there is no dedicated MCP Playwright fixture in this repository.
- **反向验证：** retired-file/reference、MCP `servers` fallback 与 `added-models.yaml` fallback 的结构检索必须为空；保持删除后通过。
- **外部声明：** 无；所有 pass 均来自本地可复查命令。

## 5. 双轴审查

### 标准轴

- **结果：** pass（1 个 local UI finding 已修复并重跑）。
- **来源：** `<Path>AGENTS.md</Path>`、`<Path>CONTRIBUTING.md</Path>`、`<Path>eslint.config.js</Path>`、`<Path>package.json</Path>` 与 Fowler baseline。
- **核对：** credential write/heal 保持 owner-only error semantics；provider catalog 读写归于单一 store；MCP defer 使用 connector-only state；server/ResourceIO 改动有对应测试；generated files 由源码 receipts 证明而非手改。
- **修正与重跑：** `DeferSettings` 现在在异步 prop 更新时同步 threshold、不会调用 save；6 个 related test files/69 tests 和 typecheck 通过。lint failure 是基线而非本 Ticket 修改。

### 规范轴

- **结果：** pass（AC-001 仍待 candidate-parent / integration 再核）。
- **来源：** `<Path>{roots.state}/specdev/changes/{change}/spec.md</Path>`、Ticket T-04、`<Path>{roots.state}/specdev/changes/{change}/ADR.md</Path>`、Goal Plan Dispatch T-04。
- **核对：** 只吸收冻结 `v0.441.3`，未进入 T-05+ 或 T-10+ redesign；旧 migration/compatibility 未被恢复；HanaKDE connector-only and security contracts preserved。
- **修正与重跑：** Lead 在 candidate 固定 SHA 上独立执行 ancestry、diff/path、affected regression 和 generated receipt review。

两轴按标准轴、规范轴顺序独立完成，未将 finding 混排。

## 6. 路径所有权审计

- **writable 内修改：** 全部在 T-04 `<Path>**</Path>` release-increment scope 内。
- **shared 修改与 owner 批准：** 无；W1 strict serial。
- **read-only 修改：** 无。
- **未声明路径：** 无。
- **生成文件或锁文件：** `package-lock.json`、release/export digests 和 build receipts 均来自冻结上游或最终源码生成；三份 receipt conflict 已以最终 source re-evaluate。

## 7. 偏差与决策

- **偏差：** 无。
- **决策来源：** T-04 locked decisions：retired provider/config migration modules 和 compatibility/cutover 不保留；`provider-catalog.json` canonical；explicit `sandbox_network: false` remains false while missing defaults enabled。
- **影响：** T-05 只能从 Lead 已接受的 T-04 integration SHA 起步。

## 8. 残余风险与后续

- **残余风险：** full suite 在高并发环境出现过超时；受影响 integration suites、focused Defer/MCP replay、typecheck and one-worker browser E2E are green, but no candidate-SHA single-worker full-suite claim is made.
- **已知限制：** lint 仍被两个 workflow-owned baseline files 阻塞，非 T-04 scope。
- **后续 Ticket：** T-05，前提是 Lead 完成 AC-001 candidate/integration acceptance。
- **监控或回滚触发条件：** candidate parent 或 target ancestry 不正确、generated receipt drift、受影响 regression 失败、MCP connector-only contract 复活时，停止在 T-03/T-04 checkpoint，不开始 T-05。

## 9. 委派交付附录

- **Execution model / Provider：** `native-subagent` / Codex.
- **Session/Package locator：** `specdev-worktree/T-04` / `n/a-local-git`.
- **Dispatch / Final checkpoint：** `12ccbc02a919b08cac86a03b2b0ba5cfb4eeba26` / `5dee75c3a1c88eddc02c2e890fd583e036c083a2`.
- **Correction rounds：** 1：标准轴发现的 defer threshold 异步显示问题已以 red/green test 修复。
- **Candidate delivery verification：** source scope (175 tracked paths), conflict paths, generated receipts, candidate parents, integration containment, targeted tests and structural scans were independently checked by Lead.
- **Lead E2E：** pass: one-worker `web-open` `E2E-KW-001 opens a blank main Knowledge shell`.
- **Unverified claims：** no candidate-SHA single-worker full suite yet; no production/native E2E claimed.

## 10. 交付定位

- **Commit / PR：** candidate `2ae89bd839473849fd28aae48130a12451e851ca`; accepted local integration merge `5dee75c3a1c88eddc02c2e890fd583e036c083a2`; no PR/push.
- **最终 Workspace 引用：** `specdev-worktree/T-04`.
- **Evidence 文件：** `<Path>{roots.state}/specdev/changes/{change}/evidence/T-04.md</Path>`.

## 11. Lead Acceptance

- **Candidate and integration checkpoint:** Lead independently inspected candidate `2ae89bd839473849fd28aae48130a12451e851ca`, its parents `12ccbc02a919b08cac86a03b2b0ba5cfb4eeba26` and `f4f1b00282ea4e32ee8f64c71c1a57012c7b052a`, and integration merge `5dee75c3a1c88eddc02c2e890fd583e036c083a2`. `v0.441.3` is an integration ancestor; there are no unmerged index entries or merge markers.
- **Lead standard-axis result:** pass. The integration replay covered 27 affected files / 402 passed / 1 skipped and 9 generated/server/session files / 175 passed. The focused one-worker Defer/MCP replay passed 5 files / 57 tests, including loaded threshold synchronization without save, MCP API/routes, and engine defer.
- **Lead specification-axis result:** pass. AC-001--AC-003, ADR-001, and the W1.3/G1 dispatch remain satisfied: only the frozen `v0.441.3` boundary was accepted, no T-05+ or T-10+ work entered, retired migrations/compatibility remain removed, and the connector-only/security contracts stay protected.
- **Lead independent quality checks:** `volta run --node 24.16.0 npm run typecheck` passed. The minimum browser E2E, `E2E-KW-001 opens a blank main Knowledge shell`, passed on `web-open` with one worker. `volta run --node 24.16.0 npm run build:client` had already passed on this integration checkpoint.
- **Residual verification boundary:** `npm run lint` remains nonzero only for the known 11 unchanged baseline errors in `<Path>speculo/skills/github-npm-ops/scripts/issue-transport.mjs</Path>` and `<Path>speculo/workflows/specdev/common/tools/validate-specdev.mjs</Path>`. A saturated full-suite run is not claimed as green; Windows/macOS native/package Gates remain assigned to later Tickets.
- **External actions:** no push, PR, deployment, release, archive, remote write, migration, or production action occurred. T-04 is accepted as `done`; T-05 may start only from `5dee75c3a1c88eddc02c2e890fd583e036c083a2`.
