# Evidence: T-03 - 整合上游 v0.433.1 checkpoint

- **Change：** `2026-08-09-openhanako-v0-446-6-integration`
- **Ticket：** `<Path>{roots.state}/specdev/changes/{change}/ticket/03-integrate-v0-433-1.md</Path>`
- **Spec / ADR / CONTEXT：** `<Path>{roots.state}/specdev/changes/{change}/spec.md</Path>`、`<Path>{roots.state}/specdev/changes/{change}/ADR.md</Path>`、`<Path>{roots.state}/specdev/changes/{change}/CONTEXT.md</Path>`。
- **Goal Plan：** `<Path>{roots.state}/specdev/changes/{change}/goal-plan.md</Path>`，Dispatch W1.2/G1。
- **依赖：** T-02=`done`；已读取 `<Path>{roots.state}/specdev/changes/{change}/evidence/T-02.md</Path>`，基线为其 Lead 集成后的 checkpoint。
- **基线 / target / 分支：** `f76c7ed6922b86f101192dea6936ca7bb2c9859f` / `v0.433.1^{commit}=427821a3c27a03e84370b285065d5fd9d56ddf98` / `speculo/2026-08-09-openhanako-v0-446-6-integration/T-03`。
- **Worktree：** `specdev-worktree/T-03`；source package=`n/a-local-git`。
- **实施者 / 状态：** native Worker-T-03 / `review`，尚未自行集成、关闭 Gate 或执行远程动作。

## 开工回执（8 行）

1. 目标：只吸收冻结 `v0.433.1`，形成 W1.2/G1 的可审计候选 checkpoint。
2. 基线：当前 HEAD=`f76c7ed6922b86f101192dea6936ca7bb2c9859f`，分支和 workspace 均与 Dispatch 一致。
3. 目标：`v0.433.1^{commit}=427821a3c27a03e84370b285065d5fd9d56ddf98`，merge-base=`e87769a070d12803247e5cc619dacf5814fe1f52`。
4. 依赖：T-02 Evidence 已读取；T-02 已由 Lead 集成，当前基线包含其 checkpoint。
5. 路径：writable=`<Path>**</Path>`，但只接受本 release 增量和保持既有合同绿色的最小适配。
6. 预期冲突：3 个 generated receipt、store index、composition boundary test、export manifest；均按五类裁决而非 `ours/theirs` 处理。
7. 最大风险：覆盖 Knowledge/Resource/Transfer/Workbench/security 合同，或提前引入 T-10+ 收敛、compat、migration、dual-run/dual-write。
8. 计划：执行 `git merge --no-ff --no-commit v0.433.1`，逐项裁决、canonical 重建、定向验证和受控反向检查；候选仅交 Lead。

## 1. 实现摘要

已在授权分支执行 `git merge --no-ff --no-commit v0.433.1`。正常上游增量保留为唯一 checkpoint；MCP 从 bundled plugin 收敛到 `core/mcp`，其 connector 配置、权限、OAuth、HTTP/stdio client、route 和设置页面随上游一起进入。Browser emergency-stop 后的下一条用户消息解除 session revocation，session compaction、tool catalog、workflow run limits、sidebar 与 connector-management 更新同样保留。

两项局部修正没有改变上层合同：agent connector 更新现在显式写入 `mcp.servers: null`，让既有 `deepMerge` 真实删除 retired key；browser-revocation fixture 则按照生产的 strict preflight interface 同步调用 `afterCachePreflight`。持久化、CLI closure 和开放边界 receipts 已在最终 merged source 上用 Node 24 canonical generators 重建。未提前实施 T-10+ Resource/History/Extraction 收敛、compatibility shell、migration、dual-run 或 dual-write。

## 2. 修改范围

| 路径 | 所有权 | 改动目的 |
|---|---|---|
| `<Path>**</Path>` 中的 `v0.433.1` release 增量 | T-03 writable | 吸收冻结 target 的正常上游功能、修复和删除。 |
| `<Path>core/mcp/**</Path>`、`<Path>plugins/mcp/**</Path>`、`<Path>server/routes/mcp.ts</Path>` | T-03 writable | 将 MCP runtime 置于 core owner，保留管理、权限和 route contract。 |
| `<Path>desktop/src/react/settings/tabs/mcp/**</Path>`、`<Path>desktop/src/react/components/input/**</Path>` | T-03 writable | 吸收 connector manager、approval 与 elicitation 用户流程。 |
| `<Path>core/mcp/manager.ts</Path>`、`<Path>tests/mcp-runtime.test.ts</Path>` | T-03 writable | 以 `null` tombstone 删除 retired `mcp.servers`，并用真实 YAML writer 覆盖持久化行为。 |
| `<Path>core/desktop-session-submit.ts</Path>`、`<Path>tests/desktop-session-submit-browser-revocation.test.ts</Path>` | T-03 writable | 保留严格 preflight 生产合同，并使 fixture 在正确的 synchronous hook 上观察 browser authorization 恢复。 |
| `<Path>build/persistence-store-inventory.json</Path>`、`<Path>build/persistence-startup-receipt.json</Path>`、`<Path>build/persistence-schema-fingerprint.json</Path>`、`<Path>build/cli-runtime-closure.json</Path>`、`<Path>build/open-boundary-baseline.json</Path>` | generated | 由最终 merged source 和 lockfile canonical 重建。 |
| 本 Evidence 与 T-03 Ticket | workflow-owned | 记录候选、审查、验证和 `review` 状态。 |

## 3. 验收与合同映射

| Contract / Acceptance ID | 验证接缝 | 证据 | 结果 |
|---|---|---|---|
| AC-001 | Git target / merge index / semantic audit | `MERGE_HEAD` 与 `v0.433.1^{commit}` 同为 `427821a3c27a03e84370b285065d5fd9d56ddf98`；65 个 upstream commits 已固定；无 unmerged index 或 conflict marker | pass，待 Lead 用候选 SHA 复核 ancestor |
| AC-002 | 上游 runtime、MCP、workflow 与 client build seams | Node 24 完整 `npm test` 为 1141 passed / 1 skipped files、11556 passed / 6 skipped tests；`npm run typecheck` 和 clean `npm run build:client` 均 exit 0 | pass |
| AC-003 | MCP persistence、browser/session、GPU/session、persistence registry 与 open-boundary seams | MCP gate 10 files / 281 tests、GPU/session gate 8 files / 157 tests、registry 11/11 均通过；legacy-marker production scan 为零 | pass |

## 4. 验证执行

| 命令或步骤 | 运行环境 | 结果 | 摘要 |
|---|---|---|---|
| `git merge --no-ff --no-commit v0.433.1` | local Git | pass | target 固定为 `427821a3c27a03e84370b285065d5fd9d56ddf98`；冲突均已裁决。 |
| canonical persistence / CLI / open-boundary receipt generators | Node `v24.16.0` | pass | 5 份 tracked receipt 与 final source 一致；MCP canonical storage 为 `HANA_HOME/mcp`，DATA_EPOCH 仍为 1。 |
| MCP gate（10 files） | Node `v24.16.0` | pass | 281/281；包含 runtime、routes、OAuth、reconnect、HTTP/stdio 与 renderer config seams。 |
| GPU/session gate（8 files） | Node `v24.16.0` | pass | 157/157；覆盖 strict preflight、browser revocation、GPU policy 和 session submit。 |
| `HANA_HOME="$(mktemp -d)" volta run --node 24.16.0 npx vitest run tests/mcp-runtime.test.ts tests/desktop-session-submit-browser-revocation.test.ts tests/persistence-store-registry.test.ts` | Node `v24.16.0` | pass | 3 files / 110 tests；MCP YAML tombstone、browser hook 和 deterministic receipts 当前复核通过。 |
| `HANA_HOME="$(mktemp -d)" volta run --node 24.16.0 npm run typecheck` | Node `v24.16.0` | pass | 三个 `tsc --noEmit` 项均通过。 |
| `HANA_HOME="$(mktemp -d)" volta run --node 24.16.0 npm test` | Node `v24.16.0` | pass | 1141 passed / 1 skipped files；11556 passed / 6 skipped tests。 |
| `HANA_HOME="$(mktemp -d)" volta run --node 24.16.0 npm run build:client` | Node `v24.16.0` | pass | clean isolated build 通过；生成 artifacts 保持 ignored/clean。 |
| `git diff --check HEAD`、`git diff --cached --check`、unmerged-index / marker scan | local Git | pass | whitespace、unmerged stages 和真实 merge marker 均为零。 |
| `HANA_HOME="$(mktemp -d)" volta run --node 24.16.0 npm run lint` | Node `v24.16.0` | baseline limitation | 11 unchanged errors 位于 `<Path>speculo/skills/github-npm-ops/scripts/issue-transport.mjs</Path>` 与 `<Path>speculo/workflows/specdev/common/tools/validate-specdev.mjs</Path>`；eslint 未给这些 Speculo `.mjs` 配置 Node globals。 |

- **失败后修复与重跑：** `LOC-T03-001` 将 omission 改为深合并删除 tombstone 后，真实 YAML persistence test 和 MCP gate 重跑通过。`LOC-T03-002` 将 fixture 的 preflight contract / hook timing 对齐后，browser/session gate 重跑通过。无未关闭的本 Ticket 产品失败。
- **受控反向验证：** persistence test 从带 legacy `mcp.servers` 的 YAML 起步，若 tombstone 改回 omission，`deepMerge` 会保留 retired key 而测试失败；registry tripwire 的 source-drift fail-closed cases 已执行。`git diff --name-only --diff-filter=U` 与 anchored marker scan 均为空。
- **未运行检查：** Windows/macOS native/package Gate、Lead 独立 integration 与真实 Desktop E2E 仍待后续验收；未被标记为 pass。
- **E2E：** 待 Lead 执行最小 Desktop MCP connector-management 和 browser emergency-stop/re-submit 场景。

## 5. 双轴审查

### 标准轴

- **结果：** pass。
- **来源：** `<Path>AGENTS.md</Path>`、`<Path>CONTRIBUTING.md</Path>`、`<Path>tests/README.md</Path>`、`<Path>eslint.config.js</Path>`、`<Path>package.json</Path>`、TypeScript configs 与 Fowler baseline。
- **Findings：** 无新的 correctness、module depth、error handling、安全、resource cleanup、test-quality 或 generated-drift finding。MCP tombstone 与 strict fixture 问题均在本审查前修正并由 stable seams 覆盖。lint 的 11 errors 为 unchanged configuration baseline，不是本 Ticket 引入的代码错误。
- **修正与重跑：** 两轮 local correction 后重跑 MCP/browser/persistence focus suite、全量 suite、typecheck、client build 与 diff checks；均符合上表结果。

### 规范轴

- **结果：** pass。
- **来源：** `<Path>{roots.state}/specdev/changes/{change}/source.md</Path>`、Spec AC-001--AC-003、ADR-001、T-03、Dispatch W1.2/G1、T-02 Evidence。
- **Findings：** target-only checkpoint、正常上游吸收、HanaKDE contract union 和 security fail-closed 仍成立。没有跳 tag、没有把 legacy MCP config 静默保留为当前 contract，也没有提前实施 T-10+ 单 owner 收敛、migration、compatibility、dual-run 或 dual-write。
- **修正与重跑：** 无需 Spec、ADR 或 Ticket deviation；candidate 仅推进 `review`，不自报 `done`。

## 6. 路径所有权审计

### 五类裁决

| 类别 | 路径 / 裁决 |
|---|---|
| upstream normal | 所有无真实 HanaKDE contract 冲突的 release paths 依 ADR-001 接受，不以 fork 内部旧实现阻止 upstream behavior。 |
| HanaKDE contract | `<Path>core/mcp/manager.ts</Path>` 保持 per-agent connector enablement 的 fail-closed behavior；`<Path>core/desktop-session-submit.ts</Path>` 保持 browser revocation 仅在下一条用户输入解除。 |
| semantic merge | agent config patch 写入 `servers: null`，符合 `<Path>lib/memory/config-loader.ts</Path>` 的 deepMerge deletion contract，同时保留现有 connector settings。 |
| generated | 5 份 build receipt 不接受任一侧历史 hash，由 final source/lockfile canonical 重建。 |
| deletion | 接受上游将 MCP runtime 移入 `core/mcp`，以及 retired legacy GPU cleanup / legacy-scan repair surface 的删除；现有 user preference 和 session manifest contracts 由定向 tests 保留。 |

- **writable 内修改：** 全部实际项目变更位于 T-03 的 `writable_paths=<Path>**</Path>`，且限于此 release 增量与 local corrections；无 shared path。
- **read-only / sibling / integration 修改：** 无。Tickets Map、Goal Plan、change worktree lifecycle 与 global status 是 Lead 集成投影，未由 Worker 改写。
- **未声明路径：** 无；未修改后续 Ticket 产品实现。
- **生成文件：** 仅第 2 节列出的 canonical receipt；来源和检验见第 4 节。

## 7. 偏差与决策

- **偏差：** `LOC-T03-001` 与 `LOC-T03-002`，均为已关闭的 local implementation correction。
- **记录：** `LOC-T03-001` 的触发事实是 omission 无法删除 existing YAML key；以 `mcp.servers: null` 复用既有 deepMerge tombstone，不改变公共 MCP connector contract。`LOC-T03-002` 的触发事实是 test double 未表示 strict `preflightSessionInput` contract；只修 fixture 的 hook timing，不放宽 production behavior。
- **决策来源：** T-03、ADR-001、Dispatch W1.2/G1，以及 `<Path>lib/memory/config-loader.ts</Path>` 的 null deletion semantics。
- **影响：** 无 Spec、ADR、Ticket、依赖、scope 或后续 Ticket 变更；无需升级为 ticket/spec/architecture/release deviation。

## 8. 残余风险与后续

- **残余风险：** Windows/macOS native/package 和 Lead integration/E2E 尚未关闭；它们仍是后续 Gate / Lead 验收，不以本机单元测试替代。
- **已知限制：** lint 受两个 unchanged Speculo `.mjs` 文件的 Node-global config 缺口阻断；不在 T-03 的 release integration scope 内。`<Path>tests/tool-defer-config.test.ts</Path>` 有一个非生产 test label 使用 invalid `permissionMode: "review"`，canonical spelling 为 `"review-all"`；当前 test 行为不依赖其为有效 policy，未扩大本 Ticket 修改面。
- **后续 Ticket：** T-04，仅在 Lead 验收并集成 T-03 candidate 后开始。
- **监控或回滚触发条件：** candidate parent / target ancestor、receipt tripwire、MCP persistence、browser session submit 或 Lead replay 失败时，保留本 worktree 从候选 checkpoint 修正，最多 3 轮。

## 9. 交付定位

- **Commit / PR：** pending local candidate merge commit；no PR、push、remote write、release、deploy 或 archive。
- **最终 Workspace：** `specdev-worktree/T-03`；Lead integration workspace 为 `specdev-worktree/integration`。
- **Evidence：** `<Path>{roots.state}/specdev/changes/{change}/evidence/T-03.md</Path>`。

## 10. Delegated Evidence Addendum

- **Execution model / Provider：** `native-subagent` / native Worker-T-03。
- **Session / Package locator：** `specdev-worktree/T-03` / `n/a-local-git`。
- **Dispatch / Final checkpoint：** `f76c7ed6922b86f101192dea6936ca7bb2c9859f` / pending local candidate merge commit。
- **Correction rounds：** 2 of 3。
- **Candidate delivery verification：** Worker 已独立核对 base、target、T-02 dependency、merge index、writable scope、receipt sources、focused regression、full suite、typecheck、build、review axes 和 lint baseline；Lead 仍须独立复核范围、candidate parents、ancestry 与受影响回归。
- **Lead E2E：** 待执行：Desktop MCP connector management 及 browser emergency-stop 后 re-submit user flow。
- **Unverified claims：** Windows/macOS native/package、Lead integration 和 Lead E2E 均未由 Worker 标记为 pass。
