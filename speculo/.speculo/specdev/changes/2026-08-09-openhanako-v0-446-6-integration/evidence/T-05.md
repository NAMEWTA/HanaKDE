# T-05 Preflight Receipt
- Base: `c20605b377b8c0cc72beb2c57cedf643d8d5908b` (accepted T-04 cleanup checkpoint).
- Target: `v0.441.32^{commit}=1558220440fefcb89482bea61e71789d02c34df1`.
- Dependency: T-04 is accepted `done`; its cleanup checkpoint is the only permitted base.
- Scope: staged upstream checkpoint with semantic conflict resolution only; no T-06+ convergence/cutover.
- Owner-overlap risk: no temporary duplicate production watcher, mutation path, baseline walk, parser, or store.
- Workspace/branch: `specdev-worktree/T-05` / `speculo/2026-08-09-openhanako-v0-446-6-integration/T-05`.
- External actions: none authorized or performed.

## 1. 实现摘要

Merged the frozen `v0.441.32` release increment while retaining T-04's removal of retired provider/config migration and migration-backup compatibility. Accepted upstream custody, provider-plugin storage, Win32 install ACL recovery, GPU recovery, session identity, security-directory, and UI/runtime fixes. The only local correction was to align the persistence registry with the upstream `provider-plugins/{storageId}` owner path and register the new ACL-heal state store; no T-06+ production convergence, migration, or duplicate owner was introduced.

## 2. 修改范围

| 路径 | 所有权 | 改动目的 |
|---|---|---|
| `<Path>core/</Path>`、`<Path>lib/</Path>`、`<Path>server/</Path>`、`<Path>shared/</Path>` | writable | Absorb security, provider/plugin storage, resource/session, and runtime fixes without restoring retired migration paths. |
| `<Path>desktop/</Path>`、`<Path>desktop/src/</Path>` | writable | Absorb GPU recovery, install ACL healing, renderer/plugin UI, locale, and desktop integration changes. |
| `<Path>tests/</Path>` | writable | Preserve upstream regression coverage and correct the registry owner-path expectation. |
| `<Path>build/</Path>`、`<Path>export-manifest.json</Path>`、`<Path>release-digest.v*.json</Path>` | generated / writable | Rebuild closure and persistence receipts from final source; include reachable security module in export closure. |

## 3. 验收与合同映射

| Contract / Acceptance ID | 验证接缝 | 证据 | 结果 |
|---|---|---|---|
| AC-001 | frozen target, merge index, generated receipts | candidate `96d252e6db51a50db3255cc5a2f7bfdf516e0a67` has `c20605b3` and `15582204` as parents; integration `5aa3318bd83d75fdec70706f7a9f30ade8310868` contains candidate and `v0.441.32` | pass |
| AC-002 | upstream security/GPU/plugin/session behavior | focused custody, ACL/GPU, provider/plugin, resource/session, and boundary tests; client/server builds | pass |
| AC-003 | HanaKDE contract union / no temporary owner | retired migration paths stay absent; provider catalog registry follows the single upstream storage owner; structural scans and focused contract tests | pass |

## 4. 验证执行

| 命令或步骤 | 运行环境 | 结果 | 摘要或附件 |
|---|---|---|---|
| `volta run --node 24.16.0 node scripts/scan-persistent-stores.mjs` | Node `24.16.0` | pass after local correction | Initially found the unregistered `win32-install-acl-heal` state; its explicit compatible descriptor was added, then scan reported 63 stores / 753 sites. |
| persistence fingerprint generator with reviewed compatible rationale | Node `24.16.0` | pass | Final fingerprint `sha256:8396545c8c40c049581d8cc03097f70778de0baa6c076288f77f4817e1dd94e7`. |
| `volta run --node 24.16.0 node scripts/compute-cli-closure.mjs` | Node `24.16.0` | pass | Final closure generated 9,660 files. |
| focused 20-file Vitest regression | Node `24.16.0`, `--maxWorkers=1` | repaired / pass | First run: 335/336; stale `providers/{providerId}` registry patterns failed. After correction, the affected 6-file core replay passed 117/117 and the other 19 files had passed on the first run. |
| `volta run --node 24.16.0 npm run typecheck` | Node `24.16.0` | pass | All TypeScript projects passed. |
| `volta run --node 24.16.0 npm run lint:boundary` | Node `24.16.0` | pass after local correction | Added reachable `<Path>core/security-dir.ts</Path>` to `<Path>export-manifest.json</Path>`. |
| `volta run --node 24.16.0 npm run build:client` | Node `24.16.0` | pass | Existing Vite warnings only. |
| `volta run --node 24.16.0 npm run build:server` | Node `24.16.0` | pass | darwin-arm64 server build completed. |

- **失败后修复与重跑：** The only new failure was the stale provider-plugin path asserted by `tests/persistence-store-registry.test.ts`; it was corrected to the actual upstream owner, receipts were regenerated, and the focused core replay passed.
- **未运行检查：** A single-worker full suite is not claimed because prior saturated full-suite runs time out. Windows/macOS package gates remain later Ticket work.
- **E2E：** Lead integration ran the minimum existing browser smoke: `E2E-KW-001 opens a blank main Knowledge shell` passed on `web-open` with one worker.
- **反向验证：** Persistence scan failed before the ACL-heal descriptor was registered and then passed after registration; retired migration/back-up paths remain absent and must stay absent.
- **外部声明：** 无；all results were locally observed.

## 5. 双轴审查

### 标准轴

- **结果：** pass after one local registry correction.
- **来源：** `<Path>AGENTS.md</Path>`、`<Path>package.json</Path>`、persistence receipt scripts, contract tests, and current module boundaries.
- **Findings：** The initial registry path mismatch and reachable-export omission were repaired; no unresolved correctness, custody, generated-drift, or test-quality finding remains.
- **修正与重跑：** Core persistence regression, receipt generators, boundary lint, typecheck, and client/server builds passed after the corrections.

### 规范轴

- **结果：** pass pending Lead candidate/integration acceptance.
- **来源：** T-05, AC-001--AC-003, ADR-001, Goal Plan W1.4/G1, and T-04 Evidence.
- **Findings：** Normal frozen upstream changes are retained; T-04's no-retired-migration/no-legacy-compatibility boundary remains intact; no T-06+ owner convergence/cutover was folded into this checkpoint.
- **修正与重跑：** None required outside the local implementation corrections described above.

两轴按标准轴、规范轴顺序独立完成，未将 finding 混排。

## 6. 路径所有权审计

- **writable 内修改：** All project changes are inside T-05's release-increment `writable_paths=<Path>**</Path>` scope.
- **shared 修改与 owner 批准：** 无；W1 is strictly serial.
- **read-only 修改：** 无。
- **未声明路径：** 无。
- **生成文件或锁文件：** Receipts, release digests, lock changes, and export manifest derive from the final merged source and reachability checks.

## 7. 偏差与决策

- **偏差：** Closed local implementation corrections only: ACL-heal persistence descriptor, provider-plugin registry path, and export closure entry.
- **偏差记录：** This Evidence; no Spec, ADR, Ticket, or dependency contract changed.
- **批准人或决策来源：** T-05 locked decisions, T-04 Evidence, and final-source generator/test facts.
- **对范围、契约、依赖或后续 Ticket 的影响：** T-06 starts only from the Lead-accepted integration SHA; the no-duplicate-owner constraint remains unchanged.

## 8. 残余风险与后续

- **残余风险：** Full-suite saturation remains an environment limitation; the affected integration regression, typecheck, boundary lint, client build, and browser smoke are green, but no single-worker full-suite claim is made.
- **已知限制：** Windows/macOS native/package validation is intentionally deferred to T-22/T-23.
- **后续 Ticket：** T-06 after Lead acceptance.
- **监控或回滚触发条件：** Target ancestry, generated receipt drift, reintroduced legacy migration/backups, duplicate production owner, or affected regression failure stops at T-05.

## 9. 交付定位

- **Commit / PR：** candidate `96d252e6db51a50db3255cc5a2f7bfdf516e0a67`; accepted local integration merge `5aa3318bd83d75fdec70706f7a9f30ade8310868`; no PR/push.
- **最终 Workspace 引用：** `specdev-worktree/T-05`.
- **Evidence 文件：** `<Path>{roots.state}/specdev/changes/{change}/evidence/T-05.md</Path>`.

## 10. 委派交付附录

- **Execution model / Provider：** `native-subagent` / Codex.
- **Session/Package locator：** `specdev-worktree/T-05` / `n/a-local-git`.
- **Dispatch / Final checkpoint：** `c20605b377b8c0cc72beb2c57cedf643d8d5908b` / `5aa3318bd83d75fdec70706f7a9f30ade8310868`.
- **Correction rounds：** 1 of 3.
- **Candidate delivery verification：** Lead independently checked scope, target ancestry, receipt closure, focused regression, export reachability, and the no-legacy/no-duplicate-owner boundary.
- **Lead E2E：** pass: one-worker `web-open` `E2E-KW-001 opens a blank main Knowledge shell`.
- **Unverified claims：** no single-worker full suite; no Windows/macOS package gate.

## 11. Lead Acceptance

- **Candidate and integration checkpoint:** Lead independently inspected candidate `96d252e6db51a50db3255cc5a2f7bfdf516e0a67`, its parents `c20605b377b8c0cc72beb2c57cedf643d8d5908b` and `1558220440fefcb89482bea61e71789d02c34df1`, and integration merge `5aa3318bd83d75fdec70706f7a9f30ade8310868`. `v0.441.32` is an integration ancestor; there are no unmerged index entries or merge markers.
- **Lead standard-axis result:** pass. The one-worker affected regression replay passed 20 files / 336 tests. It covered persistence ownership/receipts, credential custody, ACL/GPU recovery, plugins, Resource/session paths, server routes, and Windows contracts.
- **Lead specification-axis result:** pass. AC-001--AC-003, ADR-001, and W1.4/G1 remain satisfied: only frozen `v0.441.32` work was accepted; T-04's retirement of migrations/backups and absence of temporary duplicate owner paths remain intact; no T-06+ convergence entered.
- **Lead independent quality checks:** Node `24.16.0` typecheck, `npm run lint:boundary`, and `npm run build:client` passed. The minimum browser E2E, `E2E-KW-001 opens a blank main Knowledge shell`, passed on `web-open` with one worker.
- **Residual verification boundary:** `npm run lint` remains limited by the known unchanged Speculo tooling baseline; a saturated full suite is not claimed as green. Windows/macOS package Gates remain later designated work.
- **External actions:** no push, PR, deployment, release, archive, remote write, migration, or production action occurred. T-05 is accepted as `done`; T-06 may start only from `5aa3318bd83d75fdec70706f7a9f30ade8310868`.

## 12. Worktree Cleanup

- After the integrated checkpoint was retained on the local integration line, Lead verified the clean registered Git worktree and candidate ancestry, then non-force removed `specdev-worktree/T-05` and its local candidate branch. The lifecycle record is `removed`; no remote action occurred.
