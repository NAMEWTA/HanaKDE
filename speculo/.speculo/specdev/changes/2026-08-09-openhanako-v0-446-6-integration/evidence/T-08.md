# Evidence: T-08 - Integrate v0.443.54 through v0.444.1

- **Change:** `2026-08-09-openhanako-v0-446-6-integration`
- **Ticket:** `<Path>{roots.state}/specdev/changes/{change}/ticket/08-integrate-v0-443-54-through-v0-444-1.md</Path>`
- **Spec / ADR / Plan:** `<Path>{roots.state}/specdev/changes/{change}/spec.md</Path>` / `<Path>{roots.state}/specdev/changes/{change}/ADR.md</Path>` / `<Path>{roots.state}/specdev/changes/{change}/goal-plan.md</Path>`
- **Base / target:** `f6f3658985ab1beec116b7cf20c73b38a4ce2e3d` / `v0.443.54^{commit}=4cfdb13fba5356d6d0e03e89b64991de426e9ba8`; `v0.444.1^{commit}=cc19cb49b0786d61ed723764e0a83baf87887270` has not started.
- **Worktree / branch:** `specdev-worktree/T-08` / `speculo/2026-08-09-openhanako-v0-446-6-integration/T-08`
- **Execution model:** native-subagent / Codex; correction round 1 of 3.
- **Status:** `in_progress`; this is the independently green first subcheckpoint record and its local merge commit follows this Evidence update.

## 1. Implementation Summary

`v0.443.54` was merged as the first, independently auditable release increment. Its workspace mention restoration, visible Bridge paragraph handling, proactive failure classification, session-list visual fix, release metadata, and normal build/package updates are retained. The second target has not been merged or inspected as a candidate stage.

The three merge conflicts were resolved from intent rather than by a blanket side choice: `release-digest.v1.json` advances to the upstream current-release digest, while the persistence and CLI closure receipts were regenerated under Node `24.16.0` from the resolved source. The regenerated closure contains `9,663` files (`683` source graph, `11` runtime assets, `8,969` NFT traces), with zero open-boundary coupling edges.

## 2. 修改范围

| 路径 | 所有权 | 改动目的 |
|---|---|---|
| `<Path>core/bridge-session-manager.ts</Path>`, `<Path>core/desktop-session-submit.ts</Path>`, `<Path>core/slash-commands/rc-router.ts</Path>`, `<Path>lib/bridge/**</Path>`, `<Path>lib/notifications/notification-service.ts</Path>` | T-08 writable | 保留上游 Bridge visible-text 和主动投递失败分类修复。 |
| `<Path>desktop/src/react/components/**</Path>`, corresponding tests/locales | T-08 writable | 保留 `@` mention 异步搜索恢复和 session-list 状态提示修复。 |
| `<Path>package.json</Path>`, `<Path>package-lock.json</Path>`, `<Path>export-manifest.json</Path>`, release digests | T-08 writable | 同步 v0.443.54 package/export/current and rolling release metadata。 |
| `<Path>build/cli-runtime-closure.json</Path>`, `<Path>build/persistence-schema-fingerprint.json</Path>` | generated / force-staged | 从 resolved source 重建 deterministic receipts；不接受冲突任一侧的静态副本。 |
| `<Path>core/session-manifest/legacy-migration.ts</Path>`, `<Path>tests/session-manifest-legacy-migration.test.ts</Path>` | explicit exclusion | `677d8a6d` 的 BOM/old-ledger retry 和两个新增用例已恢复为派发基线，不进入此 checkpoint。 |
| `<Path>speculo/.speculo/specdev/changes/{change}/...</Path>` | T-08 metadata/Evidence | 记录 in-progress 状态、worktree lifecycle 与此子 checkpoint 证据。 |

## 3. 验收与合同映射

| Contract / Acceptance ID | 验证接缝 | 证据 | 结果 |
|---|---|---|---|
| AC-001 | first-stage target ancestry and merge topology | `v0.443.46` is an ancestor of `v0.443.54`; target/tag SHA was fixed before merge; local merge commit is created only after this record | v0.443.54 subcheckpoint pass; final target pending |
| AC-002 | upstream behavior and release-digest regression | 9 focused upstream files / 134 tests plus merged diff audit | v0.443.54 subcheckpoint pass; final target pending |
| AC-003 | HanaKDE Resource/Knowledge/History/Extraction ownership contracts | 8 contract files / 72 tests, plus 18-file rerun below | v0.443.54 subcheckpoint pass; final target pending |

## 4. 验证执行

| Command or step | Environment | Result | Summary |
|---|---|---|---|
| `volta run --node 24.16.0 npm ci` | Node 24.16.0 / npm 11.13.0 | pass | Lockfile-clean install completed; no lockfile drift. |
| `volta run --node 24.16.0 node scripts/compute-cli-closure.mjs` | Node 24.16.0 | pass | Regenerated `9,663`-file closure and zero-edge open baseline. |
| `volta run --node 24.16.0 node scripts/generate-persistence-schema-fingerprint.mjs ...` | Node 24.16.0 | pass | Exact payload `sha256:e9a75a22215d96936f267cc509e02cb44854f1acac890c789db7fa748a5856da`; only the existing session-sidecars bridge reader source hash changed. |
| 9-file upstream behavior/release command | Node 24.16.0 / Vitest | pass | 134 tests: Bridge, notification, mention, list cue, release digest, and pre-existing session-manifest coverage. |
| 8-file HanaKDE contract command | Node 24.16.0 / Vitest | pass | 72 tests: Resource, Root Identity, Knowledge, History and Extraction production boundaries. |
| 18-file corrected union command | Node 24.16.0 / Vitest | pass | 222 tests; includes reverted legacy suite (20 tests) and persistence tripwire (18 tests). |
| `volta run --node 24.16.0 npx vitest run tests/cli-closure-census.test.ts tests/verify-seed-kit.test.ts --maxWorkers=1` | Node 24.16.0 / Vitest | pass | 2 files / 28 tests; exact receipt regeneration and seed-kit validation. |
| JSON, conflict-marker, whitespace, ancestry checks | Git / Node 24.16.0 | pass before commit | All resolved JSON parses; no unmerged paths or conflict markers; final commit topology check remains immediately after commit. |

- **Reverse validation:** `git diff --cached --exit-code -- core/session-manifest/legacy-migration.ts tests/session-manifest-legacy-migration.test.ts` and the matching working-tree diff both exit `0`; a marker scan finds no `PARSE_ERROR_VERDICT_SCHEMA`, `readJsonFileUtf8`, `stripBom`, or `verdictSchema` in those paths.
- **Unrun boundary:** signed server build is intentionally not run. `HANA_SIGN_KEY` is unset and no key is created or used. Full suite, production E2E, Windows/macOS Gate, and remote actions remain outside this subcheckpoint.

## 5. 双轴审查

### Standards Axis

- **Result:** pass for the v0.443.54 subcheckpoint.
- **Sources:** resolved staged diff, upstream target history, package/build scripts, focused regressions, receipt generators, and Git conflict state.
- **Findings:** no remaining source-level issue. The new visible-text module is registered in `export-manifest.json`; proactive failure codes are tested; generated receipts are reproducible after a clean lockfile install.
- **Rerun:** all affected behavior, contract, persistence, closure, seed-kit, JSON, marker, and whitespace gates are green before commit.

### Specification Axis

- **Result:** pass for the v0.443.54 subcheckpoint, with explicit legacy exclusion.
- **Sources:** T-08, Spec AC-001--AC-003 and DEC-001/010/012/013, ADR-001/011, Goal Plan W1.7/G1, and T-07 Evidence.
- **Findings:** normal upstream behavior is retained without a second watcher/parser/write/baseline owner or a plugin change. The BOM/old-ledger retry is not compatible with DEC-010/OOS-011 and is intentionally absent.
- **Rerun:** predecessor equality and marker-negative checks accompany the green test matrix; v0.444.1 remains prohibited until this subcheckpoint is committed.

## 6. 路径所有权审计

- **writable 内修改:** all staged release, test, receipt, release metadata, state, and Evidence paths are under T-08 `<Path>**</Path>` during serialized W1.7.
- **shared 修改与 owner 批准:** none.
- **read-only 修改:** none.
- **未声明路径:** none.
- **生成文件或锁文件:** `build/persistence-schema-fingerprint.json` and `build/cli-runtime-closure.json` are force-staged only after their generators and deterministic tests pass; the lockfile changes only the release version.

## 7. 偏差与决策

- **Deviation:** local integration resolution, approved by the Lead correction during W1.7; it does not change Spec/ADR/Goal Plan authority.
- **Decision:** exclude upstream commit `677d8a6d`'s BOM parser and legacy parse-error ledger retry, restore both code and tests to the T-07/dispatch predecessor, and do not create a migration marker, ledger retry, old Profile import, schema migration, or migration rollback.
- **Existing legacy surface:** deliberately not broadened or deleted here. Lead identified `core/session-manifest/legacy-migration.ts`, `core/session-manifest/startup-migration.ts`, `core/engine.ts` (`ensureLegacySessionManifestMigration`, `_runSessionManifestStartupMigration`, `listSkippedMetaSources`), `core/session-manifest/checkpoint.ts`, `scripts/session-manifest-audit.mjs`, `scripts/session-manifest-rollback.mjs`, persistence registry entries, closure/export references, and `lib/sandbox/win32-legacy-migration.ts` / `Win32LegacySandboxCleanupQueue`.
- **Owner / impact:** T-09 is the sole owner for full removal before G2. T-08 neither adds a new legacy mechanism nor deletes the existing owner surface. This is recorded for Lead replay, not deferred as a compatibility exception.

## 8. 残余风险与后续

- **Residual risk:** the existing legacy surface remains until T-09 removes it; this first checkpoint is not a claim that the final G1 legacy scan is zero.
- **Known limit:** v0.444.1, final type/lint/build matrix, full suite, signed build, Windows/macOS Gates, production E2E, and all remote actions are unverified at this point.
- **Next checkpoint:** commit this v0.443.54 subcheckpoint, recheck its parents and clean state, then and only then start `v0.444.1`.
- **Stop conditions:** any ancestry mismatch, receipt drift, reintroduced BOM/ledger retry marker, second production owner, or affected regression failure blocks the second stage.

## 9. First Subcheckpoint Record

- **Target:** `v0.443.54^{commit}=4cfdb13fba5356d6d0e03e89b64991de426e9ba8`.
- **Conflict ledger:** generated CLI closure and persistence fingerprint were regenerated from the resolved source; `release-digest.v1.json` takes the upstream v0.443.54 current-release digest while `release-digest.v2.json` retains rolling history.
- **Legacy verdict:** the only v0.443.54 legacy enhancement was `677d8a6d`; it and its two tests are excluded by exact predecessor restoration. No v0.444.1 merge begins before the enclosing local subcheckpoint commit is green.
- **Ancestry before commit:** `v0.443.46` -> `v0.443.54` is confirmed; this branch still has dispatch base `f6f3658985ab1beec116b7cf20c73b38a4ce2e3d` as its first parent candidate.
- **Commit / final parent proof:** recorded immediately after the local merge commit and carried forward in this Evidence's final T-08 record.
