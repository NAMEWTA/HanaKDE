# Evidence T-08: Automation 运营界面

## 1. 结果摘要

T-08 已完成。Automation Page 从 `/api/automation/runs` 读取稳定 projection，展示 status/summary/diagnostic，并提供失败 retry、queued/running cancel、Session 引用复制；Projects Page 也覆盖编辑、Trash/restore。

## 2. 修改范围

`<Path>plugins/todolist/src/ui/page.tsx</Path>`、`routes/api.ts`、`tools/automation.ts`、插件 E2E fixture/config。

## 3. 验收与合同映射

- AC-020/021/022/029：UI 动作调用同一服务状态机；无宿主 session.open 时只保留稳定 ref，不展示假导航或 transcript。
- AC-031：导航、操作按钮、aria labels、错误 live region、长文本换行和五语言矩阵均在同一页面实现。

## 4. 验证执行

automation service integration、后台能力回归；插件套件 24/24；Playwright 10/10（每 locale/viewport 完成创建、编辑、trash/restore，页面加载 `page.css` 并可进入 automation/review 视图）；插件 lint 0 errors。

## 5. 双轴审查

行为轴：列表 -> retry/cancel -> reload projection 闭环。质量轴：使用 Hana plugin-components，窄布局稳定，按钮有明确可访问名称。

## 6. 路径所有权审计

所有 UI、route、fixture 和 Playwright 配置均在 `<Path>plugins/todolist/**</Path>`。

## 7. 偏差与决策

无偏差；不新增快捷键、批量操作或自然语言识别。

## 8. 残余风险与后续

宿主尚无 session.open 与原生 automation card；只提供稳定诊断 projection，待能力对外后再增强查看入口。
