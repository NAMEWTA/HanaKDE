# Evidence T-03: 捕获与组织

## 1. 结果摘要

T-03 已完成。快速捕获严格一次一项，拒绝换行/批量输入；Project 的删除不级联 Todo，失活 Project 的 Todo 投影至 Inbox 并保留 projectId 历史引用。

## 2. 修改范围

变更限定 `<Path>plugins/todolist/src/service.ts</Path>`、`src/types.ts`、`routes/api.ts`、`tools/capture.ts`、`tools/project.ts` 和 `src/ui/page.tsx`。

## 3. 验收与合同映射

- AC-006：`capture-organization.integration.test.ts` 验证一项捕获和多行拒绝；UI 只有一个 Todo 输入/提交动作，无批量创建和 NLP。
- AC-007/029/031：Project CRUD/trash/restore 不级联；E2E 证明五语言、键盘可聚焦和窄布局下的日常动作。

## 4. 验证执行

插件 Vitest 24/24；Playwright `ui.spec.ts` 10/10；`rg` 源码扫描未发现 batch create/NLP 实现，唯一 SQLite 字样为拒绝格式测试 fixture。

## 5. 双轴审查

行为轴：capture -> query -> project lifecycle 可观察闭环。质量轴：输入有 label、可读空态、长标题 `overflowWrap:anywhere`，没有依赖隐藏快捷键。

## 6. 路径所有权审计

全部生产/测试/fixture/config 文件位于 `<Path>plugins/todolist/**</Path>`。

## 7. 偏差与决策

遵循用户决定：不做批量创建、不做自然语言识别。

## 8. 残余风险与后续

复杂看板/嵌套项目/协作不在当前 Hana capability 与本 change 范围。
