# Evidence T-02: 安全删除生命周期

## 1. 结果摘要

T-02 已完成。删除默认进入 Trash，恢复保持 id 并递增 version；永久清理需要 session/version/expiry 绑定的一次性 confirmation。

## 2. 修改范围

实现位于 `<Path>plugins/todolist/src/service.ts</Path>`、`src/store.ts`、`routes/api.ts`、`tools/delete*.ts`、`tools/restore.ts` 和插件内测试/UI。

## 3. 验收与合同映射

- AC-013：`todo-lifecycle.integration.test.ts` 验证 soft delete、Trash 查询、restore 和身份保持；Playwright 验证 Move to Trash -> Trash -> Restore -> Inbox。
- AC-014/029：session 错误、one-shot replay conflict、commit fault rollback 均通过；无 Page/tool 无确认永久删除入口。

## 4. 验证执行

`npx vitest run plugins/todolist/tests ...` -> 24/24；隔离 Playwright -> 5 locales x desktop/narrow = 10/10；删除事务测试注入提交失败后 Trash 数量仍为 2，后台取消回归确认宿主 unschedule/cancel 请求。

## 5. 双轴审查

行为轴：软删除、恢复、永久清理状态机和回滚闭环成立。质量轴：危险动作有稳定错误、可诊断状态和无 secret 详情；UI 有 `aria-label` 与可恢复操作。

## 6. 路径所有权审计

产品写路径仅为 `<Path>plugins/todolist/**</Path>`；没有修改宿主删除接口。

## 7. 偏差与决策

无偏差；真实永久删除未作用于用户数据库，验证只使用临时 store。

## 8. 残余风险与后续

真实用户确认流程和发布动作仍由宿主发布 Gate 管理，本票不执行不可逆外部动作。
