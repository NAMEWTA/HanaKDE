# Evidence T-07: 隔离 Agent Run

## 1. 结果摘要

T-07 已完成插件侧 Run/Attempt 状态协议。稳定 key 为 Todo×occurrence，每次 retry 追加 Attempt；cancel 经过 `cancel_requested`，不会乐观标 cancelled，也不会自动完成 Todo。具备配置时通过宿主 `session:create`/`session:send`，缺少能力则进入 `needs_action`。

## 2. 修改范围

`<Path>plugins/todolist/src/service.ts</Path>`、`src/types.ts`、`routes/api.ts`、`tools/automation.ts` 和插件测试。

## 3. 验收与合同映射

- AC-017～019：`automation-run.integration.test.ts` 和 `background-capabilities.integration.test.ts` 验证唯一 Run、attemptNumber=2、最小 summary/diagnostic 与 Session 接缝。
- AC-020：状态按 queued/running/cancel_requested/cancelled 合同推进。
- AC-022/026/029/033：store 只持久化 sessionRef/summary，不复制 transcript、secret、absolute path；tool catalog 仍 namespaced。

## 4. 验证执行

automation integration 与后台能力回归通过；全套插件 24/24；双 tsconfig typecheck exit 0；源码扫描确认没有 transcript/absolute workspace persistence。

## 5. 双轴审查

行为轴：run/retry/cancel 可恢复且不跨 Todo 共享。质量轴：失败有诊断，UI 只呈现最小结果和稳定 ref。

## 6. 路径所有权审计

实现、测试、tool 均限于 `<Path>plugins/todolist/**</Path>`，宿主 Agent/Session 未被修改。

## 7. 偏差与决策

无偏差；当前 Hana 没有 `session.open`，不私造导航或完整 transcript viewer。

## 8. 残余风险与后续

真实 Agent executor/Session 取消确认需宿主 capability；当前仅验证应用状态机和 fail-closed 接缝。
