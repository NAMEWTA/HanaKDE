# Evidence T-01: builtin 持久 CRUD

## 1. 结果摘要

T-01 已完成。`plugins/todolist/manifest.json`、`src/store.ts`、`src/service.ts`、routes、tools 和 React Page 组成同一 application/store 闭环；生产源和测试均为 TypeScript/TSX。

## 2. 修改范围

产品变更仅位于 `<Path>plugins/todolist/**</Path>`。私有 JSON store v1 使用事务、临时文件 rename、版本号和故障注入；PluginManager 只暴露 `todolist_*` namespaced tools 与 `/page`。

## 3. 验收与合同映射

- AC-001/002/004：PluginManager discovery、route/tool smoke、`plugin-crud.integration.test.ts` 的同源创建、重开读取、bounded query、version conflict。
- AC-003/033：`plugin-manager.integration.test.ts` 验证 `todo_write` 不受影响且普通用户只看到 namespaced tools。
- AC-029：validation、conflict、transaction_failed 和脱敏 error 测试通过。

## 4. 验证执行

`npx vitest run plugins/todolist/tests --exclude 'plugins/todolist/tests/e2e/**' --exclude '**/dist-server/**' --exclude '**/dist-*/**' --reporter=dot` -> 10 files / 20 tests passed；`npm run typecheck` -> exit 0；`node --experimental-strip-types -e "await import('./plugins/todolist/routes/page.ts'); await import('./plugins/todolist/routes/api.ts')"` -> route modules ok；`node plugins/todolist/build.ts` -> exit 0。

## 5. 双轴审查

行为轴：Page/tool/store 读写同源，版本冲突和提交失败不产生部分写入。质量轴：TypeScript 编译通过；插件 lint 0 errors（69 warnings，主要是既有 SDK context `any` 与空 catch）。

## 6. 路径所有权审计

`git status --short -- plugins/todolist` 仅显示该目录为本 change 产品根；宿主和其它插件未被实现修改。Creator preflight 与 baseline ancestor 检查均通过。

## 7. 偏差与决策

无未批准偏差。Python 仅用于 creator preflight，未进入生产源、测试或运行依赖。

## 8. 残余风险与后续

真实宿主通知 receipt/Bridge 不在当前 Hana 能力中；本票只交付 CRUD，后台副作用由 T-06 按 fail-closed 边界处理。
