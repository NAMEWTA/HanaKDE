# Evidence T-09: JSON 交换与 Review

## 1. 结果摘要

T-09 已完成。`hana-todolist` JSON schema v1 支持 preview 零写、digest/one-shot commit、显式 export 和 Markdown Review；SQLite/unknown/sensitive 输入 fail closed。

## 2. 修改范围

`<Path>plugins/todolist/src/exchange.ts</Path>`、`routes/api.ts`、`tools/exchange.ts`、`src/ui/page.tsx` 和 `import-export.integration.test.ts`。

## 3. 验收与合同映射

- AC-022/027：preview 不写 store；commit 导入后重复 commit 返回 already_committed，schema/version 严格校验。
- AC-028/030/029/031：export 默认排除 Trash、Review 不写 workspace；结果不含 secret/transcript/workspacePath，错误稳定。

## 4. 验证执行

`import-export.integration.test.ts` 2/2；插件套件 24/24；导入/导出不触发 reminder/run；`npm run typecheck` exit 0。

## 5. 双轴审查

行为轴：preview -> commit -> duplicate rejection 与 export -> review 闭环。质量轴：下载由显式按钮触发，内容最小化并通过敏感字段断言。

## 6. 路径所有权审计

交换实现与测试均在 `<Path>plugins/todolist/**</Path>`，没有触碰旧 SQLite 或宿主迁移。

## 7. 偏差与决策

无偏差；没有真实脱敏 0.0.5 样本，因此未声明旧数据库兼容，仅明确拒绝。

## 8. 残余风险与后续

大文件导入/导出仍受当前插件 bounded JSON 约束，后续可在 Hana 提供流式能力后扩展。
