# Evidence: T-00 — 移除已拒绝的内置工作台插件

- **Change：** `2026-08-28-knowledge-explorer-convergence`
- **Ticket：** `<Path>{roots.state}/specdev/changes/2026-08-28-knowledge-explorer-convergence/ticket/00-retire-rejected-workbenches.md</Path>`
- **Lead：** `root`
- **Workspace/branch：** `current` / `hanakde`
- **状态：** 实现、验证与本地集成完成；实现提交 `b0c74282`。

## 实现摘要

删除 `plugins/finance-workbench/**` 与 `plugins/markdown-wechat/**`，清理它们的构建排除、持久化登记和唯一消费者 `plugin.page.open`。用户 `HANA_HOME` 数据、Todo、其余 builtin 和内部 artifact 签名均保留。

## 验证

| 验证 | 结果 |
|---|---|
| bundled PluginManager inventory | pass；两个 id 不存在 |
| plugin/protocol/SDK/persistence focused suites | pass；239 tests |
| `rg` live runtime audit | pass；仅保留否定断言与历史记录 |
| full Vitest | pass；1204 files，12259 tests |
| typecheck / ESLint errors | pass / 0 errors |
| client + temporary-key signed server build | pass |

## Integration

实现已提交为 `b0c74282`，并获用户授权进入 `v0.0.9` release。用户已有未跟踪 pnpm 文件未修改。
