# Evidence: T-05 - 资源收敛跨层回归与用户流程 Gate

- **Change：** `2026-08-12-knowledge-workspace-resource-convergence`
- **Ticket：** `<Path>{roots.state}/specdev/changes/{change}/ticket/05-run-resource-convergence-integration-gate.md</Path>`
- **Baseline / branch：** `af658cbe3a998f5631851a7fba8dc7485a53032c` / `hanakde`
- **Implementer：** current-implementer
- **Status：** verified

## 1. 实现摘要

新增真实 route/临时磁盘 integration、资源树 open/icon integration 和 Web Open Playwright 用户流程。Gate 从公共 ResourceIO 与 Knowledge routes 出发验证 expected-version save、create、Trash commit 和磁盘事实；从 UI 验证创建生命周期、md/pdf/jpg/html 打开、右键动作、clipboard 边界，以及 Web/remote 对 native/absolute-path 动作的隐藏。该 Gate 汇合 T-01—T-04，不引入生产实现旁路。

## 2. 修改范围

- `<Path>tests/knowledge-workspace-resource-convergence.test.ts</Path>`：公共 composition + 临时磁盘 integration。
- `<Path>desktop/src/react/__tests__/components/KnowledgeResourceTree.integration.test.tsx</Path>`：共享 icon/open seam 的 md/pdf/jpg/html 覆盖。
- `<Path>tests/knowledge-workspace-e2e/specs/resource-convergence.spec.ts</Path>`：Web Open 直接用户流程。
- 本 Ticket 只新增测试，生产修改由 T-01—T-04 各自拥有。

## 3. 验收与合同映射

| Contract | Evidence | Result |
|---|---|---|
| AC-001 | 公共 save/create/Trash route 共享活动目录 owner，磁盘事实可读 | pass |
| AC-002 | matching/stale expected-version integration | pass |
| AC-003 | lifecycle/generation focused tests纳入最终 matrix | pass |
| AC-004 | dialog 单次提交、成功关闭和 E2E 创建 | pass |
| AC-005 | dialog 失败保留/显式重试 component test | pass |
| AC-006 | Web 用户流程打开右键菜单并看到适用资源动作 | pass |
| AC-007 | md/pdf/jpg/html 共享 icon/open component + Playwright | pass |
| AC-008 | Web Open 不显示 absolute/Finder/default-app native actions | pass |
| AC-009 | same-source cut/move route + clipboard tests | pass |
| AC-010 | cross-source cut 拒绝、copy 源/链接不变 | pass |
| AC-011 | stale owner/workspace clipboard/event fail closed | pass；native ABI 项独立分类 |
| AC-013 | icon/tooltip/ARIA/menu roles 和 UI 稳定性断言 | pass |

## 4. 验证执行

| Command / step | Result |
|---|---|
| final focused Vitest command over backend routes/lifecycle/clipboard/integration/native and React dialog/tree/workspace/native client/clipboard/file-kind/preview suites | pass: 20 files / 214 tests |
| `npx playwright test -c tests/knowledge-workspace-e2e/playwright.config.ts --project=web-open tests/knowledge-workspace-e2e/specs/resource-convergence.spec.ts --workers=1 --reporter=list` | pass: 1/1 |
| `npm run typecheck` | pass: three TypeScript projects |
| focused ESLint `--quiet` over changed production/tests | pass: 0 errors |
| `npm run build:renderer` | pass；仅既有 Vite script/chunk warnings |
| change-scoped tracked + untracked `git diff --check` | pass |
| `npm test -- --exclude 'specdev-worktree/**'` | non-zero: 1,159 files / 12,012 tests pass；46 files / 258 tests fail；8 skipped；分类见下文 |

全量 suite 的失败集中在当前 Node 22 不满足 package 要求 `>=24.12 <25`、`better-sqlite3` ABI 127/137 不匹配，以及与本 change 无关的既有 snapshot/platform/closure/FileHistory 失败。同一全量运行中的 change tests 通过，随后最终 20 files / 214 tests、typecheck、lint、build 与 Playwright 全绿。未把全量 non-zero 改写为 pass，也未重建共享原生工件。

## 5. 双轴审查

### Standards axis

通过。审查基于仓库 AGENTS/CONTRIBUTING、ESLint/TypeScript/Vitest 约定和既有共享模块；发现并修复了误导性的 action icons，修正后完整重跑 focused、typecheck、lint 和 build。测试均通过公共 route/client/component seam，不依赖测试专用生产旁路。

### Specification axis

通过。逐项对照 Spec、ADR、Ticket、Goal Plan：单一 owner、source-relative、安全 grant、同源 cut/跨源 copy、dialog 不重入、Web/remote 降级和 `main` 可见命名均成立。复核中还移除了 source root 的 native action 入口，没有扩大到所有 mounted resources。

## 6. 路径所有权审计

T-05 新增的三个路径与 frontmatter `writable_paths` 完全一致。Gate 只读取 T-01—T-04 生产路径；未修订实现以迎合测试，也未触碰 package/lockfile、CI、发布配置或真实用户工作目录。

## 7. 偏差与决策

- 无验收合同偏差。全量 suite 的环境/基线失败按项目 completion 规则保留分类，不作为本 change 新失败，也不被隐藏。
- 当前工作树含用户与已归档 integration 的其他改动；change-focused path audit 与测试采用明确路径，不还原、不覆盖这些改动。
- 用户未授权 commit，因此没有 immutable candidate SHA；baseline、branch、命令结果和未提交路径集合共同构成交付定位。

## 8. 残余风险与后续

要获得仓库全量绿色，需在项目声明的 Node 24.12—24.x 环境中使用匹配 ABI 的 native dependencies，并由相应 baseline owner 处理无关 snapshot/platform/closure/FileHistory 失败。该工作会替换共享运行时工件，超出本 change，也不是 Knowledge 收敛合同的豁免条件。

## 9. 交付定位

- **Implementation：** 当前 `hanakde` 未提交工作树，baseline `af658cbe3a998f5631851a7fba8dc7485a53032c`。
- **Evidence：** `<Path>{roots.state}/specdev/changes/{change}/evidence/T-05.md</Path>`。

