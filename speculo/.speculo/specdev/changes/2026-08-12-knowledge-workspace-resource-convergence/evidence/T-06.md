# Evidence: T-06 - upstream checkpoint 遍历与兼容收口

- **Change：** `2026-08-12-knowledge-workspace-resource-convergence`
- **Ticket：** `<Path>{roots.state}/specdev/changes/{change}/ticket/06-traverse-upstream-resource-convergence-checkpoints.md</Path>`
- **Baseline / branch：** `af658cbe3a998f5631851a7fba8dc7485a53032c` / `hanakde`
- **Implementer：** current-implementer
- **Status：** verified；未执行 upstream merge/rebase/cherry-pick/publish

## 1. 实现摘要

在现有 `<Path>docs/upstream-sync-ledger.md</Path>` 追加本 change 的独立 checkpoint：冻结 v0.446.6 upstream target、ledger baseline 和 local source SHA，记录 ancestry、150-path census、当前 overlay 与 5 条精确 overlap，并用既有五路词汇逐项分类。ledger 同时记录 owner/security scan、受影响测试、Node ABI 环境限制和 change-scoped diff receipt，为后续 fork 遍历升级提供 stop condition。

## 2. 修改范围

- 仅修改 `<Path>docs/upstream-sync-ledger.md</Path>`。
- 读取 Git ancestry、当前生产/test paths、T-01—T-05 Evidence 和 owner/security seams。
- 未执行 upstream 合并、冲突解析、提交、推送、发布或生成物回写。

## 3. 验收与合同映射

| Contract | Evidence | Result |
|---|---|---|
| AC-012 | ledger 冻结 `c45d1e544f8f2611f92a459947b6a49e9b91239d`、`de0eb983c5fe237e1349b82a927050c524129b8f`、local `af658cbe3a998f5631851a7fba8dc7485a53032c`；记录 ancestry、overlap、五路分类、scan/test 和 stop rule | pass |

## 4. 验证执行

| Command / step | Result |
|---|---|
| `git merge-base --is-ancestor` for frozen upstream target and ledger baseline against local source checkpoint | pass: both are ancestors |
| path census from `c45d1e54..af658cbe` | 150 relevant paths |
| overlay overlap scan | 5 exact overlaps：Engine、desktop Main、ResourceIO route 与两个既有 tests |
| five-way classification review | pass：upstream accepted / HanaKDE kept / semantic integration / generated / deleted duplicate 均有具体决定 |
| owner/security scan | pass：route-local ResourceIO owner 删除；Native `copyPath` 不回传绝对路径；Web action 缺席 |
| affected contract receipts | pass：20 files / 214 tests、Playwright 1/1、typecheck、focused lint、renderer build |
| change-scoped diff check | pass |

## 5. 双轴审查

### Standards axis

通过。ledger 沿用现有 checkpoint 和五路分类格式，使用完整 SHA 与实际命令结果；环境失败和未授权动作明确记录，没有把“Git 无冲突”当作语义兼容证明。

### Specification axis

通过。五条 overlap 均保留既有上游能力并用最小 adapter 收敛 HanaKDE owner/security 合同；没有第二 watcher、parser、route kernel、provider 或 Renderer raw-path API。未来若这些事实发生变化，ledger 明确要求停在对应 checkpoint 并返回 Spec/Grill。

## 6. 路径所有权审计

T-06 唯一项目写路径是 `<Path>docs/upstream-sync-ledger.md</Path>`，与 Ticket expected/writable/shared owner 一致。Tickets Map、Evidence 和 status 属 SpecDev 当前 Work 的治理同步，不被伪装为 T-06 项目写路径。

## 7. 偏差与决策

- 没有 upstream merge，因此 local source checkpoint 是本 change 开始时的 Git SHA，加未提交 overlay；ledger 明确区分二者。
- overlay path 数量包含当前共享工作树中的相关路径，语义判断以精确 overlap 与 owner contract 为准，不声称整个脏工作树都属于本 change。
- 无批准偏差；兼容策略仍是最小 seam 和 checkpoint-by-checkpoint 审计。

## 8. 残余风险与后续

下一次真实 upstream 更新必须重新冻结 source SHA、重新计算 overlap、复跑 owner/security scan 和受影响合同测试；本 Evidence 不能被复用于未来 SHA。当前 Node/native ABI 限制按 T-01/T-05 分类，不能作为未来 checkpoint 的通行证。

## 9. 交付定位

- **Ledger：** `<Path>docs/upstream-sync-ledger.md</Path>` 中 `2026-08-12 Knowledge workspace resource convergence`。
- **Evidence：** `<Path>{roots.state}/specdev/changes/{change}/evidence/T-06.md</Path>`。
