# Evidence: T-25 - umbrella final acceptance (blocked)

- **Change:** `2026-08-12-openhanako-v0-446-6-platform-gates` (carried from original integration change)
- **Ticket:** `<Path>{roots.state}/specdev/changes/{change}/ticket/25-final-umbrella-acceptance.md</Path>`
- **Product fixed point:** `feb906d6` (T-26 merge)
- **Quality fixed point:** `8aa9f7b1` (post-T-26 deterministic receipts/tests)
- **Audit checkpoint / final HEAD:** `7198ffcb`; this is a docs-only descendant of packaged direct-flow implementation `99b6a993`, with no product-path delta after that runner commit
- **Owner:** Worker-T-25 / Root Lead
- **Status:** blocked; change remains active

## 1. 实现摘要

**Final verdict: BLOCKED.** The current local fixed point closes the T-15 Node
strip-only regression, T-20 incremental Office projection, T-16 Workbench
History host/E2E, T-18 physical Desk-search cancellation, the deterministic
T-23 production workspace gap/reconcile probe, the T-26 authoritative Agent
mutation producer, and the post-T-26 G10 repository quality failures. It does
not close T-22 or T-23: no real Windows runner has executed the blocking matrix,
and macOS still lacks x64 runtime/DMG, literal kernel descriptor accounting,
and physical sleep/wake with naturally lost events.

The arm64 `.app`/DMG was rebuilt for the packaged audit from product
`feb906d6` and quality `8aa9f7b1`. Exact-current package inventory, DMG checksum,
Office edit/re-extraction/restore, Workbench History diff/UI restore and T-26
Agent write/receipt/replay/reload/History all pass in an isolated home after
artifact-only ad-hoc re-sign. The committed T-23 direct-flow runner at
`99b6a993` additionally proves readonly DMG mount/copy install, health, absence
of an unsafe `--no-sandbox` argument, packaged `@` search/select/Escape, and
app/server/mount/sandbox cleanup. Production signing/notarization remains an
out-of-scope release concern under T-23's explicit local unsigned mode, not a
T-23 completion row.

T-26 now supplies T-17's required session, UUID operation and source-relative
resource envelope from exactly one successful ResourceIO mutation receipt; the
desktop and exact-current packaged Agent write/reload/History flows pass.
Missing platform Evidence is not converted into a pass, so the 28 ACs and 15
DoD items still cannot be accepted as an umbrella release.

## 2. 修改范围

T-25 itself changes only SpecDev Evidence/status projections. Product and
quality commits since the previous audit are owned corrections in T-23, T-18,
T-26 and G10, after the earlier T-15, T-20 and T-16 corrections. The
audit does not authorize product fixes, a push/PR, production signing, notarization,
publishing, migration, archive, or any write to external infrastructure.

`git diff dce4676b..8aa9f7b1` is intentionally not docs-only: it contains the
T-15/T-20/T-16 corrections, the T-23 product reconcile probe, the T-18
AbortSignal transport correction, T-26 producer chain, and bounded G10
lint/style/receipt/test fixes. The repository-wide quality commands below were
rerun at `8aa9f7b1`. The arm64 package was rebuilt from that product/quality tree
for the packaged audit at `99b6a993`; artifact-only ad-hoc re-sign is local validation,
not a production signature claim.

## 3. 验收与合同映射

### 3.1 Fixed point and dependencies

The frozen target `5f08a4f3`, T-21 `e1b232b3`, T-22 harness merge `e06a5230`,
T-23 adapter/harness `dce4676b`, T-24 docs `10a79be8`, T-15 correction merge
`9738a105`, T-20 correction merge `d482768f`, T-16 final product `2421bfed`,
T-23 reconcile probe `2b0ef66b`, T-18 cancellation correction `3b08e6ff`, G10
lint correction `9c3a507b`, G10 receipt/style correction `46a83517`, T-26 merge
`feb906d6`, T-16 current owner-flow test `6687c150`, and post-T-26 quality
correction `8aa9f7b1` and packaged direct-flow implementation `99b6a993` are
ancestors of the audit checkpoint. T-22 remains
`blocked`, T-23 remains `review`, and T-25 remains `blocked`.

### 3.2 AC-001..AC-028 verdict

`covered` means current focused/local Evidence exists. It does not override a
blocking platform or final-package row.

| AC | Verdict | Basis |
|---|---|---|
| AC-001 | covered | frozen target and integration ancestry verified |
| AC-002 | covered | upstream integration and T-21 build-input Evidence |
| AC-003 | covered | current T-20 122-test and Office E2E results |
| AC-004 | covered | main lifecycle/watcher focused Evidence |
| AC-005 | covered | main-only History service and boundary tests |
| AC-006 | covered | History timeline/deleted/diff plus owner E2E |
| AC-007 | covered | deterministic policy/store tests |
| AC-008 | covered | T-26 derives the strict producer envelope from exactly one successful ResourceIO write/edit receipt; desktop Agent E2E passes |
| AC-009 | blocked | no real Windows watcher Gate |
| AC-010 | blocked | no real Windows cutover Gate |
| AC-011 | covered | plugin/EventBus adapter and 140-test matrix |
| AC-012 | blocked | deterministic product gap/reconcile passes on arm64; physical sleep/event-loss and Windows matrix are absent |
| AC-013 | blocked | deterministic product health transition passes; final physical/cross-platform matrix is incomplete |
| AC-014 | blocked | macOS security fixture passes; Windows junction/root runtime absent |
| AC-015 | blocked | local restore passes; final cross-platform/package Gate incomplete |
| AC-016 | blocked | secure TOCTOU tests pass; Windows native runtime absent |
| AC-017 | covered | Office and History modify/restore convergence passes locally |
| AC-018 | blocked | exact-current arm64 packaged extraction flow passes; Windows/x64 package matrix is incomplete |
| AC-019 | blocked | extraction failure tests and exact-current arm64 package pass; cross-platform matrix is incomplete |
| AC-020 | blocked | materialize/root tests pass; Windows native Gate absent |
| AC-021 | covered | Office extraction/index/search/restore E2E passes in dev and exact-current arm64 packaged runtime |
| AC-022 | blocked | no-write/no-OCR and exact-current arm64 packaged flows pass; cross-platform package proof is incomplete |
| AC-023 | blocked | transfer/materialize tests exist; final platform proof incomplete |
| AC-024 | covered | T-18 physical cancellation, Workbench History and T-26 Agent write/reload/History flows pass in desktop and exact-current arm64 package without a second store/event owner |
| AC-025 | covered | new-store initialization Evidence |
| AC-026 | covered | route/schema/security scans and focused tests |
| AC-027 | blocked | exact-current arm64 `.app`/DMG/inventory/launch pass; macOS x64 and Windows NSIS are absent |
| AC-028 | blocked | architecture docs and producer closure pass; platform closure is incomplete |

### 3.3 Umbrella DoD

| DoD | Verdict | Reason |
|---|---|---|
| 1 | pass | frozen target is an ancestor of `8aa9f7b1` |
| 2 | pass | full repository test, lint, typecheck and client/server build Evidence is current |
| 3 | pass | Workspace History and strict T-26 Agent production-envelope direct flows pass |
| 4 | covered | extraction fixtures and stable-failure tests pass |
| 5 | covered | current dev and exact-current arm64 packaged Office/History direct flows pass |
| 6 | covered | Materialize/Transfer contracts pass focused tests |
| 7 | blocked | Windows native root/junction verification absent |
| 8 | blocked | macOS arm64 fixture only; Windows and physical sleep proof absent |
| 9 | covered | EventBus single-owner/adapter contract passes |
| 10 | covered | baseline reconciliation focused tests exist |
| 11 | blocked | final cross-platform convergence traces incomplete |
| 12 | blocked | T-22/T-23 blocking Gates are not both passed |
| 13 | blocked | exact-current arm64 `.app`/DMG passes; macOS x64 and Windows NSIS remain unverified |
| 14 | blocked | structural checks pass; platform-dependent closure does not |
| 15 | pass | architecture and upstream ledger are integrated |

## 4. 验证执行

Executed on required Node `24.16.0` at product fixed point `feb906d6`, quality
fixed point `8aa9f7b1`, or package/direct-flow implementation checkpoint
`99b6a993`; final Evidence/status audit is at `7198ffcb`:

| Command | Result |
|---|---|
| SpecDev implement validator | pass: 0 errors, 0 warnings |
| `git merge-base --is-ancestor <target/checkpoints> HEAD` | pass for frozen target and listed T-15/T-16/T-20/T-21/T-22/T-23/T-24/T-26/G10 checkpoints |
| Initial post-T-26 full test | expected Gate failure: 6 files / 16 tests exposed missing helper prerequisite, stale receipts/legacy event expectation and two async UI assertions; no pass claimed |
| D-G10-02 style/persistence matrix | pass: 3 files, 40 tests; no style baseline increase and deterministic receipts match |
| D-G10-03 failed-file matrix | pass: 6 files / 134 tests; CLI closure writes are independently idempotent, persistence fingerprint matches, native conditional writes and async UI states pass |
| full `npm test -- --maxWorkers=1 --reporter=dot --silent` with isolated `HANA_HOME` after `build:secure-fs-helper` | pass: 1,201 files passed, 1 skipped; 12,253 tests passed, 8 skipped; 381.55s |
| full `volta run --node 24.16.0 -- npm run lint -- --quiet` | pass: exit 0 |
| `volta run --node 24.16.0 -- npm run typecheck` | pass: all three TypeScript projects |
| `volta run --node 24.16.0 -- npm run build:client` at T-26 final SHA | pass: main, preload, renderer, splash and theme bundles built |
| sequential `volta run --node 24.16.0 -- npm run build:server` with ephemeral local key/keyset | pass: 1,225-module server bundle, CLI/document child assets, dependency pruning, better-sqlite3/jieba/AnyDoc smoke, signed Node startup smoke and signed seed packing |
| T-15 secure writer/helper/provider/runtime focused matrix | pass: 4 files, 44 tests |
| T-20 Office/Knowledge/extraction matrix | pass: 9 files, 122 tests |
| T-16 History UI/service/store/boundary matrix | pass: 8 files, 91 tests |
| T-26 producer/consumer/session/activity matrix | pass: 12 files, 411 tests; full typecheck, scoped lint and client build pass |
| T-18 cancellation focused matrix and `web-full` E2E | pass: 7 files, 68 tests; E2E 1/1; clear/unmount abort the exact signal forwarded to `hanaFetch` |
| T-23 macOS production reconcile runner and Vitest | pass on darwin/arm64: 3/3; single watcher lease, suppressed callback repaired by shared baseline, four-state health convergence |
| Office E2E, `desktop-full` + `web-open` | pass: 2/2; modify, automatic reindex, restore/search convergence, no derived file |
| History E2E, `desktop-full` | pass: 1/1 in 1.1m; visible Workbench entry, real 60s merge window, diff, expected-version restore, ResourceIO read-back |
| Agent file-change E2E, `desktop-full` | pass: 1/1 in 5.2s; production Engine tool write, strict receipt correlation, renderer reload/replay and shared History modal |
| current macOS/Windows harness contract Vitest matrix | pass: 9; skip: 1 real Windows-only row |
| plugin context/route/EventBus/manager/SDK matrix | pass: 5 files, 140 tests |
| scoped T-16/T-20 ESLint and `git diff --check` | pass |
| exact-current `npm run dist -- --arm64` with ephemeral key/keyset, local unsigned mode and Node 24.16.0 | pass: Computer Use/client/secure helper/current 1,225-module server, signed Node smoke, seed verification, `.app` and DMG |
| exact-current macOS gate with `--package dist/mac-arm64` | pass: arm64 filesystem/product reconcile/package inventory complete; six recursive events, single watcher lease and four-state health convergence |
| exact-current `hdiutil verify` | pass: DMG checksum VALID; SHA-256 `b4388d7ef508255abc360f5e5b8e086b54bda5b159b14da0a3d5b112ed247228` |
| artifact-only recursive ad-hoc re-sign plus `codesign --verify --deep --strict` | pass for local smoke only; no production signing/notarization claim |
| committed packaged arm64 direct-flow Gate at `99b6a993` | pass: full macOS fixture + production workspace + real DMG; DMG verification, readonly mount/copy install, app launch with `unsafeNoSandboxAbsent`, health, Office edit/re-extraction/restore/search, live and post-reload History diff/UI restore/read-back, Agent write/persisted receipt/replay/reload/History, `@` search/select/Escape, and all cleanup fields true |

Platform evidence retained without over-promotion:

- arm64 package inventory, DMG verification and packaged Office/History/Agent/@
  direct flows pass for product `feb906d6` at packaged audit checkpoint
  `99b6a993` after
  artifact-only ad-hoc signing. This is exact-current local package Evidence, not
  a Developer ID signature/notarization claim.
- x86_64 helper cross-compiles, but this arm64 host has no Rosetta and cannot
  execute it or an x64 DMG.
- Windows harness tests pass statically on macOS while the real matrix is an
  explicit skip. No MSVC/native runtime, `dist:win`, NSIS install/start, or
  Windows Office/History flow has run.
- Read-only external audit on 2026-08-11 found no remotely reachable current
  integration ref, no self-hosted Actions runner and no repository signing
  secrets; hosted workflow definitions alone are not execution Evidence.

## 5. 双轴审查

- **Standards axis:** pass for the local audit. Current product corrections are
  owner-scoped; full tests, lint, typecheck, client/server builds and the
  exact-current arm64 package/direct flows are green; missing platform evidence
  remains classified fail-closed.
- **Specification axis:** blocked. T-22 and T-23 do not satisfy their blocking
  platform rows; the former T-17 producer gap is closed by T-26.

## 6. 路径所有权审计

T-25 writes only its Evidence/status management files. T-15/T-20/T-16/T-18/
T-23/T-26 product changes and G10 corrections are recorded under approved
tickets/deviations. No package, lockfile, release config, credential, remote
ref or user namespace is changed
by this audit. Untracked/generated build helpers are not counted as Evidence.

## 7. 偏差与决策

- **D-T25-01:** retain `blocked` while T-22 is blocked and T-23 is review. This
  status decision waives no AC, DoD, platform row or producer contract.
- **D-G10-01 / D-G10-02 / D-G10-03:** closed. Speculo scripts are linted as
  Node sources, the two new style counts use existing tokens, and the deterministic
  CLI/persistence receipts match the current authorized source graph. The
  stale legacy event test now enforces ResourceIO-only ownership and the UI
  tests await their existing async states. No rule or baseline was weakened
  and no schema/DATA_EPOCH changed.
- No compatibility fallback, raw-path projection, platform substitute,
  unsigned-release exception, push or release action is authorized.

## 8. 残余风险与后续

1. Run T-22 on real Windows: filesystem matrix, MSVC helper, watcher/restore,
   extraction, `dist:win`, NSIS install/start, and Office/History E2E.
2. Complete T-23 on macOS x64 and a sleep-capable macOS runner: x64
   package/runtime, literal kernel descriptor accounting, and physical
   sleep/wake with actual loss/reconcile. Rebuild all platform packages if those
   owner changes advance the product SHA. Production signing/notarization stays
   outside T-23's local mode.
3. Re-run T-25 at the resulting final SHA. Do not reuse this blocked verdict as
   completion evidence.

## 9. Delivery Location

- **Product fixed point:** `feb906d6`; **quality fixed point:** `8aa9f7b1`; **final Evidence HEAD:** `7198ffcb` (docs-only after `99b6a993`).
- **Evidence:** `<Path>{roots.state}/specdev/changes/{change}/evidence/T-25.md</Path>`.
- **Ticket status:** `blocked`; umbrella change remains `active`.

## 10. Lead Integration Acceptance

- **Standards axis:** pass for audit scope.
- **Specification axis:** blocked by T-22/T-23 platform residuals.
- **Decision:** preserve the integrated product through `feb906d6` and quality
  checkpoint `8aa9f7b1`; do not mark
  the umbrella complete, archive it, or claim release readiness.
