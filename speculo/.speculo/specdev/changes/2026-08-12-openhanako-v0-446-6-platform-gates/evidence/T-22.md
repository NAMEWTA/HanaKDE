# Evidence: T-22 - Windows blocking gate

- **Change:** `2026-08-12-openhanako-v0-446-6-platform-gates`
- **Ticket:** `<Path>{roots.state}/specdev/changes/{change}/ticket/22-windows-blocking-gate.md</Path>`
- **Remote fixed point:** `origin/hanakde` / `61dcfcba5b7fe92cbf0c3219d1ac3d4114b41875`
- **Uncommitted product/test fingerprint:** SHA-256 `485ABFA47F334672A07845500260A07E922AE79491A0EA9F30A0B411AD252EB1` over 29 ordered non-Speculo paths
- **Host:** Windows 11 Pro 10.0.26200 x64; Volta Node 24.16.0; npm 11.13.0; Visual Studio 2022 17.14.37
- **Status:** done; all Windows-owned G2 rows closed

## 1. 实现摘要

The final Windows x64 matrix passed on the synchronized remote fixed point plus
the recorded local fingerprint. The run covered native filesystem behavior,
production package inventory, signed seed/standalone smoke, NSIS install,
installed-package direct flow, user-confirmed component repair and process
cleanup. No Windows blocking row was skipped.

## 2. 修改范围

- T-22 写入 Windows gate/direct-flow harness 与本 Evidence。
- T-27/T-28/T-29 产品修改分别由对应 owner 交付；T-22 只消费最终产物。
- 临时安装、HANA_HOME、签名材料和生成制品均不进入版本控制。

## 3. 验收与合同映射

| Contract | Result |
|---|---|
| AC-009/010、AC-014--023 | pass: Windows native filesystem/security/restore/Knowledge matrix |
| AC-027 | pass: formal NSIS build, inventory, install and direct flow |
| AC-029--031 | pass: dependency gate, packaged repair and optional startup behavior |

## 4. 验证执行

| Command or step | Result |
|---|---|
| `volta run npm ci` | pass; postinstall verified 49 production dependencies and Pi AI import |
| focused final Vitest matrix | pass: 13 files / 147 tests |
| `volta run npm run typecheck` | pass: root, node and test TypeScript projects |
| scoped ESLint over changed JS/TS paths | pass: 0 errors / 8 warnings |
| `volta run npm run dist:win` with matching ephemeral local key/keyset paths | pass in 730.6s; helper, client, server, signed seed, seed verification, standalone smoke and NSIS completed |
| `node scripts/platform/windows/run-gate.mjs --package dist/win-unpacked --installer dist/HanaAgent-0.447.4-Windows-x64.exe` | pass |
| silent NSIS install into `.cache/t22-final-20260821-installed` | pass: exit 0; main executable, seed, MinGit and sandbox helper present |
| installed direct flow | pass: app launch, health, workspace, office, agent and `@`; all app/server/sandbox cleanup fields true |
| isolated repair flow | pass: missing `typebox/build/index.mjs` detected, native repair dialog clicked, target restored, user sentinel preserved, authenticated health endpoint healthy |
| final owned process inspection | pass: 0 application/server/sandbox processes remained |

The native receipt reported case-insensitive filesystem behavior, junction
escape rejection, root identity change detection, locked rename denial,
rename-burst and temp-save observations, 62 recursive events, watcher closure
and an untouched outside target. Package inventory found the 0.447.4
server/renderer archives, manifest/signature, MinGit, Windows sandbox helper
and secure filesystem helper; the NSIS PE header was valid.

Installed direct-flow receipt:

```json
{"platform":"win32","arch":"x64","appLaunched":true,"healthReady":true,"workspace":true,"office":true,"agent":true,"at":true,"cleanup":{"appStopped":true,"serverStopped":true,"sandboxDisposed":true}}
```

### 4.1 Final artifacts

- Installer: `dist/HanaAgent-0.447.4-Windows-x64.exe`, 485,549,648 bytes, SHA-256 `86F69B61500968E9073668303D77F45855EAC453AB9F267E5551204D90D9896B`.
- Server seed: `server-0.447.4-win32-x64.tar.gz`, 182,679,291 bytes, SHA-256 `BFE6EC9BAD7CB1E8E1F9EBCC14717585DD3CA406D0277C71108116C934962111`.
- Secure helper: SHA-256 `0A3A9DD0A27058D087F1E00930C88A36C354D7A548A6F4D8FE5E3324D7A43D8D`.

### 4.2 Repair evidence

The healthy bootstrap activated the 0.447.4 seed in a new isolated
`HANA_HOME`. The test then moved only
`node_modules/typebox/build/index.mjs` to an adjacent backup and wrote
`user/t22-final-repair-sentinel.txt = preserve-user-state`.

The first and single retry failed with `ERR_MODULE_NOT_FOUND`, after which the
native dialog exposed `修复组件`, `修复并重启` and `退出`. The repair
button was clicked through the Win32 control. The target was restored, the
sentinel remained exact, the relaunched server PID was 7260, and its
authenticated `/api/health` response was healthy. The driver cannot observe
the relaunched process through the original stdout handle, so
`ReadyAfterRecovery=false` from that handle is not treated as a failure; the
new process, server-info receipt and authenticated health call are the
authoritative observations.

### 4.3 Quality observations

The repository-wide lint command is not green on the synchronized remote
baseline: 1,437 errors and 8,084 warnings, with 1,435 errors in the remote
`plugins/todolist/assets/page.js` alone. The changed-path lint has zero
errors. A default-worker full Vitest attempt became resource-unstable and a
serial attempt exceeded 20 minutes; neither is claimed as a pass. The observed
`FileHistoryModal` failure passed 9/9 when isolated. These repo-wide baseline
and execution-capacity findings are retained for T-25 and do not weaken the
147-test Windows-owned matrix.

The server build also emitted the existing non-fatal NFT trace fallback
`Debug Failure. Output generation failed`; runtime dependency installation,
native smokes, signed seed, standalone smoke, package launch and direct flow
all subsequently passed.

## 5. 双轴审查

- **Standards axis:** pass. The Windows runner is fail-closed, temporary state is isolated, and final package claims are backed by the installed artifact.
- **Specification axis:** pass for AC-009/010, AC-014--023, AC-027 and AC-029--031 on Windows.
- **Path ownership:** T-22 harness changes remain inside its writable paths; product fixes belong to T-27/T-28/T-29.

## 6. 路径所有权审计

T-22 的永久修改只在 Windows platform harness/test writable paths；产品路径变更由
T-27/T-28/T-29 Evidence 审计。无 release、remote 或真实用户 Profile 写入。

## 7. 偏差与决策

无合同豁免。错误环境变量导致的两次签名失败不计为通过 Evidence；仅匹配
`HANA_SIGN_KEY`/`HANA_SIGN_KEYSET` 的 exit-0 正式构建被采纳。

## 8. 残余风险与后续

整仓 lint/full-suite 基线问题与 NFT trace fallback 已显式保留。T-22 `done`
只关闭 Windows 半边 G2；T-23 仍 blocked，T-25 不得开始。
