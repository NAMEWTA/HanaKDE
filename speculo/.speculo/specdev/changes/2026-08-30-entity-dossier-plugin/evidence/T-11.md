# Evidence: T-11 — 装配 Hana Dossiers 插件并完成主机 E2E

- **Change：** `2026-08-30-entity-dossier-plugin`
- **Ticket：** `<Path>{roots.state}/specdev/changes/2026-08-30-entity-dossier-plugin/ticket/11-plugin-composition-and-e2e.md</Path>`
- **Spec / Goal Plan：** `<Path>{roots.state}/specdev/changes/2026-08-30-entity-dossier-plugin/spec.md</Path>` / `<Path>{roots.state}/specdev/changes/2026-08-30-entity-dossier-plugin/goal-plan.md</Path>`
- **Lead：** `root`
- **Workspace/branch：** `current` / `hanakde`
- **Base/implementation/result SHA：** `034eb8ece16e057f2cfa41fc9e3d6a3e358d65a8` / `00d8cd071654913f4654b8fc5a81569a6f1b6448` / `00d8cd071654913f4654b8fc5a81569a6f1b6448`
- **Result tree SHA：** `f7b7b007a1d89549c6f0cf3e2f80a8b6b7c700ca`
- **状态：** done

## 1. 实现摘要

完成 `dossiers` builtin 插件装配：manifest、entry、34 个 Agent tools、同插件 Page route、生产 browser bundle、最小 ResourceIO/picker 权限、构建与独立发布目录 smoke。Page 只保存正式 ResourceRef，browser 只经 `hana.resources.pick` 与 `hana.api.fetch("ui/dispatch")` 工作；route 在写入前执行 compatibility gate，并组合目录、资料、联系人、索引、模型开关、生命周期、ZIP 交换与迁移应用服务。

真实 Hana 主机上的桌面与窄屏旅程已覆盖组织档案创建、资料复制、元数据搜索、模型关闭、回收/恢复、ZIP 导出检查/确认导入、迁移 ready、reload 持久化；另以停止并重启主机验证相同 dossier/revision/document 保持。产品 diff 仅位于 `<Path>plugins/dossiers/**</Path>`。

## 2. Lead Dispatch And Candidate Return

- **Implementation owner：** Lead；current/direct，base `034eb8ece16e057f2cfa41fc9e3d6a3e358d65a8`。
- **返回：** commit `00d8cd071654913f4654b8fc5a81569a6f1b6448`；16 个 T-11 白名单文件；产品工作树 clean，仅余 Speculo 状态工件。
- **Lead 独立核对：** pass；重读固定 diff/tree/ancestor、manifest/SDK 边界、生产 bundle、standalone package、真实主机结果、截图和临时目录清理。
- **Subagent：** 未派遣。

## 3. 修改范围与路径所有权

| 路径 | 所有权 | 改动目的 |
|---|---|---|
| `<Path>plugins/dossiers/manifest.json</Path>`、`package.json`、`tsconfig.json`、`build.ts`、`index.ts` | T-11 writable | 插件身份、最小权限、入口、生产构建和验证命令 |
| `<Path>plugins/dossiers/routes/page.ts</Path>` | T-11 writable | same-plugin dispatch、ResourceRef 校验、兼容门及应用服务组合 |
| `<Path>plugins/dossiers/src/ui/page.tsx</Path>`、`browser-app.ts`、`assets/**` | T-11 writable | 全页面 shell、host bridge、响应式生产资源 |
| `<Path>plugins/dossiers/scripts/**</Path>`、`tests/e2e/**`、`README.md` | T-11 writable | 精确发布包 smoke、真实宿主 E2E 与使用边界 |

- **read-only 修改：** 无；T-01–T-10 共享/上游路径未修改。
- **未声明路径/根依赖/锁文件：** 无。
- **Evidence assets：** `<Path>{roots.state}/specdev/changes/2026-08-30-entity-dossier-plugin/evidence/assets/T-11-desktop-journey.png</Path>`、`<Path>{roots.state}/specdev/changes/2026-08-30-entity-dossier-plugin/evidence/assets/T-11-narrow-journey.png</Path>`。

## 4. 验收与合同映射

| Contract | 验证接缝 | 证据 | 结果 |
|---|---|---|---|
| AC-001 | Git diff allowlist | `034eb8ec..00d8cd07` 恰为 16 个文件，全部位于 `<Path>plugins/dossiers/**</Path>` | pass |
| AC-002 | real PluginManager discovery | builtin source、full-access trust、无 load error；Dossiers Page/routes/lifecycle 与 34 个 namespaced tools 均被真实主机加载 | pass |
| AC-017 | real-host Playwright | desktop 1440x900 与 narrow 390x844 共 4/4；完整旅程、键盘、无横向溢出、无 401/403；两张截图人工核对无空白/重叠/截断 | pass |
| AC-032 | exact standalone package smoke | 隔离发布目录加载 38 个 bundled entries、34 tools，生产依赖随包解析，无 repo-only import | pass |

## 5. Workspace Verification

| 命令或步骤 | 运行环境 | 结果 | 摘要 |
|---|---|---|---|
| `npm run verify` | current-workspace / plugin package | pass | typecheck；core 106/106；UI 18/18；production build；syntax；standalone smoke 38 entries/34 tools |
| production React scan | current-workspace | pass | bundle 695,984 bytes；无 DevTools/dev modules，构建固定 `NODE_ENV=production` |
| real Hana Playwright | current-workspace / actual host workbench | pass | 2 projects × 2 tests = 4/4；完整用户旅程与 responsive/keyboard checks |
| host restart rehearsal | current-workspace / real Hana server | pass | 重启前 dossier rev2；重启后相同 ID/revision 2 与 `restart-source.txt` 可见 |
| PluginManager diagnostics | current-workspace / real Hana server | pass | builtin/full-access/visible/activated；Page/routes 与 34 tools；diagnostics error `null` |
| exact package verifier | isolated temporary directory | pass | 38 bundled entries、34 tools；better-sqlite3/yauzl runtime dependencies 可解析 |
| static boundary/privacy scan | current-workspace | pass | runtime 无 raw fs、external fetch、`model.sample`、iframe ticket、硬编码机器路径；browser 仅两个公开 host API |
| Git fixed-point audit | current-workspace | pass | HEAD/result/tree/ancestor 通过；`plugins/dossiers` 无未提交改动 |
| Speculo structural validator | current-workspace | pass | `--stage implement` 与 `--stage complete` 均为 0 errors、0 warnings |
| Speculo repo-aware validator | current-workspace | tool limitation | 21 个误报：对 T-01–T-10 的历史 current result 逐项要求等于最终 HEAD，并对未归档 Speculo 工件逐 Ticket 重复报告 dirty；未改写真实 SHA |
| process/temp cleanup | host/workbench | pass | 测试 server 已停止；E2E/restart/debug 临时目录为 0；collection-only 不遗留目录 |

- **失败后修复与重跑：** 首轮真实资料提交暴露 native secure-fs helper 未构建及测试目录不在 active workbench；按仓库既有构建补齐 helper，并将 E2E 工作区显式参数化到实际 workbench。随后修正自定义 toggle label 定位、Playwright collection-time 临时目录泄漏和 React development bundle；每项均完成针对性重跑及最终 all-up rerun。
- **未运行检查：** builtin 插件不能通过仅接受 `source=dev` 的 `plugin.dev.invokeTool` 端点调用，因此未伪造 dev shadow 安装。真实宿主完成 34 tools 的发现/加载；工具执行语义由 T-04 的确定性 application/tool/permission tests 覆盖。
- **E2E：** required / passed；由 Lead 在 current-workspace 的真实 Hana 主机运行。

## 6. 双轴审查

### 标准轴

- **固定输入：** `034eb8ece16e057f2cfa41fc9e3d6a3e358d65a8..00d8cd071654913f4654b8fc5a81569a6f1b6448`
- **来源：** workspace `AGENTS.md`、hana/plugin creator instructions、`PLUGIN_SDK.md`、插件 package/tsconfig 与代码审查规则。
- **结果：** pass。
- **Findings 与修正：** production bundle 起初包含 React dev modules，已在 build define 中固定 production；E2E 临时目录起初在 test collection 创建，已移入 `beforeAll` 并验证 `--list` 零遗留。最终无未解决 finding。

### 规范轴

- **固定输入与来源：** Spec、ADR、CONTEXT、T-11、Goal Plan；合同 AC-001/002/017/032。
- **结果：** pass。
- **核对：** 插件盒子、ResourceIO、相对引用、显式写入/确认、无后台模型、可恢复迁移、完整 Page、独立包与真实宿主 Gate 均有可定位证据；无产品范围偏差。

## 7. Integration Verification

| 项目 | 结果 |
|---|---|
| Parent before SHA | `034eb8ece16e057f2cfa41fc9e3d6a3e358d65a8` |
| Implementation/source SHA | `00d8cd071654913f4654b8fc5a81569a6f1b6448` |
| Candidate branch/workspace | current；candidate not-applicable |
| Method/conflicts | direct-parent；无冲突 |
| Integration checks | verify、生产 bundle、真实 host E2E/restart、standalone、boundary/privacy、fixed-point 全通过 |
| E2E disposition/result | required / passed；desktop+narrow 4/4，restart persistence passed |
| Parent result/re-read | `00d8cd071654913f4654b8fc5a81569a6f1b6448`；tree `f7b7b007a1d89549c6f0cf3e2f80a8b6b7c700ca`；base/result ancestor pass |

## 8. 偏差与决策

- **偏差：** 无产品范围、路径、公开 SDK 或用户数据边界偏差。
- **本机前置：** 真实 host 的 native secure-fs helper 使用仓库既有脚本生成，产物被既有 ignore 规则排除且未进入 commit。
- **授权边界：** 仅本地 implementation commit 与 direct-parent；未执行 marketplace、push、PR、remote merge、deploy、真实用户迁移或 source cleanup。

## 9. 残余风险与交付定位

- **残余风险/已知限制：** Page invocation 不持有 Agent invocation token，因此不把模型建议伪装成可用 Page 功能；Agent tools 保持独立公开贡献。builtin 工具的真实 host 执行端点受 host dev-source policy 限制，发现/加载已验证，执行合同由 T-04 自动化覆盖。当前 validator 的 repo-aware 算法不支持同一 branch 上多个串行 `current` result fixed points；结构合同全绿，Git fixed point 由明确的 HEAD/tree/ancestor 命令独立核对。
- **后续 Ticket：** 无；G7 已关闭，Change 可完成，Archive 需独立 Work。
- **Source commit / Parent result：** `00d8cd071654913f4654b8fc5a81569a6f1b6448` / `00d8cd071654913f4654b8fc5a81569a6f1b6448`。
- **Source workspace：** `current`。
- **Evidence：** `<Path>{roots.state}/specdev/changes/2026-08-30-entity-dossier-plugin/evidence/T-11.md</Path>`
